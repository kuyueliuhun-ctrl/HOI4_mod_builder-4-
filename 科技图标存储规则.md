# HOI4 科技（Technology）图标存储规则

> 研究日期：2026-01
> 方法：官方 wiki（[Technology modding](https://hoi4.paradoxwikis.com/Technology_modding)，[存档快照](https://web.archive.org/web/20260101141634/https://hoi4.paradoxwikis.com/Technology_modding)）＋ 游戏本体实测（`E:\SteamLibrary\steamapps\common\Hearts of Iron IV`）＋ 5 个现成 mod 实测（`E:\mods\`）。

---

## 一、结论摘要（TL;DR）

科技图标**不是**在科技定义文件里指定图片路径，而是**三层结构**：

```
common/technologies/*.txt        科技定义（不含任何图片字段）
        ↓ 约定：科技 id = my_technology_1
interface/*.gfx                  sprite 注册（名字 = GFX_<科技id>_medium）
        ↓ 约定：纹理路径
gfx/interface/technologies/*.dds 图片文件（扁平目录，文件名任意）
```

- 科技图标 = `interface/*.gfx` 中注册的 `spriteType`，**名字必须是 `GFX_<科技id>_medium`**（国家专属：`GFX_<TAG>_<科技id>_medium`）。
- 纹理统一放 `gfx/interface/technologies/`（**扁平目录**，游戏本体 1318 个文件，全部 dds；mod 用 png 同样被引擎接受）。
- **文件名与科技名无关**：例如 `GFX_infantry_weapons_medium` 实际指向 `infantry_equipment_0.dds`。
- 尺寸**没有固定值**：纹理按原始像素绘制，由 `interface/countrytechtreeview.gui` 里每个 folder 的 item 盒子决定摆放；实测从 64×64 方图到 249×54 宽条都有。
- 常见误区：科技文件里的 `path = { ... }` 是**科技树连线**（`leads_to_tech`/`research_cost_coeff`），不是图片路径！

---

## 二、三层结构详解

### 第 1 层：科技定义 `common/technologies/*.txt`

科技块（实测 `infantry.txt`）：

```
infantry_weapons = {
    enable_equipments = { infantry_equipment_0 }
    path = {                          # ← 这是科技树连线，不是图标！
        leads_to_tech = infantry_weapons1
        research_cost_coeff = 1
    }
    research_cost = 1.5
    start_year = 1918
    folder = { name = infantry_folder position = { x = 0 y = -1 } }
    categories = { infantry_weapons }
    ...
}
```

- 全游戏 `common/technologies/*.txt` 共 390 条 `path = {`，**全部是连线块**（`leads_to_tech`），无一是图片路径。
- 全文件里唯一一条 `icon = "GFX_JAP_improved_heavy_tank_medium"` 位于 `NSB_armor.txt` 的 `if/upgrades` 块内——那是**坦克设计器模块**的图标（sprite 指向 `gfx/interface/technologies/JAP_imp_heavy_tank.dds`），不是科技图标。即：**坦克设计器模块图标也复用 technologies 目录**。
- 科技图标完全不写在科技定义里，靠 sprite 名字约定解析。

### 第 2 层：sprite 注册 `interface/*.gfx`

官方 wiki 原文（Technology modding → "Name and icon"）：

> The icon is defined as a spriteType within any `/interface/*.gfx` file, by prepending the name of the technology with `GFX_` and appending `_medium` to the end.
>
> 国家专属：inserting the country tag between `GFX` and the name, e.g. `GFX_HON_my_technology_1_medium`.
>
> 装备借用：if the equipment doesn't have a unique icon defined, then it will use the one defined by the technology（装备无专属图标时借用科技图标）。

游戏本体实测：

```
# interface/Technologies.gfx  L183
spriteType = {
    name = "GFX_infantry_weapons_medium"
    texturefile = "gfx/interface/technologies/infantry_equipment_0.dds"
}

# interface/famous_battleships_2d.gfx
SpriteType = {
    name = "GFX_ENG_advanced_battleship_medium"
    texturefile = "gfx/interface/technologies/ENG_advanced_battleship.dds"
}
```

- 全游戏 `GFX_*_medium` 类 sprite 共 **2133** 个；指向 technologies 目录的 `texturefile` 共 **1894** 条（部分文件被多个 sprite 复用，因此 > 1318 文件数）。
- 主注册文件：`interface/Technologies.gfx`（2200+ 行），其余散落在 `famous_battleships_2d.gfx`、`equipment.gfx`（舰船 hull 图标）等。
- 注意 `texturefile` 的斜杠写法 `gfx//interface//technologies//` 与 `gfx/interface/technologies/` 混用，引擎均接受。

### 第 3 层：图片文件 `gfx/interface/technologies/`

- **扁平目录，无子目录**（游戏本体与 mod 均如此）。
- 游戏本体：1318 个文件，100% `.dds`。
- 引擎接受 PNG：mod 3228475937 的 28 个科技图标全部是 `.png`，且**与 sprite 引用 1:1 对应**（28 个 texturefile 引用 = 28 个文件）。

---

## 三、图片规格（实测）

### 3.1 格式

| 来源 | 格式 | 备注 |
| --- | --- | --- |
| 游戏本体（采样 200 个） | DDS **未压缩 A8R8G8B8** 32bit + alpha | 采样 200 个全部如此；pixel format flags=0x41（ALPHAPIXELS\|RGB），masks R=00FF0000 G=0000FF00 B=000000FF A=FF000000；无 DXT1/DXT5/DX10 |
| 游戏本体 | 部分带 mipmap | 采样 200 个中 25 个 mipmap 数 > 1 |
| mod 3228475937 | PNG（带 alpha） | 引擎直接接受 |

结论：**32bit 带 alpha 的 DDS（或 PNG）**即可；压缩格式（DXT）非必需。

### 3.2 尺寸（无固定值，实测表）

| 文件 | 尺寸 | 用途 |
| --- | --- | --- |
| `infantry_equipment_0.dds` | 120 × 24 | 步兵装备科技（宽条） |
| `infantry_weapons.dds` | 64 × 64 | 方图 |
| `tank_icon.dds` | 76 × 42 | 默认占位（`GFX_technology_medium`） |
| `tank_small_icon.dds` | — | 小项占位（`GFX_technology_small`） |
| `advanced_heavy_cruiser.dds` | 249 × 54 | 舰船科技（超宽条） |
| `AA_1_allies.dds` | 103 × 50 | 防空炮科技 |
| mod `SOV_EX_t72.png` | 268 × 68 | 坦克科技（宽条） |
| mod `water_tank.png` | 268 × 76 | 宽条 |
| mod `SOV_EX_support_equipment_2.png` | 133 × 55 | 支援装备 |

尺寸决定因素 = **GUI 盒子**（见下）。纹理按**原始像素尺寸**绘制（`centerposition = yes` 时以盒子内某点为圆心摆放），不做缩放。

### 3.3 GUI 盒子（显示尺寸的真正决定者）`interface/countrytechtreeview.gui`

```
containerWindowType = {
    name = "techtree_infantry_folder_item"     # 常规项（每个 folder 一个）
    size = { width = 183 height = 84 }
    background: GFX_technology_unavailable_item_bg
    iconType { name = "Icon" position = { x=91 y=50 }
               spriteType = "GFX_technology_medium" centerposition = yes }   # ← 运行时替换为 GFX_<科技id>_medium
}
containerWindowType = {
    name = "techtree_infantry_folder_small_item"   # 小项
    size = { width = 72 height = 72 }
    iconType { name = "Icon" position = { x=3 y=3 } spriteType = "GFX_technology_medium" }
}
```

- 常规项盒子 **183×84**，小项盒子 **72×72**；宽条图标（120×24、249×54、268×68）正是为常规项盒子设计的比例。
- 每个科技 folder 在 GUI 中都有对应的 item 定义（`techtree_<folder>_item` / `_small_item`）与 5 态背景 sprite（available / unavailable / researched / currently_researching / branch_item_bg，命名 `GFX_technology_<folder>_<state>_item_bg`）。
- **wiki 明确：新增科技 folder 必须改 GUI**（countrytechtreeview.gui），否则科技不显示——除非挂在已有科技（有 path）下。

### 3.4 子科技（sub-technology）

- 子科技**没有** `GFX_<名字>_medium` 图标；GUI 里是固定槽位（`sub_technology_slot_0..N`，35×26 盒子）+ 固定 sprite 集合（wiki 所谓 "a set of indices"）。
- 实测集合（`interface/Technologies.gfx` L2416+）：`GFX_subtech_td`、`GFX_subtech_rocket`、`GFX_subtech_art`、`GFX_subtech_aa`、`GFX_subtech_cag`、`GFX_subtech_heavy_battleship`、`GFX_subtech_carrier_plane` 等；纹理多在 technologies 目录（`subtech_*.dds`），个别直接放 `gfx/interface/`。

---

## 四、五个现成 mod 的实测证据（约定一致性）

| mod | 科技文件 | 图标文件 | `GFX_*_medium` sprite 数 | 做法 |
| --- | --- | --- | --- | --- |
| 3228475937 | 1（SOV_EX_tech.txt） | 28（全 png） | 151 | `GFX_water_tank_medium → gfx/interface/technologies/water_tank.png`；`GFX_SOV_EX_tank_t72_medium → SOV_EX_t72.png`；文件夹引用 1:1 |
| 2879371758 | 27 | 301 | 14585 | 同约定（如 `GFX_FRG_basic_light_tank_chassis_medium`） |
| 3273220145 | 3 | 388 | 1699 | 同约定 |
| 3350890356 | 18 | 192 | 3487 | 同约定 |
| 2064073319 | 3 | 无 | 13 | 科技无专属图标（复用原版/借用装备图标） |

结论：**不同作者、不同体量的 mod 全部遵守同一约定**（sprite 名 `GFX_<id>_medium` + 纹理在 `gfx/interface/technologies/`），且允许无图标科技存在（引擎用占位/借用机制）。

---

## 五、给新科技配图标的操作清单

1. 制作图片：32bit 带 alpha 的 `.dds`（或 `.png`），宽条比例（参考 268×68 / 120×24 风格，具体由目标 folder 的 GUI 盒子决定）。
2. 放入 `gfx/interface/technologies/<任意文件名>.dds`（扁平目录）。
3. 在 mod 的 `interface/*.gfx` 注册：
   ```
   spriteType = {
       name = "GFX_my_technology_1_medium"
       texturefile = "gfx/interface/technologies/my_icon.dds"
   }
   ```
4. 国家专属版本：`GFX_TAG_my_technology_1_medium`（sprite 名里插国家标签）。
5. 若科技挂在已有 folder/科技下（有 `path`），**无需改 GUI**；若新建 folder，必须同步改 `interface/countrytechtreeview.gui` 的 item 定义。

---

## 六、对编辑器的意义（图标完整性校验算法建议）

结合本项目已有的 `icon_resolver` 全局索引（mod+游戏 `gfx/` 递归、小写文件名缓存），可实现的科技图标校验：

1. 解析 `common/technologies/*.txt` 得到科技 id 集合（`_quick_focus_scan` 同款轻量解析即可）。
2. 对每个 id：在 mod+游戏 `interface/*.gfx` 中查找 `name = "GFX_<id>_medium"`（含 `GFX_<TAG>_<id>_medium`）。
3. 命中 sprite → 用其 `texturefile` 在全局索引中验证文件存在。
4. 未命中 → 报告"无专属图标"（合法：引擎借用/占位，但 mod 作者常想补图）。
5. 目录清理辅助：`gfx/interface/technologies/` 中未被任何 sprite 引用的文件可标为冗余。

参考链接：
- [HOI4 wiki: Technology modding](https://hoi4.paradoxwikis.com/Technology_modding)（本次读取的[存档快照](https://web.archive.org/web/20260101141634/https://hoi4.paradoxwikis.com/Technology_modding)）
- [Steam 讨论：How to create an icon to HOI4 mod](https://steamcommunity.com/app/394360/discussions/0/1762481957320980105/)
- [HOI4 wiki: Graphical asset modding](https://productionwiki-hoi4.paradoxwikis.com/Graphical_asset_modding)
