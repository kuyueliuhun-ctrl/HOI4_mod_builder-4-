# HOI4 Mod 编辑器 — 项目问题分析与优化意见

**总体判断**：这是一个功能覆盖面很广（90+ 内容类型）、且已经建立了「写入纪律/契约测试」这类少见良好实践的单人项目。但它有明显从「能跑」向「可维护」过渡阶段尚未完成的结构性问题，以及若干会影响他人使用/协作的实际缺陷。

---

## 一、严重问题（会直接影响协作与安全）

### 1. `settings.json` 泄露个人路径且被版本控制跟踪 ⚠️

- 证据：`git status` 显示 `M settings.json`（已跟踪），文件内容含真实个人路径：
  - `E:/SteamLibrary/steamapps/common/Hearts of Iron IV`
  - `C:/Users/枯月流魂/Documents/Paradox Interactive/Hearts of Iron IV/mod`
- 后果：
  1. 真实用户名（枯月流魂）被提交进仓库历史；
  2. 其他人 clone 后路径全失效，启动即触发「目录无效清空」逻辑。
- 建议：`settings.json` 移出版本控制，改为 `settings.example.json` + `.gitignore` 忽略 `settings.json`。

### 2. `.gitignore` 过于简陋（仅 3 行）

当前内容只有：

```
__pycache__/
*.pyc
.runtime/
```

但工作区实际存在 `.venv/`、`.idea/`、`.jspace/`、`.opensquilla-cache/`、`__pycache__/` 等目录，其中 `.jspace/` 已被 git 显示为 untracked。`.venv/`（含完整虚拟环境）一旦被误 add，仓库会爆炸。

- 建议：补齐 `.venv*/`、`.idea/`、`.jspace/`、`.opensquilla*/`、`settings.json`、`*.log`。

### 3. 依赖清单不完整

- 只有 `requirements-wsl.txt`（WSL/Linux 用），但主平台是 Windows（`启动.bat` 用 `.venv\Scripts\python.exe`）。
- 缺少 Windows 的 `requirements.txt`，也没有版本锁定（`PyQt6`、`numpy`、`Pillow` 全是不带版本的裸声明）。他人无法复现环境。

---

## 二、架构问题（可维护性风险）

### 4. 巨型 God Class / 巨型文件

- `focus_view.py` **112 KB**、`generic_tree_editor.py` 52 KB、`division_editor.py` 43 KB、`main_window.py` 规模同样庞大。
- 单一类同时承担：画布渲染、拖拽、菜单、图标上传、翻译回显、文件定位……违反单一职责，任何改动都需通读上千行上下文。
- 证据：`focus_view.py` 内 `from workbench import ...` 延迟导入出现 **15 处**，说明该类与工作台深度耦合。

### 5. 循环依赖靠「函数内延迟导入」硬解

全项目有 **30+ 处**函数内 `from ... import`（`focus_view`→`workbench`、`api_server`→`workbench`、`game_data`→`workbench` 等）。这是循环依赖的表征，而非解决方案：

- 它把导入错误推迟到运行时，破坏了依赖的静态可见性；
- 说明 `workbench`（工作台/类型注册）与业务层没有清晰边界。

建议：抽出 `content_types.py`（纯数据：CONTENT_TYPES、ICON_RULES 等），让 `workbench` 只做 UI 组装。

### 6. 模块级全局单例 + 反复读盘

- `focus_view.py` 用 `_translator`、`_loc_manager` 模块级缓存，需手动 `reload_translator()` 重置。
- `_get_settings()` 每次调用都重读 `settings.json`（注释说「不缓存」），被 `focus_view`/`gui_translator`/`localization_mgr` 等反复调用——`settings.json` 被当成了**进程间/模块间通信通道**。
- 建议：引入单一 `AppConfig` 对象（启动时加载一次，路径变更时显式更新），而不是散落的多处读盘。

---

## 三、代码质量问题

### 7. 异常吞噬泛滥（60+ 处 `except Exception:`）

- 大量 `except Exception:` 后接 `pass` / `continue` / 返回默认值（`ai_assist.py`、`bop_loader.py`、`api_server.py`、`building_lib.py` 等）。
- 后果：真实错误被静默吞掉，用户只看到「没反应」，排查困难。尤其 `api_server.py` 的核心操作也大量吞异常，会返回误导性的「成功/空结果」。
- 建议：至少记录日志；对外接口应区分「预期失败」（返回结构化错误）与「意外异常」（带 traceback 上报）。

### 8. 冗余/未使用导入

- `main_window.py` 第 7 行 `import os` 后又第 13 行 `import os as _os`，`_os` 未见使用。
- 这类残留说明经历过多次重构，缺少 lint（flake8/ruff）把关。

---

## 四、解析器正确性（本项目核心能力，需重点审视）

### 9. `pdx_parser.py` 是「简化版」解析器，非完备 PDX 解析

- 分词正则为 `\{|\}|=|"[^"]*"|[\w\.\-]+`，**不识别比较运算符**（`>` `<` `>=` `<=` `==` `!=`）、括号、`@`、逗号分隔列表等。
- `parse_focus_file` 里大量 trigger 表达式（如 `has_political_power > 100`）会被错误切分或吞掉运算符。
- 结论：它适合「树形浏览/编辑结构块」，但**不能作为语义校验的依据**。当前 `validate_file` 依赖正则二次提取（`traits\s*=\s*\{...\}`），两条解析路径并存且互不对账。
- 注释剥离 `re.sub(r'#.*', '', text)`：默认 `.` 不跨行所以只删到行尾——基本正确，但**字符串值内若含 `#`（如某些颜色/路径）会被截断**。这是隐患。

### 10. `focus_parser.py` 与 `pdx_parser.py` 功能重叠

两套手写递归下降解析器并存，`focus_parser` 还维护了自己的 `KNOWN_KEYS` 白名单和 `_find_block_end_char` 等边界修正逻辑。功能重叠意味着修复一处 bug 需要同步两处，容易产生不一致。

---

## 五、测试与工程化

### 11. 测试是「单文件巨型契约测试」

- `tests/test_contracts.py` 高达 **251 KB**（约 4000+ 行），所有测试类堆在一个文件里。
- 这是少见的「写入契约/健康检查/纪律扫描」良好实践（值得肯定），但单文件规模已经影响可读性与增量运行速度。
- 建议：按模块拆分为 `test_write_contracts.py`、`test_health_check.py`、`test_parser.py` 等。

### 12. 无 CI、无 `pyproject.toml`、无类型注解

- 全项目无 `pyproject.toml`/`setup.cfg`/`tox.ini`；无 GitHub Actions 跑 `unittest` 或 `check_write_discipline.py`。
- 写入纪律靠人工运行 `tools/check_write_discipline.py`，没有自动化保障，纪律会随开发逐渐失效。

---

## 六、建议的优先级排序

| 优先级 | 事项 | 成本 | 收益 |
|---|---|---|---|
| P0 | `settings.json` 移出版本控制 + 补 `.gitignore` | 低 | 消除隐私泄露/协作障碍 |
| P0 | 补 Windows `requirements.txt`（带版本） | 低 | 可复现 |
| P1 | 抽出 `content_types.py`，打破 `workbench` 循环依赖 | 中 | 长期可维护 |
| P1 | 引入 `AppConfig` 单例，替代散落读盘 | 中 | 消除状态混乱 |
| P1 | 异常处理改为「日志 + 结构化错误」 | 中 | 可排查性 |
| P2 | 拆分巨型文件（focus_view/generic_tree_editor） | 高 | 长期可维护 |
| P2 | 拆分测试文件 | 低 | 可读性 |
| P2 | 加 ruff/flake8 + CI | 中 | 自动化质量门禁 |
| P3 | 统一/增强 PDX 解析器（识别比较符、完整词法） | 高 | 校验准确性 |

---

## 附：证据索引

- `settings.json`：真实个人路径（`E:/SteamLibrary/...`、`C:/Users/枯月流魂/...`），且被 git 跟踪（`git status` 显示 `M settings.json`）。
- `.gitignore`：仅 3 行，未忽略 `.venv/`、`.idea/`、`.jspace/` 等。
- `requirements-wsl.txt`：唯一依赖清单，无 Windows 版本、无版本锁定。
- `focus_view.py`（112 KB）：15 处 `from workbench import ...` 延迟导入，模块级 `_translator`/`_loc_manager` 单例，`_get_settings()` 每次重读盘。
- `main_window.py`：`import os` 与 `import os as _os` 并存。
- `pdx_parser.py`：分词正则不识别比较运算符；`re.sub(r'#.*', ...)` 剥离注释会截断含 `#` 的字符串值。
- `focus_parser.py`：与 `pdx_parser.py` 功能重叠，维护独立 `KNOWN_KEYS` 白名单。
- `tests/test_contracts.py`（251 KB）：单文件巨型测试。
- 全项目 30+ 处函数内 `from ... import`（循环依赖表征），60+ 处 `except Exception:`（异常吞噬）。
