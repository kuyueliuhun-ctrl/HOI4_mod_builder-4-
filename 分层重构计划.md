# 四层分离开发规范 — 整合重构计划

> 本文档是 `AGENTS.md` §4 第 9 条「四层分离开发规范」的落地执行计划。
> 目标：在不改变功能与 UI 行为的前提下，把项目从「能跑」提升到「可维护」，
> 把每个工作台/编辑器的职责明确拆为：
> **算法层（Core Algo）← 绘图层（Render）← UI 层（Widget/Layout）← 信号槽层（Controller/Binding）**
>
> 依赖方向单向向下，下层禁止反向依赖上层。
> 判定顺序：算法 > 绘图 > UI > 信号槽（信号槽是最薄兜底层）。

---

## 一、已完成基线（此前计划已落地，作为本次计划的起点）

| 模块/工作台 | 已完成的改造 | 分层现状 |
|---|---|---|
| AI 内容编辑器（8 类） | 专用 UI + 固定侧边栏，移除 GenericTreeEditor 依赖（§6.19） | `ai_loader.py`（算法/数据）+ `ai_*_editor_dialog.py`（UI/信号槽）+ `ai_ui_common.py`（公共 UI），已基本符合四层 |
| 地图画布渲染 | 瓦片缓存/矢量填充/边界烘焙（§6.3） | `map_loader/map_vector/map_fill`（算法）+ `map_canvas`（绘图+UI），绘图与算法已分文件 |
| 舰艇/飞机/坦克设计器 | 数据层 + 设计器 UI（§6.6~6.9） | `*_design.py`（算法/数据）+ `*_design_dialog.py`（UI/信号槽），已基本符合四层 |
| 编制编辑器 | 数据层 + DivisionEditor v2（§6.5） | `oob_loader.py`（算法/数据）+ `division_editor.py`（UI/信号槽），已基本符合四层 |
| BOP 力量平衡 | 专用工作台（§6.16） | `bop_loader.py`（算法/数据）+ `bop_editor_dialog.py`（UI/信号槽），已基本符合四层 |
| 通用树形编辑器 | 已分离 `tree_node.py`（算法）+ `tree_model.py`（模型） | `generic_tree_editor.py` 本体仍偏大（52KB），需进一步把 UI 与信号槽/写文件编排分离 |

**结论**：新近开发的工作台大多已按「数据/算法层 + UI/信号槽层」自然分层；本次计划的核心不是推倒重来，而是：
1. 把**历史遗留的 God Class**（`focus_view.py` 为主）拆开；
2. 把**已分层但边界模糊**的工作台（`workbench.py`、`generic_tree_editor.py`、`main_window.py`、`map_canvas.py`）进一步收敛；
3. 把四层规范固化为新增代码的默认纪律。

---

## 二、总体阶段

| 阶段 | 内容 | 风险 | 依赖 |
|---|---|---|---|
| **P0 规范落地** | 写入 AGENTS.md §4.9；建立本计划文件；补充契约测试模板（算法层纯函数测试/绘图 offscreen 测试/UI 冒烟测试） | 低 | 无 |
| **P1 试点拆分** | 拆分 `focus_view.py`（科技树先行试点） | 中 | P0 |
| **P2 全工作台收敛** | 逐个工作台按四层对齐（见下） | 中 | P1 |
| **P3 自动化门禁** | 依赖方向静态检查、体积门槛、CI 接入 `verify_contracts.py` | 低 | P2 |

---

## 三、按工作台定位的详细计划

> 每个工作台给出：现状 → 目标分层 → 具体动作 → 验证。

### 1. 主工作台 `workbench.py`（WorkbenchDock）

- **现状**：`CONTENT_TYPES` / `ICON_RULES` / `SPECIAL_TYPE_KEYS` 等**纯数据**直接定义在 UI 模块内；`_quick_*_scan` 实体解析器也混在 UI 类里。
- **目标**：
  - 算法/数据层：`content_types.py`（纯数据：类型注册、图标规则、特殊类型）+ `entity_scanner.py`（实体解析/扫描纯函数）
  - UI 层：`workbench_view.py`（列表/布局/控件）
  - 信号槽层：`workbench.py` 保留为薄壳（路由、双击分发、无文件模式装配）
- **具体动作**：
  1. 从 `workbench.py` 抽出 `CONTENT_TYPES`、`ICON_RULES`、`SPECIAL_TYPE_KEYS` 到 `content_types.py`；
  2. 把 `_quick_*_scan` 纯解析逻辑移到 `entity_scanner.py`，UI 只调用结果；
  3. `workbench.py` 改为 import `content_types` / `entity_scanner`，删除内部重复定义；
  4. 全项目延迟导入 `from workbench import CONTENT_TYPES/ICON_RULES` 改为 `from content_types import ...`（减少对重 UI 模块的依赖）。
- **验证**：`WorkbenchTypeListGroupTest` 等现有路由测试全绿；新增 `content_types` 纯数据测试（类型键唯一、图标规则完整）。

### 2. 国策树/科技树画布 `focus_view.py`（最高优先级）

- **现状**：2476 行 / 101 方法 / 133 个实例属性，五合一（画布内核 + 国策编辑 + 科技树 + 实体画廊 + 无文件模式）。
- **目标**：
  - 算法层：`focus_algo.py` — 坐标换算（`_scene_to_grid`/`_grid_to_scene`/`_snap_to_grid_center`）、块查找（`_find_focus_block_range`）、文本构建（`_build_focus_text`/`_entity_block_text*`/`_entity_block_end`）、唯一键生成；
  - 绘图层：`focus_render.py` — 国策/科技/画廊的图元生成与绘制（从 `redraw`/`_render_entity_gallery`/`_redraw_tech_tree`/`_add_tech_edge`/`_tech_pixmap` 抽出）；
  - UI 层：`focus_view.py` 保留为 `FocusView(QGraphicsView)` 瘦壳，只做视图装配与基础事件透传；实体画廊/科技树控件搭建抽到 `EntityGalleryView` / `TechTreeView`（或同文件内独立类）；
  - 信号槽层：`focus_view_ctrl.py`（或 Mixin）— 鼠标/键盘/右键菜单、`_open_*`/`_pick_*`/`_upload_*`/`_delete_*`/`_move_*`、写文件编排（`atomic_write_text` 调用留在控制器）。
- **具体动作（分步、每步可独立提交）**：
  1. **先拆科技树**（状态最正交，`_tech_*` 独立）：把 `show_tech_tree_*`/`_redraw_tech_tree`/`_add_tech_edge`/`_show_tech_context_menu`/`_upload_tech_icon_for`/`_open_tech_in_editor` 等抽到 `TechTreeCanvas`（QGraphicsView 子类或组合组件），`FocusView` 委托调用；
  2. 再拆**实体画廊**：`show_entity_gallery*`/`_render_entity_gallery`/`_add_new_entity*`/`_open_entity_tree_editor`/实体图标/本地化 → `EntityGalleryCanvas`；
  3. 再拆**算法纯函数**到 `focus_algo.py`；
  4. 最后把**国策编辑写文件**集中到 `focus_view_ctrl.py` 或 `FocusEditorController`。
- **验证**：现有 offscreen 冒烟（FocusView 相关）逐步改指向新组件；每步跑 `verify_contracts.py` 双版本；保持 `FocusView` 公开入口（`main_window`/`workbench` 调用不变）直至收尾。

### 3. 科技树 `tech_view.py`

- **现状**：已含布局算法（`_classify`/`layout_tech_trees`）与图元类（`_TechNodeItem`/`_SubTechSlot`），算法/绘图基本已分，但仍是单文件。
- **目标**：保持算法层（`tech_layout.py`）+ 绘图层（`tech_items.py`）的物理拆分；若单文件规模增长再拆。
- **具体动作**：
  1. 把 `_classify`/`layout_tech_trees`/`_settings` 移到 `tech_layout.py`（纯算法）；
  2. `tech_view.py` 保留图元类与视图组装。
- **验证**：现有科技树测试（若有）+ offscreen 冒烟；新增 `tech_layout` 纯函数测试。

### 4. 通用树形编辑器 `generic_tree_editor.py`

- **现状**：52KB，已用 `tree_node.py`（算法）+ `tree_model.py`（模型）分离数据/模型，但 `GenericTreeEditor` 自身仍同时承担 UI 搭建、右键菜单、写文件、翻译回显、搜索对话框编排。
- **目标**：
  - 算法层：`tree_node.py` 继续承载（已达标）；
  - 绘图层/模型：`tree_model.py`（已达标）；
  - UI 层：`tree_editor_view.py`（树视图/工具栏/状态栏搭建）；
  - 信号槽层：`generic_tree_editor.py` 瘦身为控制器（菜单动作、写文件、对话框编排）。
- **具体动作**：
  1. 把 `_setup_ui`/`_connect_signals`/工具栏/状态栏抽到 `TreeEditorView`；
  2. 把 `_edit_node`/`_delete_node`/`_move_up`/`_move_down`/`_add_node_search` 等动作保留在控制器；
  3. 写文件逻辑（`atomic_write_text`）统一收敛到控制器方法。
- **验证**：现有树编辑器契约测试 + offscreen 冒烟全绿。

### 5. 地图编辑器 `map_editor_dialog.py` / `region_editor_dialog.py` / `oob_map_editor.py`

- **现状**：算法已在 `map_loader.py`/`map_vector.py`/`map_fill.py`/`map_region_ops.py`/`state_*`；绘图在 `map_canvas.py`；但对话框仍较大且混有业务编排。
- **目标**：
  - 算法层：保持（`map_region_ops`/`state_edit_ops`/`state_build_ops` 等）；
  - 绘图层：`map_canvas.py` 内 `VectorBaseItem`/`MapCanvas` 绘制逻辑保持；若继续膨胀可拆 `map_render_items.py`；
  - UI 层：各对话框只做控件/布局；
  - 信号槽层：对话框保留为控制器（点选/涂色/写回编排）。
- **具体动作**：
  1. 检查 `map_editor_dialog`/`region_editor_dialog` 中是否还有内嵌纯算法（坐标/掩码计算），有则下沉到对应 `*_ops.py`/`map_canvas` 纯函数；
  2. 保持 `MapCanvas` 的信号 API（`province_clicked`/`selection_changed` 等）作为 UI↔控制器的边界；
  3. 若对话框超过 ~800 行，将右侧信息面板/左侧建筑面板抽为独立 `QWidget` 类。
- **验证**：现有地图/区域/OOB 冒烟测试全绿。

### 6. AI 编辑器群（8 类）

- **现状**：已基本符合四层（`ai_loader.py` = 算法/数据，`ai_ui_common.py` = 公共 UI，各 `ai_*_editor_dialog.py` = UI+控制器）。
- **目标**：**维持**，仅做边界收敛：
  - 算法层：`ai_loader.py` 保持纯数据/写回；
  - UI 层：`ai_ui_common.py` 的 `EntityListSidebar`/`KeyValueTableEditor`/`ScriptBlockEditorDialog` 保持；
  - 信号槽层：各 `AiXxxEditorDialog` 保持为控制器；若某对话框超过 ~600 行，把 CRUD 动作与 UI 搭建拆成 `AiXxxView` + `AiXxxController`。
- **具体动作**：
  1. 在新增 AI 类型/字段时，先在 `ai_loader.py` 加算法函数，再在对话框加 UI/槽；
  2. 避免在槽里直接写 `replace_top_block_child` 等算法调用之外的业务拼接。
- **验证**：`AiCrudWriteTest`/`AiSimpleEditorSmokeTest`/`AiUiCommonTest` 全绿。

### 7. 编制编辑器 `division_editor.py` / `initial_oob_editor.py`

- **现状**：`oob_loader.py`（算法/数据）+ `division_editor.py`（UI/控制器）已分层；`initial_oob_editor.py` 是入口工厂。
- **目标**：维持；将 `division_stats`/`load_equipment_stats` 等数值计算保持在 `oob_loader.py`；UI 中 `_fmt_num`/`_fmt_pct` 等格式化可抽到 `oob_format.py`（算法层）便于测试。
- **具体动作**：
  1. 把 `_fmt_num`/`_fmt_pct`/装备花费汇总等纯函数移到 `oob_format.py`；
  2. `DivisionEditor` 只做 UI 搭建 + 信号槽编排。
- **验证**：`SubUnitStatsTest`/`DivisionEditorSmokeTest` 全绿。

### 8. 舰艇/飞机/坦克设计器

- **现状**：`*_design.py`（算法/数据）+ `*_design_dialog.py`（UI/控制器）已分层。
- **目标**：维持；三个设计器共用逻辑（模块选择、槽位布局、同款同步）可抽公共 `designer_common.py`（UI 层）。
- **具体动作**：
  1. 把 `ModulePickerDialog`/槽位网格/同款同步的通用 UI 逻辑抽到 `designer_common.py`；
  2. 算法层保持 `*_design.py`。
- **验证**：`ShipDesignLoaderTest`/`PlaneDesignLoaderTest`/`TankDesignLoaderTest`/`DesignLayoutSyncTest` 全绿。

### 9. BOP 力量平衡 `bop_editor_dialog.py`

- **现状**：`bop_loader.py`（算法/数据）+ `bop_editor_dialog.py`（UI/控制器）已分层。
- **目标**：维持；若继续加功能，把滑块/修正展示/动作列表拆为 `BopView`（UI）+ `BopController`（信号槽）。
- **验证**：`BopLoaderTest`/`BopEditorDialogSmokeTest` 全绿。

### 10. 本地化/翻译 `translation_editor.py` / `localization_mgr.py` / `translation_widget.py`

- **现状**：`localization_mgr.py`（数据/算法）+ `translation_editor.py`（控制器）+ `translation_widget.py`（UI）已基本分层。
- **目标**：维持；确保写 yml 仍走 `encoding="utf-8-sig", allow_bom=True` 且在控制器层编排。
- **验证**：本地化相关测试全绿。

### 11. 模板/词条/自定义语句/节点搜索

- **现状**：`template_scheduler.py`/`term_registry.py`/`custom_statement_dialog.py`/`node_edit_dialog.py` 等，算法与 UI 大体已分（`template_scheduler` 是算法，`template_dialog` 是 UI/控制器）。
- **目标**：维持；`NodeEditDialog`/`TemplateDialog`/`CustomStatementDialog`/`TermDialog` 是复用的「词条/模板/自定义语句搜索」组件，已满足四层（UI + 控制器）。
- **验证**：现有模板/词条测试全绿。

### 12. 图标库/单位标牌

- **现状**：`icon_manifest.py`/`unit_counter_library.py`（算法/数据）+ `*_dialog.py`（UI/控制器）已分层。
- **目标**：维持。
- **验证**：`IconManifestTest`/`UnitCounterLibraryTest` 全绿。

### 13. 主窗口 `main_window.py`

- **现状**：77KB，是全局控制器/装配壳，负责菜单、路由、文件树、画布装配、写文件编排；内部有大量延迟导入与路由分发。
- **目标**：
  - 信号槽层：`main_window.py` 保留为顶层控制器（路由/装配/菜单）；
  - 抽 `app_routes.py`（纯路由表：路径/类型 → 打开函数）+ `menu_factory.py`（菜单构建，UI 层）。
- **具体动作**：
  1. 把 `_open_tree_editor` 中的路径/类型分发逻辑抽成纯路由表；
  2. 把工具菜单构建抽到 `menu_factory.py`；
  3. 保留 `main_window` 作为薄壳调用它们。
- **验证**：`OobFileModeOpenTest`/`WorkbenchRouteTest`/`AiWorkbenchRouteTest` 等路由测试全绿。

### 14. 对话框类（mod 创建、项目向导、国家设置、顾问分配、健康检查等）

- **现状**：多为单文件小对话框，UI+控制器混在一起但规模小。
- **目标**：不强制拆文件；遵循**类内分层**：`_setup_ui` 只搭控件，槽方法只做编排，纯算法放模块顶层函数或 `*_algo.py`。
- **验证**：各对话框 offscreen 冒烟全绿。

### 15. API / MCP（`api_server.py` / `mcp_server.py`）

- **现状**：非 UI 工作台，但同样应分层：`ApiCore`（业务/算法）与 HTTP/MCP 传输层分离（项目已约定必须共用 `ApiCore`）。
- **目标**：维持；新增 API 端点只调用 `ApiCore`，不在传输层写业务算法。
- **验证**：API/MCP 契约测试全绿。

---

## 四、质量门禁与验证

1. **每步重构后必须跑**：
   ```bat
   python tools/verify_contracts.py
   .venv\Scripts\python.exe tools/verify_contracts.py
   ```
   退出码 0。
2. **新增代码分层自检**：新增方法先判层（算法 > 绘图 > UI > 信号槽），在 PR/提交说明中标注所属层。
3. **体积门槛（软性）**：单个类 > 600 行且混装两层以上时，须拆层；新文件 > 800 行应拆分。
4. **依赖方向检查（P3 可选）**：写 `tools/check_layer_deps.py` 静态扫描，禁止算法/绘图模块反向 import UI/控制器模块；纳入 `verify_contracts.py`。
5. **测试配套**：
   - 算法层：纯函数 `unittest`；
   - 绘图层：offscreen 渲染 + 像素/几何断言；
   - UI/信号槽：offscreen 冒烟。

---

## 五、风险与注意事项

1. **`focus_view.py` 是核心视图**：`main_window`/`workbench` 大量直接引用，拆时必须保持公开入口（类名/方法名）不变，或一次性同步更新所有调用点。
2. **延迟导入不全是坏味道**：拆出 `content_types.py` 后，`focus_view` 等对 workbench 的延迟导入会自然减少，但不要为「消灭延迟导入」而制造真正的循环依赖。
3. **不要为了分层而分层**：小对话框保持类内分层即可，物理拆文件只在规模/复用达到阈值时做。
4. **写文件纪律不随分层放宽**：无论哪一层调用，最终写 mod 文件必须走 `atomic_write_text`/`icon_ops.write_file_utf8`。
5. **本计划与「AI 专用 UI + 固定侧边栏」计划的关系**：后者已完成并作为基线；本计划只在其上做职责边界收敛，不改变已定 UI 行为。
