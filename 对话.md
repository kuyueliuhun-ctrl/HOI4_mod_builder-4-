# UI 落地总任务清单与具体方法（唯一总表）

> 2026-08-23 生成，替代此前的评估对话记录（评估结论已沉淀到
> `docs/UI修复与建构执行方法.md` 批次表与 §0.x）。
> 本文 = **全部待办任务 + 落地具体方法**；进度打勾一律回写
> `docs/UI修复与建构执行方法.md` 批次表（第一批）与本文各节（第二批）。
> 硬约束沿用既有执行文档 §0：双版本 `verify_contracts`、原子写纪律、
> Python 3.14（Windows .venv 3.14.5 / WSL 3.14.4）、offscreen 测试禁截图、
> ui_gap_probe `--max-files 0` 闭环（0 缺口或写明豁免）、四层分离、复用件速查表。
> 视觉基准 = `prototypes/proto_*.py`（P4~P39）；内容依据 =
> `prototypes/设计器槽位与升级调研.md` / `AI工作台与通用类型调研.md` /
> `剩余工作台调研.md`。**原型在对应任务验收前不得删除。**

---

## A. 第一批收尾（批次 4~9 + §0.x，差距已核到代码行号）

### A1 · 批次 4A 事件编辑器完整版（视觉基准 P4）
- 数据层 `src/event_data.py`：补 `parse_event_options(content, event_id)`
  （option 的 name/trigger/ai_chance/效果体逐项）、MTTH `{days, modifier}`、
  `immediate/after` 定位；option 写回用 `ai_loader.upsert_top_block_child:1048`
  按索引定位（需新增 `replace_nth_child(content, top_id, key, index, text)`）。
- UI `src/event_editor_dialog.py` 重构（现 190 行纯 QLineEdit → P4 形态）：
  - title/desc/option name 一律 LocEdit 双行（组件见 B0）；
  - picture 行 = 96×64 预览（`icon_resolver.resolve_pixmap:205`）+
    「⬆ 上传」（`icon_ops.upload_icon` + `ICON_RULES["event"]`）+「🔍 图标库」；
  - major/is_triggered_only/fire_only_once/hidden 复选保留；MTTH 天数 +
    modifier 结构化卡；`immediate/after` 结构化卡；
  - **option 列表卡**（P4 OptionCard）：name 双行 + trigger/ai_chance/效果
    三结构化块 + ↑↓⧉🗑 +「＋ 新增选项」插入 `option = { name = … }` 模板；
  - 「其他字段」表兜底（B0 OtherFieldsTable）；「🌐 批量补写缺失本地化」=
    `localization_editor_data.batch_fill_missing_loc:337` 限定本文件键集。
- 保存链：`ensure_file_in_mod` → 内容级替换 → `atomic_write_text` →
  title/desc/option name `upsert_loc_entry:230`。
- 测试：EventOptionCrudTest / EventPictureUploadTest / EventLocRoundTrip；
  `ui_gap_probe --types event --max-files 0`。

### A2 · 批次 4B 科技编辑器完整版（视觉基准 P5）+ 画布联动（3.4）
- 数据层 `src/tech_data.py`：补 allow / ai_will_do / `category_*` 加成块 /
  folder.position / path / enable_equipments 解析；
  `set_tech_folder_position`（`replace_or_upsert_nested_child:1346`）。
- UI：左 QTreeWidget 侧栏（folder→科技，folder 不可选，搜索过滤）；
  表单 = 名/desc 双行 + **图标 183×84**（`tech_icon_ops.upload_tech_icon:169`
  自动注册 GFX）+ start_year/research_cost + categories 徽章增删（候选
  `common/technology_tags`）+ folder 下拉 + position x/y + path 表 +
  enable_equipments 列表 + allow/`category_*` 结构化块（修正名中文+百分比）
  + 其他字段表。
- 画布联动：`tech_view` 节点双击 → `tech_editor_dialog.open_tech_editor(
  file_path, tech_id)`；编辑器保存后回调画布刷新（信号）。
- 测试：TechSidebarTest / TechIconUploadTest / TechCanvasLinkTest；
  `ui_gap_probe --types tech --max-files 0`（covered 扩 `technologies.**`）。

### A3 · 批次 5 州字段完整版（P6）＝§0.x-3 前半
- `src/map_editor_dialog.py`：resources 单行文本框（:252-255）改
  **KeyValueTableEditor**（候选 = 油/铝/铬/钢/钨/橡胶/煤 + 自定义）；
  victory_points 改 **两列表**（pid/点数，增删行）；州名 LocEdit 双行
  （STATE_<id> 键 + 中文）。
- 数据层：补 `set_state_name_in_content` + 保存时
  `upsert_loc_entry(mod yml, STATE_<id>, 中文)`；保存后 `StateData.reload()`。
- 测试：StatePanelFormTest；state spec 补
  `name/resources.**/history.victory_points.**/history.manpower` covered。

### A4 · 批次 6 BOP 完整版（P8）＝§0.x-3 后半
- `src/bop_editor_dialog.py`：区间 modifier 只读标签（:438）改**行内键值表**
  （`set_bop_range_modifiers` 已有）；区间 min/max SpinBox + 区间增删；
  势力卡可编辑（icon/中文名/关联区间勾选，`set_bop_side_fields` 已有）；
  决议列表 +「＋ 新建决议」（模板取 `templates/系统模板/决议/`，
  `insert_bop_decision` 已有）+ 选中编辑表单（名称双行/cost/
  add_power_balance_value 走 `replace_top_block_fields:256`）+ 删除。
- 测试：BopRangeEditTest / BopDecisionCrudTest；bop spec covered 扩
  `["*", "*.**"]`。

### A5 · 批次 7 兵种/命名组完整版（P20/P9）
- `src/sub_unit_editor_dialog.py`（现 130 行 9 输入框 → P20 形态）：
  属性分组网格（基础/战斗/后勤三组，QTableWidget 键值）+ **地形矩阵三列表**
  （movement/attack/defence，值正负着色）+ categories 标签多选 +
  名称/描述 LocEdit + 其他字段表；`oob_loader.save_sub_unit` 扩全字段写回
  （need/terrain/stats/others）。
- `src/names_group_dialog.py`：组列表 + 表单 + **名称条目结构化块展开**
  （每条目行卡：类型/顺序/名称键双行）。
- 测试：SubUnitFullRoundTrip / Terrain3WriteTest / NamesGroupEntryTest。

### A6 · 批次 8 raw 降级完成（＝B0 的第一步）
- `src/ai_ui_common.py` ScriptBlockEditorDialog：默认视图改
  **「键值表 + 子块列表（面包屑双击进入）」**（实现即 B0 的
  StructuredBlockBrowser，raw 已在「高级 ▾」末项）。
- AI 七编辑器 `_edit_raw` 入口文案统一「高级：原始 PDX（兜底）」并移入
  「高级 ▾」；node_edit_dialog.py:352-384 文案微调。
- 测试：ScriptBlockStructuredTest。

### A7 · §0.x 补充项（1/2；3 已并入 A3/A4；4 已完成）
1. **变体高级字段**：`parse_equipment_variants` 补
   design_team/parent_version/obsolete/icon 四键解析与写回
   （`replace_top_block_fields`）；三个 `*_design_dialog` 变体区加
   design_team（RefPicker=MIO 组织）/parent_version（spin）/obsolete
   （check）/icon（图标行）。⚠️ 不做 desc/自定义 stats（不存在）。
2. **角色未知块结构化**：`character_editor_dialog.py:296`「保留不可编辑」
   区改为 ScriptBlockEditorDialog 编辑；`character_data.py` 解析 TFR
   `instance = {…}` 包装（子树并入解析，不进只读区）。

### A8 · 批次 9 收尾
- 文档：AGENTS §6 摘要；未完成计划/UI评分清单落点同步；三份 prototypes
  调研文档并入 `docs/游戏文件内容详解.md` 对应章节（§4.8）。
- 全量验证 + `ui_gap_probe`（全部 spec）重生成缺口报告存档。
- 验收后**清除原型 P4~P9**（P7 随批次 8 验收一并清除）。

---

## B. 第二批 P10~P39（AI 改版 + 通用/全量工作台）

### B0 · 公共组件落地（一切前置；2026-08-23 用户反馈已定形态）
- 新建 `src/ui_widgets.py`，从原型移植并接真实数据层：
  **BlockTreeList**（块树列表：键（中文+原键）/值/中文 三列，嵌套块缩进为
  子行，**双击单元格就地编辑**，**右键通用菜单**=添加词条/添加子块/删除/
  上移下移/复制/快速本地化/结构化浏览器兜底）——用户拍板的块展示标准形态；
  **BlockListCard**（内联块列表卡 = BlockTreeList + 标题双语 + 「▾ 收起」
  切换单行摘要）；**BLOCK_CN 词典**（50+ 常用块键中文，visible→可见性 等，
  正式版扩充并接 game loc/QIUQI 词条库）；`LocEdit`（键+中文双行）/
  `StructuredBlockBrowser`（面包屑导航，作为「高级」入口保留）/
  `TriggerCard`（紧凑形态，低频块用）/ `WeightCard`（factor/base+modifier
  行）/ `WeightTable`（键枚举下拉+数值列，非数值转文本单元格）/
  `RefPicker`（下拉+搜索+「⚠ 未找到」不阻止手输）/ `OrderRowList` /
  `OtherFieldsTable` / `source_badge`（mod 改写/置空/本体）。
- `ScriptBlockEditorDialog` 内部重构为 BlockTreeList 封装（=A6）。
- 测试：UiWidgetsTest（每组件 roundtrip + 树编辑/右键动作/双语显示）。

### B1 · AI 工作台改版（P10~P16，消灭 12 处 raw 兜底）
| 任务 | 具体方法 |
| --- | --- |
| P10 战略计划 | `ai_loader.parse_ai_plans` 补 traits/内嵌 ai_strategy 解析+写回；`ai_plan_editor_dialog`：生命周期 TriggerCard×3 替换 ADVANCED_FIELDS、国策顺序 OrderRowList +「🎯 从国策树点选」接已有 focus_order_picker、research/ideas/traits 三张 WeightTable（键下拉）、focus_factors 表（0=永不点提示）、weight WeightCard、内嵌策略条目表 |
| P11 战略倾向 | `parse_ai_strategies` 补特工条目字段（operation/mission/operation_target/mission_target/num_operatives/state）；条目卡 type 下拉（25 枚举带中文），type=operative_* 时条件字段行展开；组侧栏标注「mod 置空/改写」source_badge |
| P12 师模板 | 两级侧栏（role 组→目标）；升级链卡（replace_with RefPicker=同文件目标 + replace_at_match/target_min_match + 链路文字可视化 + 成环校验）；loader 补 division_names_group/reinforce_prio/custom_icon |
| P13 AI 装备 | 组→设计两级；category(land/air/naval)/roles 标签组/available_for/blocked_for；target_variant 卡（type=archetype RefPicker + match_value +「在设计器中编辑模块」按钮按 category 调 tank/plane/ship dialog）；allowed_modules 标签组 + history |
| P14 AI 海军 | goals：objective_type 下拉（10 枚举）+ min/max priority；fleet：required/optional_taskforces WeightTable（taskforce 跨文件 RefPicker + 断链检查）；taskforce：mission 多选 + min/optimal_composition 舰种→数量表 + keep_updated + ai_will_do WeightCard —— 全部替换 `_edit_raw_selected` |
| P15 区域+战区 | regions 行卡（「🗺 从地图框选」接 map_editor 框选）；战区 name LocEdit + cancel TriggerCard + ai_will_do WeightCard |
| P16 AI 杂项（新建） | 五页签：attitudes（11 布尔 flag 复选网格）/personalities（KV 表）/ai_peace（peace_action_type RefPicker + enable TriggerCard 带 ROOT/FROM 提示 + ai_desire）/MIO ai_bonus_weights（archetype→stat WeightTable + default 块）/ai_focuses（category 下拉 WeightTable）；新路由 + content_types 键（ai_attitudes/ai_personalities/ai_peace/mio_ai_weights） |

每任务：loader 补解析+写回 → 对话框改造/新建 → 路由 → 测试（LoaderParseTest
+ SmokeTest）→ `ui_gap_probe` 对应 ai_* spec 扩 covered（traits/特工字段/
composition/regions 等）→ `--max-files 0` 闭环。

### B2 · 通用类型工作台（P17~P23 + P24~P27 新建）
- **B0 组件 + 以下每项**统一四件套：数据层（`ai_loader` 模式 `_scan_files`
  + 顶层 CRUD 复用）→ 对话框 → `app_routes.ROUTES` + `SPECIAL_TYPE_KEYS`
  → ui_gap_probe spec + 测试。
| 任务 | 具体方法 |
| --- | --- |
| P17 脚本库 | `script_lib_dialog` 三页签：effects/triggers（代码视图 QPlainTextEdit+QSyntaxHighlighter（PDX 关键字/注释/字符串），**默认结构化视图**、代码=作者模式可记忆偏好；引用计数=全工程调用点扫描缓存，重命名全工程同步提示）；scripted_localisation=行卡（触发→loc 键，顺序敏感） |
| P18 on_actions | 钩子侧栏（内建 on_* 枚举清单含 TAG 细分）+ effect StructuredBlockBrowser + random_events WeightTable（事件 RefPicker）+「＋ 新钩子」 |
| P19 动态修正 | `dynamic_modifier_dialog`：icon 上传（ICON_RULES 可扩）+ enable/remove_trigger TriggerCard + country/state/attacker 三页 WeightTable + idea 同构布局 |
| P21 装备定义 | `equipment_def_dialog`：属性 KV 表 + archetype/parent RefPicker + allow_mission_type 标签 + upgrades 上限表 + year/resources + is_archetype 复选 |
| P22 MIO | `mio_dialog`：组织页（特质树 QTreeWidget，节点编辑卡=修正/花费/前置/随机池/available）+ 政策页 + 与 P16 ai_bonus_weights 互跳 |
| P23 国家历史 | `country_history_dialog`：国家侧栏 + 效果流 OrderRowList（效果名+参数摘要，常用效果模板插入）+「与本体对比」+ oob/变体/精神联动按钮 + 未识别键表 |
| P24 特殊计划 | available TriggerCard + 突破点/复杂度 + project_output/generic_prototype_rewards 结构化 + tags 标签 + icon 上传 |
| P25 学说 | 树形侧栏（军种→track→学说）+ effect 结构化 + rewards 两表（battalion_mult 兵种倍率 / modifier）+ 双向联动画布 |
| P26 派系 | goals（决议同构卡）/member_upgrades/rules 三页签 |
| P27 间谍行动 | 补 cost_modifiers WeightTable + outcome_execute 浏览器 + outcome_modifiers 表（P27 原型已有，落成 src） |

### B3 · 全量覆盖（P28~P39，`simple_entity_tab` 驱动快速铺开）
- 公共构造器：`src/simple_entity_tab.py`（移植 proto_misc_common：
  侧栏+表单+触发卡/加权卡+权重表+其他字段表+**自动名称/描述双行**）。
  小类型 = 一份行数据即一页签；大类型单独建对话框：
| 任务 | 具体方法 |
| --- | --- |
| P28 决议 | 两级侧栏（分类→决议）；55 实测键表单（调研 §1.1 全集）；targeted decisions 的 targets/target_array/highlight_states 走 StructuredBlockBrowser；categories 页签（icon/picture/allowed/visible） |
| P29 民族精神+理念标签 | slot 分组两级侧栏（分组级 law/designer/use_list_view 标量）；idea 表单（cost/removal_cost/level/ledger + modifier/research_bonus/equipment_bonus/targeted_modifier/rule 五表 + on_add/on_remove/cancel/do_effect 触发卡 + traits 引用 country_leader）+ idea_tags 页签（category→slot/character_slot/cost/ledger + slot_ledgers 表） |
| P30 意识形态 | color QColorDialog 取色器 + war/faction_impact 等数值 + rules 布尔网格（9 枚举）+ modifiers/faction_modifiers 表 + dynamic_faction_names 行卡 + types 子意识形态树（can_be_randomly_selected/color） |
| P31 领袖与特质 | 五页签：country_leader（裸修正词条表+sprite+random+ai_will_do+equipment_bonus）/unit_leader traits（type/trait_type 枚举、六技能、gui_row/column、三修正容器 modifier/corps/field_marshal、prerequisites/gain_xp 触发卡、顾问三级映射）/skills（等级 1..10 KV）/scientist_traits（specialization 枚举+sp_* 修正表）/advisor 生成（every_possible_country+generate_character：token_base/slot/traits/cost/available/portraits） |
| P32 持续国策+内嵌窗口 | palette 卡（唯一容器）+ focus 表单（daily_cost/enable=激活效果语义区分/ai_will_do/modifier 表/supports_ai_strategy）；inlay=window_name/internal/visible + images/buttons/progressbars 结构化 |
| P33 政治与和会 | 九页签（simple_entity_tab）：game_rules（name/group loc 双行 + default/option 行卡含 allow_achievements/exclude_dlc）/difficulty/opinion_modifiers/wargoals（触发块+花费数值）/peace_conference（categories+cost_modifiers，enable 带 ROOT/FROM 提示）/autonomy（rule+modifier 表+ai wants 两卡）/occupation_laws/resistance_compliance（threshold/margin+type 枚举）/resistance_activity |
| P34 经济与地图 | 六页签：buildings（level_cap 子块结构化+country/state_modifiers 表+damage_factor）/resources/state_category/terrain（units 三值+buildings_max_level+舰种块）/map_modes（top/bottom 结构化+far/near_text 枚举）/strategic_locations |
| P35 军事奖励与情报 | 十页签：abilities/aces（type 装备标签+effect 表）/medals（三档 bronze/silver/gold 条件卡+colors）/ribbons/unit_medals/raids（target_type/success_factors/success_levels 结构化+arrow 枚举）/timed_activities/intelligence/intel upgrades（五部门分支）/operation_tokens |
| P36 脚本与修正 | 十页签：scripted_guis（context_type 枚举+triggers/effects/properties/dynamic_lists 结构化）/scripted_diplomatic_actions（ai_acceptance 卡）/script_constants（schema 表）/同步令牌清单/mtth/generation（固定槽位树）/scorers（targets+score WeightCard）/collections（input+operators 行卡）/modifier_definitions（color_type/value_type/postfix 枚举+category 多行）/modifiers 静态修正表 |
| P37 剧本与主界面 | 五页签：bookmarks（国家条目多版本行卡+required_dlc）/frontend 背景（dds 文件名+dlc_allowed）/profile 背景/图片（数字 id+icon+tag 解锁）/超事件三件套联动页（事件 flag↔scripted_gui↔interface gfx 互查跳转） |
| P38 配置与清单 | 七页签：technology_sharing（重复块）/tech 标签/装备组（嵌套引用）/unit_tags/国家别名（country_score 加权卡）/names（male/female/surnames/callsigns/ace/operation 池行卡）/国家定义（countries+colors+cosmetic+country_tags 整合） |
| P39 高级文件 | 五页签：defines（.lua 29 组树+KV 表+注释 tooltip）/gui（元素树+属性表，源码入高级菜单）/gfx（sprite 表接 icon_manifest + 缺失警示 + 上传注册）/mod 描述表单/operation_phases（requirements 令牌联动） |

- ⚠️ **调研否定的字段不得实现**：决议 removal_cost / 建筑 cost·air_damage·
  build_time·max_level / 意识形态 can_lean·support·popularity_effect·
  icon / 王牌姓名池 / 持续国策 cost·xp_cost·search_filters / 自治
  is_default·freedom_level·cost·min_puppet / game_rules csg·visible /
  地形 is_urban·air_combat / 变体 desc·自定义 stats。

---

## C. 执行顺序与依赖

1. **B0（公共组件）最先**——A6 与 B1/B2/B3 都依赖它；A 线与 B 线可并行
   （文件域不重叠）。
2. A 线：A1/A2（批次 4）→ A3/A4/A5 并行 → A7 → A8 收尾验收 → 清 P4~P9。
3. B 线：B0 → B1（P10~P16）与 B2（P17~P27）并行 → B3（simple_entity_tab
   就绪后铺开 P28~P39）。
4. 每任务交付物：代码 + `tests/test_contracts.py` 用例 + 真实数据冒烟
   （mods 3350890356/3228475937 + 本体）+ `ui_gap_probe --max-files 0`
   闭环 + 批次表打勾 + 文档同步。
5. 全部验收后：prototypes/ 仅留 README 与三份调研文档存档，画廊关闭。
