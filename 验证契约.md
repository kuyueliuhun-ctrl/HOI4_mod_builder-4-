# 写入纪律与可执行契约（验证契约.md）

对应 Scenario Forge 工程方法论移植的第二批：**状态写入纪律 + 原子写 + 导出前健康检查 + 把工程教训写成可执行契约**。

## 一、契约清单

| # | 契约 | 执行方式 | 违反后果 |
| --- | --- | --- | --- |
| 0 | 全部契约一键验证 | `python tools/verify_contracts.py` | 退出码 1 |
| 1 | 写入纪律：mod 内容文件必须走原子写 | `python tools/check_write_discipline.py` | 退出码 1（新增直写未登记） |
| 2 | 原子写：写失败不破坏原文件 | 单元测试 `tests/test_contracts.py` | 测试失败 |
| 3 | 编码契约：默认 UTF-8 无 BOM + LF | 单元测试 + 健康检查 warning | 测试失败 / 检查报告 |
| 4 | 撤销快照：写前自动登记 undo | 单元测试 | 测试失败 |
| 5 | 导出前健康检查能检出常见缺陷 | 单元测试（descriptor/括号/BOM/GFX/重复 id 等夹具） | 测试失败 |
| 6 | 双版本兼容：Python 3.8 + 3.13 | verify_contracts 内 py_compile 全模块 | 编译失败 |

## 二、写入纪律（契约 #1）

**规则：所有 mod 内容文件（.txt / .gfx / .yml / .mod / .csv 等）的写入必须经过**

```python
from write_utils import atomic_write_text
atomic_write_text(path, text)            # 默认 UTF-8 无 BOM + LF + 撤销快照
atomic_write_text(path, text, encoding="utf-8-sig", allow_bom=True)  # 本地化 .yml（HOI4 BOM 惯例）
```

或兼容入口 `icon_ops.write_file_utf8(path, text)`（内部已委托原子写）。

- **原子写**：同目录临时文件 + `os.replace` 原子替换。写入中途失败（磁盘满、编码错误、进程被杀）绝不破坏原文件。
- **BOM 拒绝**：文本以 `\ufeff` 开头默认抛 `WriteContractError`（HOI4 脚本解析器对 BOM 敏感）。本地化 `.yml` 走 `utf-8-sig` + `allow_bom=True`。
- **撤销快照**：写前自动登记到 `undo_mgr`（画布 Ctrl+Z / 工具菜单「撤销上次文件写入…」）。

**允许直写的情形**（必须登记到 `tools/write_discipline_allowlist.json` 并写明理由）：
- 程序自身配置（settings.json 等）
- 程序数据文件（custom_statements.json、词条库等）
- 一次性开发脚本（tools/ 复现脚本）
- 二进制复制（图片拷贝等，扫描器仅 info 提示）

**静态扫描**：`tools/check_write_discipline.py` 用 AST 扫描全部源码，检出绕过原子写的文本直写；新增直写未登记即失败（CI/本地验证拦截）。

## 三、导出前健康检查（契约 #5）

工具菜单 → **「导出前健康检查…」**，或编程调用：

```python
from export_health import run_export_health_check
report = run_export_health_check(mod_path, hoi4_path)  # hoi4_path 可选（游戏贴图回退）
report.to_json()  # JSON 报告
```

检查项（error 级应修复后再发布）：

| 类别 | 级别 | 检查内容 |
| --- | --- | --- |
| descriptor | error/warning | .mod 描述文件存在、path 指向目录存在、name 字段 |
| encoding | error/warning/info | 非法 UTF-8（error）、BOM（warning）、CRLF（info）、空文件（warning） |
| syntax | error | 括号配对（引号/注释感知），抓缺 `}` / 多 `}` |
| reference | error | interface/*.gfx 定义的 sprite 贴图必须存在（mod 内或游戏本体） |
| reference | warning | 悬空前置国策（prerequisite/mutually_exclusive） |
| reference | info/error | 科技图标 sprite 注册与贴图 |
| locale | warning | 实体本地化词条缺失（复用 validation 检测，可一键补全） |
| duplicate | error/warning | focus/tech 重复 id（error）、character 块重复（warning） |

真实 mod 实测（mod 3228475937，3.3 秒）：抓出 2 处真实括号缺陷、45 处贴图引用问题（部分引用游戏本体资源，已用 hoi4_path 回退消除误报）。

## 四、单元契约测试（契约 #2/#3/#4/#5）

`tests/test_contracts.py`（**173 个用例 / 41 个测试类**，unittest，Python 3.8 兼容）：

- **基础写入与健康检查**：AtomicWriteTest（原子写/BOM 拒绝/撤销/写失败保持原文件）、
  HealthCheckTest（descriptor/括号/BOM/GFX 引用/重复 id）、WriteDisciplineScannerTest
- **地图数据与编辑**：RegionOpsTest、StateEditOpsTest、StateExtLoaderTest、
  BuildingLibTest、StateBuildOpsTest、MapEditorDialogSmokeTest、RegionScanTest、
  MapCanvasPureTest、MapVectorTest、MapSettingsTest、MapFillTest、
  MapCanvasSmokeTest（瓦片缓存/预览/边界烘焙/位置跟随）、MapCanvasExtensionsTest、
  OobMapEditorSmokeTest
- **Scenario Forge 移植**：OverlayRulesTest、IconManifestTest、UnitCounterLibraryTest
- **编制/装备设计器**：SubUnitStatsTest、DivisionEditorSmokeTest、ShipDesignLoaderTest、
  ShipDesignDialogSmokeTest、PlaneDesignLoaderTest、PlaneDesignDialogSmokeTest、
  TankDesignLoaderTest、TankDesignDialogSmokeTest、DesignTemplateTest、
  DesignTemplateDialogSmokeTest、DesignLayoutSyncTest（槽位/锁定/空提示/同款同步）
- **路由与工作台**：WorkbenchNofileCountryTest、OobOpenDesignerTest、
  OobFileModeOpenTest、WorkbenchOobDoubleClickTest、WorkbenchTypeListGroupTest、
  OobKindDetectTest、DynamicModifierTemplateTest

## 五、迁移记录（已完成的直写点 → 原子写）

| 模块 | 迁移点 |
| --- | --- |
| icon_ops.py | write_file_utf8 委托原子写；update_gfx_file |
| undo_mgr.py | undo 恢复写入走原子写 |
| focus_view.py | 创建国策 / 移动 x,y / 删除国策（3 处） |
| generic_tree_editor.py | 树编辑器保存 / 模板更新 |
| tree_info_dialog.py | 树信息保存（2 处） |
| oob_loader.py | OOB 保存 |
| translation_editor.py | 本地化 yml 保存（utf-8-sig + allow_bom） |
| validation.py | 一键补本地化（utf-8-sig + allow_bom） |
| advisor_assign_dialog.py | 顾问分配写回 |
| template_scheduler.py | 模板创建 / 应用（3 处，配置 json 豁免） |
| main_window.py | 模板创建文件 / 新建空文件 / AI 回填（3 处） |
| workbench.py | 新建空文件 / 模板创建 |
| country_setup_dialog.py | 同名空文件 / 新建国家文件 / country_tags 追加（3 处） |
| template_manager_dialog.py | 模板保存 |
| mod_creator_dialog.py | .mod / descriptor.mod / gfx / 本地化 yml（4 处） |
| state_build_ops.py | 州建筑/州类别/国家颜色写回（原版自动复制到 mod 后再写） |
| ship_design.py / plane_design.py / tank_design.py | 设计器保存（upgrades/modules 块写回，原版自动落 mod） |
| design_template.py | 设计器模板保存（原子写） |

## 六、新增代码规范（把教训写成可执行契约的延续）

1. **新写入**必须走 `atomic_write_text`；确需直写时登记 allowlist 并写明理由。
2. **新检查器**必须配套契约测试（fixture + 断言），跑 `verify_contracts.py` 全绿才算完成。
3. **bug 修复**必须补回归测试（真实缺陷 → 对应夹具）。
4. 修改后必须双版本验证：`python tools/verify_contracts.py` 与 `.venv\Scripts\python.exe tools/verify_contracts.py`。
5. 本地化 `.yml` 保持 HOI4 BOM 惯例（`utf-8-sig` + `allow_bom=True`），其余内容文件一律无 BOM。
6. **游戏本体只读**：设计器/地图/州建筑等写回必须经 `state_build_ops.ensure_file_in_mod` 把原版文件先复制到 mod，再在 mod 内原子写；禁止直接写游戏本体文件。
7. **新功能必须补契约测试**：数据层纯函数优先，GUI 用 offscreen 冒烟；设计器/地图/OOB/力量平衡等新增模块应随功能提交测试类（当前 173 个用例）。
