# P1 调研：外部工具/功能可行性（H4MPS / IRIS / 大众脸 / 电台 / AOR）

> 调研方式：web 搜索 + GitHub REST API + 目标站点抓取 + 本地仓库通读（`E:\hoi4modutilities`、`E:\QIUQI-LIBRARY`）。
> 取数时间：2026-08-26 UTC（约 05:24–07:30）。paradoxwikis 有反爬，其正文以搜索引擎索引快照为准。
> 结论供主智能体排期；本轮含本地 xls 结构分析（未写实现代码）。

## 总览

| # | 候选 | 结论 | 集成方式 | 工作量 |
|---|------|------|----------|--------|
| 1 | H4MPS 审查 | **做（换实现）**：公开渠道查无 "H4MPS"；审查能力改用 CWTools / HOI4-Validator 承接 | 子进程跑 CLI 或 LSP 语言服务 | M |
| 2 | IRIS | **不做（概念澄清）**：是 HOI4 二次元架空地图 mod，非工具/库 | — | — |
| 3 | 大众脸（肖像生成） | **做（有条件）**：技术可行、社区已有实践，但许可/商用受限，须限定非商业场景 | 内置 ONNX/torch 模型 + GFPGAN 后处理 + 模板导出 | M–L |
| 4 | 电台/广播 OGG 生成 | **做（拆两档）**：TTS 语音+打包转码可行；"生成完整音乐"不建议自建 | edge-tts/Piper + ffmpeg + music.txt 模板 | S–M |
| 5 | 批量填鸭 AOR | **已澄清**：= 秋起图书馆《批量填鸭工具：AOR自研版（9.22版）.xls》，赤色极光制作组自研的 **表格批量生成代码** 工具；非部署责任区，也非 ho4modutilities 功能 | 列表驱动模板批量生成器（类似 xls 的 A列列表 + 模板占位符 → 批量代码块） | S–M |

## 1. H4MPS 审查

- 公开渠道（GitHub 仓库搜索、Bing/DDG 精确搜索、变体搜索）查无 "H4MPS" → 大概率是内部/未公开工具、拼写变体或误称。
- 若意图是「对 HOI4 mod 做语法/规范审查」，标准答案：
  - **CWTools**（[cwtools/cwtools](https://github.com/cwtools/cwtools)）：F#/.NET，Clausewitz 脚本语言解析/编辑/校验；HOI4 规则在 [cwtools-hoi4-config](https://github.com/cwtools/cwtools-hoi4-config)；配套 [cwtools-vscode](https://github.com/cwtools/cwtools-vscode)（LSP 语言服务）与 [cwtools-action](https://github.com/cwtools/cwtools-action)（GitHub Action，`game: hoi4` 输出 `output.json`）。
  - **HOI4-Validator**（[SpaceKraken/HOI4-Validator](https://github.com/SpaceKraken/HOI4-Validator)）：.NET CLI/图形校验器；老牌 [Kaiserreich/HOI4-Validator](https://github.com/Kaiserreich/HOI4-Validator) 已弃用、README 明示 replaced by CWTools。
- 集成：Python/PyQt6 侧不自己写解析器；二选一：LSP 子进程对接（实时提示）或包装 action CLI 的 `output.json`（批量报告）。
- 工作量 M；风险：CWTools 规则随版本滞后、大 mod LSP 首次缓存慢。

## 2. IRIS

- HOI4 语境下 IRIS = 二次元架空地图 **mod**（Steam 创意工坊《【Blossoms Blaze/華灼】~IRIS~ 九色堇之心》id=2739911713；[Skymods](https://hearts-of-iron-4.smods.ru/archives/36699)；[ztz233/Project_IRIS](https://github.com/ztz233/Project_IRIS)），**不是可集成库**。
- 结论：不排期；除非目标是把该 mod 作为编辑器适配对象。若本意是「审查/解析库」，最接近的是 CWTools。

## 3. 大众脸（肖像生成）

- 可行：StyleGAN2-ADA（[NVIDIA 页](https://nvlabs.github.io/stylegan2-ada-pytorch/)）生成人脸成熟；GFPGAN（[TencentARC/GFPGAN](https://github.com/TencentARC/GFPGAN)）修复/超分；社区已有 HOI4 画风 SDXL 模型/LoRA。
- HOI4 侧：`common/characters/*.txt` 的 `portrait` 引用 `gfx/interface/` sprite/dds（wiki Character modding）；导出需 dds 转换（[maldun/hoi4_converter](https://github.com/maldun/hoi4_converter)）。
- **许可硬约束**：FFHQ 数据集 CC BY-NC-SA（非商用）；StyleGAN2-ADA 权重 NVIDIA Source Code License-NC；GFPGAN 仓库无标准许可声明。→ 只做「非商业、本地生成」版。
- 成本：模型 100MB–1GB+、GPU 优先、torch/onnxruntime 依赖重、产出需人工筛选。
- 结论：P1 做本地生成版 M–L。

## 4. 电台/广播（Radio OGG 生成）

- HOI4 音乐/广播为 `.ogg`（Vorbis），经 `music.txt`/音乐电台条目引用（wiki Music modding；`plays an audio track` effect 要求歌曲在某 music station 中定义）。
- 低档（S，强烈建议）：OGG 打包/替换 + 语音广播（TTS）——ffmpeg 转码 + music.txt 模板。
- TTS：`edge-tts`（[rany2/edge-tts](https://github.com/rany2/edge-tts)，免 key 免费但在线灰色地带）或 `piper`（[rhasspy/piper](https://github.com/rhasspy/piper)，本地开源离线）。
- 高档（不建议 P1 自建）：自动生成完整背景音乐（MusicGen/Suno 等重型或付费）。

## 5. 批量填鸭（AOR）

### 5.1 结论：AOR = 赤色极光制作组的「批量填鸭工具」xls

- 本地 `E:\hoi4modutilities`（VSCode 扩展：世界地图/国策/事件/科技/MIO/GUI/GFX 预览与 state/strategic region 编辑）**全库无 AOR 字样**（源码、Wiki、Git 历史、issues 均无）；它只处理 `state`、`strategic_region`、`supply_area` 等“区域”文件，不是 AOR 出处。
- 真正出处是 **`E:\QIUQI-LIBRARY\工具\通用\批量填鸭工具：AOR自研版（9.22版）.xls`**（[QIUQI-LIBRARY](https://github.com/Paradox-Developer-Foundation/QIUQI-LIBRARY) 内 `docs/CATALOG.md` 登记为“批量填鸭工具：AOR自研版（9.22版）.xls”）。
- “AOR” = **赤色极光（Aurora of Red）制作组**；该 xls 是 AOR 自研的**表格批量生成代码**工具，不是“Area of Responsibility 部署区”，也不是 hoi4modutilities 的功能。

### 5.2 xls 结构（已用 xlrd 读取）

| Sheet | 作用 | 输入列 | 输出 |
| --- | --- | --- | --- |
| `normal` | 一般图标注册批量生成 | A=图片名（如 `CH`） | `spriteType = { name = "GFX_idea_CH" texturefile = "gfx/interface/ideas/CH.dds" }` 列表 |
| `shine` | 国策图标 shine 动画注册 | A=图片名（如 `逻各斯`） | `SpriteType = { name = "GFX_逻各斯_shine" … animation … }` 列表 |
| `将领` | 将领/元帅代码批量生成 | A=将领名、B=job、C=等级、D/E/F/G=攻/防/计/补 | `NAME = { name = NAME portraits … corps_commander/field_marshal = { skill … } }` 列表 |
| `你给我看！` | 使用说明 | — | 原理：适合一切手动模板填充；2023-09-22 增加将领功能 |

- 原理：Excel 公式对模板中的占位符（`AAA`/`NAME`/`SKILL`/`ATTACK`…）做替换，A 列下拉批量生成。
- 关键模式 = **“单列/多列表格 → 同一模板 → 多个代码块”**。

### 5.3 与本项目现状

- `docs/QIUQI-LIBRARY映射与复刻矩阵.md` 早已把“批量填鸭工具（AOR）”列为“表格批量生成代码”，对应现状为 `template_scheduler.py` / 词条库，状态“需调研/先分析 xls 结构”——现已完成 xls 结构分析。
- 现有 `src/template_scheduler.py` 支持**单个模板**的变量替换（`__变量__`）与文件生成，但缺少**列表驱动批量展开**（多行输入 → 拼接多个代码块）。
- 结论：复刻方向为 **通用“列表 → 模板占位符 → 批量代码块”生成器**，可先支持 xls 三套预设（idea spriteType / shine SpriteType / 将领代码），再开放自定义模板。工作量 S–M。

## 排期建议

1. H4MPS：先澄清词义；审查能力用 CWTools（LSP/CLI）承接，M，价值高。
2. IRIS：澄清后大概率砍掉，不排期。
3. 大众脸：本地生成版 M–L，须限定非商用许可。
4. 电台 OGG：先做 S 档（打包/转码 + edge-tts/Piper 语音），音乐生成另议。
5. AOR：已澄清为 xls 批量填鸭工具；下一步实现列表驱动批量模板生成器（S–M），复用 `template_scheduler.py` 的变量替换。
