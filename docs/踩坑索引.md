# 踩坑索引 — HOI4 Mod 编辑器（hearts_of_iron_builder）

> 本文档是**全项目踩坑索引**：把 PROJECT_DOC §5、`docs/已知问题与修复.md`、
> `docs/历史迭代日志.md`、MCP 文档中散落的真实坑集中成表，供人类开发者与 AI Agent 在
> **开始功能开发前按类别速查**，避免重复踩坑。
> 使用约定：开发/修改某个功能时，先定位“功能属于哪个类别”，再读对应分类的「正确范式」列；
> MCP 工具开发还需读 `docs/MCP开发者指南.md` 与 `src/mcp_validator.py` 的规则库。

---

## 目录（TOC）

1. [环境 / 工具](#1-环境--工具)
2. [HOI4 文件事实](#2-hoi4-文件事实)
3. [解析 / 写回](#3-解析--写回)
4. [PyQt6 / UI](#4-pyqt6--ui)
5. [测试](#5-测试)
6. [地图渲染](#6-地图渲染)
7. [settings / 数据单例](#7-settings--数据单例)
8. [MCP 工具注册 / schema](#8-mcp-工具注册--schema)
9. [MCP 运行 / 协议](#9-mcp-运行--协议)
10. [MCP 调用安全](#10-mcp-调用安全)
11. [性能 / 冒烟](#11-性能--冒烟)
12. [历史遗留 / 收口](#12-历史遗留--收口)

---

## 1. 环境 / 工具

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-ENV-001 | 主 AI 无法读图（`read_image` 失败） | 验证界面用「截图 + 统计颜色数」（PIL getcolors）或让用户看实际窗口；禁止产出预览截图 | PROJECT_DOC §5.1 / §4.1 |
| PIT-ENV-002 | Windows 控制台是 GBK，`print` emoji 报 UnicodeEncodeError | 脚本加 `python -X utf8`，或代码强制 UTF-8；verify_contracts 子进程固定 PYTHONIOENCODING/PYTHONUTF8 | PROJECT_DOC §5.1 |
| PIT-ENV-003 | GUI 测试在真实平台会闪窗 / 依赖字体 | 统一 `QT_QPA_PLATFORM=offscreen`；需要真实字体才用 windows | PROJECT_DOC §5.1 |
| PIT-ENV-004 | 沙箱无外网直连，GitHub 访问失败 | 走 Clash 代理 `http://127.0.0.1:7890`；curl.exe schannel 失败时用 python urllib 带代理 | PROJECT_DOC §5.1 |
| PIT-ENV-005 | PyQt6 枚举位置易记错 | `RenderHint` 在 QPainter；`QLineF` 在 QtCore；`QGraphicsItem.CacheMode` | PROJECT_DOC §5.1 |
| PIT-ENV-006 | `np.save` 自动补 `.npy` 后缀 | 临时文件名必须带 `.npy` 结尾（如 `fp + ".tmp.npy"`） | PROJECT_DOC §5.1 |
| PIT-ENV-007 | 启动脚本写死虚拟环境绝对路径（如 `/root/hoi4_builder_venv`），换电脑/换用户必失败 | 统一走 `launcher.py`：项目根=脚本所在目录，虚拟环境默认项目内 `.venv` / `.venv-linux`，也可用 `--venv` / `HOI4_VENV` 覆盖 | docs/启动器.md / launcher.py / tests/test_launcher.py |
| PIT-ENV-008 | 直接从其他目录执行 `python src/main.py`，`settings.json`/`templates` 相对路径失效 | 启动器固定以项目根为工作目录启动主程序；手动直跑前必须 `cd` 到项目根 | docs/启动器.md / src/main_window.py `settings.json` |
| PIT-ENV-009 | Windows `.venv`（Scripts/python.exe）与 WSL/Linux `.venv`（bin/python）在同一仓库互相破坏 | 启动器自动隔离：Windows 遇到 Linux `.venv` 改用 `.venv-win`，POSIX 遇到 Windows `.venv` 改用 `.venv-linux`；创建前拒绝覆盖非当前平台的非空 venv 目录 | launcher.py `default_venv_dir` / tests/test_launcher.py |
| PIT-ENV-010 | Windows cmd 里 `set QT_QPA_PLATFORM=offscreen && ...` 会把尾部空格写进变量，Qt 找不到 `offscreen ` 平台插件 | 用 `set QT_QPA_PLATFORM=offscreen& ...`（`&` 前无空格）或 `set "QT_QPA_PLATFORM=offscreen"`；launcher 的 `--verify` 已用 Python env 设置无此问题 | docs/启动器.md / launcher.py `run_verify` |
| PIT-ENV-011 | 把整套 Python 直接提交进 Git，仓库体积巨大且 Windows/Linux 不通用 | 用 `tools/build_portable.py` 生成便携发布包（`dist/`），不提交 `portable/`；`launcher.py` 检测到 `portable/python/...` 自动进入便携模式 | docs/便携版打包.md / tools/build_portable.py |

## 2. HOI4 文件事实

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-HOI4-001 | 高度图文件名记错 | 高度图是 `heightmap.bmp`；`terrain.bmp` 是地形类型图 | PROJECT_DOC §5.2 |
| PIT-HOI4-002 | 本地化 yml 编码/BOM 处理不当 | 读 `utf-8-sig + allow_bom=True`；脚本文件默认无 BOM；写本地化 yml 用 `utf-8-sig` | PROJECT_DOC §5.2 / docs/MCP与接口规格.md §1 |
| PIT-HOI4-003 | 科技图标三层结构理解错 | 科技定义无图字段 → `interface/*.gfx` sprite 名必须 `GFX_<科技id>_medium` → `gfx/interface/technologies/` 扁平文件 | PROJECT_DOC §5.2 / docs/科技图标存储规则.md |
| PIT-HOI4-004 | 科技树 `path = {}` 被当图标 | `path` 是树连线（leads_to_tech），不是图标 | PROJECT_DOC §5.2 |
| PIT-HOI4-005 | 建筑图标 strip 帧宽硬编码 | 帧宽 = strip 宽 / noOfFrames；mod 与游戏可能不同，按来源取 | PROJECT_DOC §5.2 |
| PIT-HOI4-006 | 国家文件是展开式，无 TAG 包裹 | TAG 从**文件名前缀**取（`JAP - Japan.txt` → JAP），异常名回退内容正则 | PROJECT_DOC §5.3 |

## 3. 解析 / 写回

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-PARSE-001 | `parse_pdx_text_to_nodes` 只返回顶层节点，包裹块被漏 | `equipments = { }` 等必须递归（`_iter_blocks` 先检查自身再递归） | PROJECT_DOC §5.3 |
| PIT-PARSE-002 | 大国家文件提前截断 / 块定位错 | 大文件用字符级 `_block_ranges` 定位块 | PROJECT_DOC §5.3 |
| PIT-PARSE-003 | `tree_node` 把块内裸值解析成 key | `51 204 51` 是三个 key；`victory_points = { 10 2 11 1 }` 按 pid-points 配对解析 | PROJECT_DOC §5.3 |
| PIT-PARSE-004 | 字符串中的 `#` 被当注释截断 | 只在引号外剥离 `#`；未闭合引号抛 ValueError；分词正则保留 `@ : + / ( ) ,` | docs/已知问题与修复.md P0-2 |
| PIT-PARSE-005 | 未闭合引号 / 多余 `}` 被静默吞掉 | `parse_block` 增加 `top` 标记：顶层多余 `}` 抛错；嵌套块未闭合 `{` 抛错 | docs/已知问题与修复.md P0-2 |
| PIT-PARSE-006 | 单行内联块不支持 | `modules = { slot = mod }`、`10 = { naval_base = 3 }` 行尾带 `}`——块匹配/替换须同时支持多行块与单行内联块 | PROJECT_DOC §5.3 |
| PIT-PARSE-007 | 修改后原始行仍覆盖编辑结果 | 结构编辑写回 TreeNode 后必须清 `raw_lines`，父块结构变更同样失效 | docs/历史迭代日志.md 6.75 |
| PIT-PARSE-008 | `tree_node._tokenize` 丢 `/` | 标识符字符集须含 `/`：`gfx/interface/...` 路径保持一个 token | docs/历史迭代日志.md 6.75 |

## 4. PyQt6 / UI

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-UI-001 | `QPainter.drawPixmap` 没有 `(float, float, QPixmap)` 重载 | 用 `drawPixmap(QPointF(...), pixmap)` | PROJECT_DOC §5.4 |
| PIT-UI-002 | `QWidget.updatesEnabled` 在 PyQt6 是方法 | `c.updatesEnabled()` | PROJECT_DOC §5.4 |
| PIT-UI-003 | QGraphicsView 默认 QFrame 边框导致 grab 坐标偏移 | 断言 hover 高亮读 `hover_item.pixmap()`，不要 grab 像素 | PROJECT_DOC §5.4 |
| PIT-UI-004 | `QTest.mouseMove` 不可靠，事件不投递 | 构造 QMouseEvent 带 buttons；offscreen 多窗口不投递时用 `_send_move` helper（app.sendEvent） | PROJECT_DOC §5.4 |
| PIT-UI-005 | 模态对话框 `exec()` 在 offscreen 阻塞测试 | 测试不得直接调用，直接改数据/重建编辑器 | PROJECT_DOC §5.4 |
| PIT-UI-006 | `QComboBox.addItem` 在 blockSignals 期间自动选第 0 项 | 初始化后手动补 `_on_combo_changed(0)` | PROJECT_DOC §5.4 |
| PIT-UI-007 | 测试 patch 错 QInputDialog 模块路径 | 需指向 `PyQt6.QtWidgets.QInputDialog`（方法级 import，模块级无该名字） | PROJECT_DOC §5.4 |
| PIT-UI-008 | PyQt6 重写 `QTreeWidgetItem.setData` 返回 bool 导致崩溃 | C++ 签名是 void，`setData` 必须返回 None；提交后行刷新/信号延到事件循环（QTimer.singleShot(0)） | docs/历史迭代日志.md 6.78 / src/structure_view.py |
| PIT-UI-009 | 行内编辑器太矮显示不全 | 用 QPlainTextEdit 自动换行 + `updateEditorGeometry` 向下扩展多行 | docs/历史迭代日志.md 6.78 |
| PIT-UI-010 | 对话框/事项未及时关闭导致事件不投递 | 测试用例里及时 close/deleteLater + processEvents | PROJECT_DOC §5.5 / docs/历史迭代日志.md 6.83 |

## 5. 测试

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-TEST-001 | 全量测试在真实平台不稳定 | offscreen 下跑：`QT_QPA_PLATFORM=offscreen PYTHONPATH=src python -m unittest discover -s tests -t .` | PROJECT_DOC §5.5 |
| PIT-TEST-002 | Windows 只读目录权限用例失败 | 权限用例在 Windows skip | PROJECT_DOC §5.5 |
| PIT-TEST-003 | 测试临时目录乱写系统 %TEMP% | 契约测试统一在 `.runtime/test_tmp` 建临时目录 | tests/test_infra.py `_mkdtemp` |
| PIT-TEST-004 | 无文件模式测试构造国家名失败 | 需在 `game/common/country_tags` 写 `TAG = "countries/…"` | PROJECT_DOC §5.5 |
| PIT-TEST-005 | 真实翻译器给展示文本加 `--中文`，断言写死失败 | 断言用 `.split("--")[0]` 取原始部分 | docs/历史迭代日志.md 6.83 |

## 6. 地图渲染

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-MAP-001 | QTransform 组合语义写反 | `scale(s,s).translate(-rx0,-ry0)` = 点先平移后缩放（`(p-r)*s`） | PROJECT_DOC §5.6 |
| PIT-MAP-002 | 瓦片尺寸按逻辑像素而非设备像素 | 按 w×dpr 计算，高分屏才不会缺画面 | PROJECT_DOC §5.6 |
| PIT-MAP-003 | `_blend_border` 依赖 0xAARRGGBB 小端布局 | 大端回退 QPainter | PROJECT_DOC §5.6 |
| PIT-MAP-004 | clipBoundingRect/exposedRect 在 grab 场景不可靠 | `_paint_fill` 用设备尺寸反变换算可见范围 | PROJECT_DOC §5.6 |
| PIT-MAP-005 | 瓦片位置跟随偏移 | 瓦片内容 blit 时把场景区原点经 `worldTransform().map()` 投影到当前设备位置并四舍五入 | PROJECT_DOC §5.6 |

## 7. settings / 数据单例

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-SET-001 | 调试忽略 settings.json 当前值 | 先读 `settings.json`（mod_path / HOI4_path / ui_mode / map_*）；如无必要不要改 | PROJECT_DOC §5.7 |
| PIT-SET-002 | 无文件模式“选择”误写文件 | 选择国家纯选择不写文件；“国家设置（复制/创建）”才是写操作 | PROJECT_DOC §5.7 / tests |
| PIT-SET-003 | 数据单例/方向搞反 | `owner_province_map()` 返回 tag→pids；`country_overlay_pixmap` 需要 pid→tag | PROJECT_DOC §5.7 |
| PIT-SET-004 | 缓存清理 no-op | `_clear_oob_caches()` 若查自身 globals 的 `_EQUIP_STATS_CACHE`（实际在 oob_stats）永远 None；改用 `oob_stats.clear_equip_stats_cache()` | docs/已知问题与修复.md P0-5 收口 |

## 8. MCP 工具注册 / schema

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-MCP-REG-001 | 工具名重复/无 schema/handler 不可调 | 工具名全局唯一；`inputSchema.type=object`、properties 为 dict、handler callable | tests/test_infra.py::McpRegistrationTest |
| PIT-MCP-REG-002 | `create_mod` schema 缺 `mod_folder_path`/`approved` | `mod_folder_path*`；`dry_run=false` 时必须 `approved=true` | tests/test_mcp_categories.py |
| PIT-MCP-REG-003 | AI CRUD 必填缺失 | `create/update/delete` 必填 `id`；`rename/duplicate` 必填 `id`+`new`；`ai_navy_*` 必填 `section` | tests/test_mcp_categories.py |
| PIT-MCP-REG-004 | 生成器无示例，agent 不会用 | 7 个 `generate_*` 附加 `examples` 最小入参 | tests/test_mcp_categories.py |
| PIT-MCP-REG-005 | 写工具 schema 没有体现写回纪律/dry_run | 批量/结构操作默认 `dry_run=true`；写操作只写 mod 目录；游戏本体先 ensure_file_in_mod | docs/MCP与接口规格.md §1 / src/mcp_validator.py |
| PIT-MCP-REG-006 | 工具描述为空/含 TODO，agent 无法判断用途 | 每个工具 description 非空、说明正确范式（dry_run/approved/路径约束） | src/mcp_validator.py |

## 9. MCP 运行 / 协议

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-MCP-RUN-001 | Windows 控制台 GBK 破坏 MCP stdio JSON | `_force_utf8_stdio()` 强制 stdin/stdout UTF-8 | src/mcp_server.py |
| PIT-MCP-RUN-002 | 官方 mcp 库未安装时无法工作 | 回退内置零依赖 newline JSON-RPC 2.0（2024-11-05），支持 initialize/ping/tools/list/tools/call/resources/prompts | docs/MCP与接口规格.md §6 |
| PIT-MCP-RUN-003 | 178 个工具全部暴露导致 agent 上下文爆炸 | A+B 分类：默认只暴露核心精选+3 导航；`MCP_EXPOSE_CATEGORIES` 白名单或 `all` 全开；隐藏工具经 `invoke_tool` 调用 | docs/MCP与接口规格.md §6A |
| PIT-MCP-RUN-004 | resources/prompts 与文档脱节 | `hoi4://docs/*` 资源的文件名/路径与 docs 目录一致；新增文档要同步 `_list_resources/_read_resource` | tests/test_mcp_categories.py::ResourcesAndPromptsTest |
| PIT-MCP-RUN-005 | 工具调用不校验参数直接进 handler | MCP server 在 `tools/call`、官方 FastMCP 包装层与 HTTP `/api/mcp/invoke_tool` 调用 `mcp_validator.validate_call`，缺必填/类型错/越界路径/未批准写操作自动拦截 | src/mcp_validator.py / tests/test_mcp_validator.py |

## 10. MCP 调用安全

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-MCP-SEC-001 | 文件路径可越出 mod 目录 | 所有文件入口经 `path_safety.safe_join` / `is_within`；拒绝绝对路径、`..`、盘符、符号链接逃逸 | docs/已知问题与修复.md P0-3 |
| PIT-MCP-SEC-002 | `analyze_error_log` 可读任意绝对路径 | `absolute_path` 仅限 mod/game 根内；`path`（相对 mod）沿用 `_safe_join` | docs/已知问题与修复.md P0-3 收口 |
| PIT-MCP-SEC-003 | `import_unit_counters`/生成器 output_dir 可越界 | `output_dir` 必须为 mod 内相对路径；生成器 filename 校验 | docs/已知问题与修复.md P0-3 收口 |
| PIT-MCP-SEC-004 | `create_mod` 未批准就落盘/路径越界 | 非 dry_run 需 `approved=true`；`mod_folder_path`/`mod_file_path` 限制在 settings 白名单根与当前 mod 父目录内 | docs/已知问题与修复.md P0-3 收口 |
| PIT-MCP-SEC-005 | 本地化 tag/文件名可注入分隔符 | `validate_component()` 拒绝分隔符 / `..` / 盘符 / 以数字开头 | docs/已知问题与修复.md P0-3 |

## 11. 性能 / 冒烟

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-PERF-001 | 重型 MCP 工具未归 heavy，冒烟挂死 | `get_icon_manifest`（实测 238s / 20,430 条）、`list_missing_localisation`/`batch_fill_localisation`（202s）归入 `HEAVY_TOOLS` | docs/MCP与接口规格.md §6H / tools/smoke_mcp_tools.py |
| PIT-PERF-002 | 冒烟 `skip-data` 被误判为 error | 数据缺失记 `skip-data` 非致命；退出码只按 error 判定 | tools/smoke_mcp_tools.py |
| PIT-PERF-003 | 写工具无 dry_run 冒烟直接落盘 | 写工具自动填 dry_run=true；无 dry_run 写工具记 `skipped-write` | tools/smoke_mcp_tools.py |
| PIT-PERF-004 | 真实数据全量冒烟不设 guard | 无真实 mod/game 时用 `skipUnless` 跳过，真实环境才跑全量 | tests/test_mcp_smoke_real.py |
| PIT-PERF-005 | `verify_contracts` 语法编译 / 写入纪律扫描遍历 `.runtime` 大量测试临时文件导致挂起 | `_py_files` 与扫描器跳过 `.runtime`、`.idea`、`.ruff_cache`、`.jspace` | tools/verify_contracts.py / tools/check_write_discipline.py SKIP_DIRS |

## 12. 历史遗留 / 收口

| ID | 踩坑描述 | 正确范式 | 证据 / 回归 |
| --- | --- | --- | --- |
| PIT-HIST-001 | mod/vanilla 覆盖顺序颠倒 | 数据加载一律「先游戏、后 mod」，mod 值覆盖游戏值 | docs/已知问题与修复.md P0-1 |
| PIT-HIST-002 | 撤销丢 BOM/CRLF、失败丢条目 | 快照按原始字节；`atomic_write_bytes()` 无损恢复；成功后才 pop | docs/已知问题与修复.md P0-4 |
| PIT-HIST-003 | OOB 保存写坏外层 `}` / 误删无 name 模板 | `save_names_group` 精确匹配组块闭合 `}`；模板匹配支持“无 name 字段” | docs/已知问题与修复.md P0-5 |
| PIT-HIST-004 | 结构组件退出编辑崩溃 | `StructureItem.setData` 返回 None；提交刷新/信号延迟到事件循环 | docs/历史迭代日志.md 6.78 |
| PIT-HIST-005 | `set_compact` 不影响间距 / 外层块行多余展示 | StructureView 提供 `set_compact`、`set_header_visible`；内层列表载入剥最外层 wrapper，序列化自动补 wrapper | docs/历史迭代日志.md 6.80 |

---

## 使用说明（开发前必读）

1. 开始任何功能开发前，**先按功能类别阅读上表对应分类**；MCP 工具开发额外阅读
   `docs/MCP开发者指南.md` 与 `src/mcp_validator.py` 的规则。
2. 遇到新坑时，先搜本文档有没有同 ID/同类记录；若无，在对应分类新增一行，并在
   `docs/历史迭代日志.md` 记录现场。
3. 本文档是索引，详细证据/修复步骤看“证据 / 回归”列指向的文档。
4. `src/mcp_validator.py` 的规则 ID（`MCPVAL-*`）与本文档 MCP 分类条目一一对应，
   新增规则时同步补索引。