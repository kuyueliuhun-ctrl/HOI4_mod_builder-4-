# 未完成计划（整合总表）

> 建立：2026-08-19
> 用途：把散落在三处的「未完成」条目**整合为唯一待办总表**，供下一接手代理直接翻用：
> 1. `docs/QIUQI-LIBRARY映射与复刻矩阵.md`  —— QIUQI 工具/模板复刻总索引（§2/§3/§5）
> 2. `docs/RHoiScribe知识映射与补全.md`       —— RHoiScribe 知识吸收的剩余主题与补全
> 3. `AGENTS.md §6.17 遗留/可选后续` + 已知待办
>
> 本文件**只汇总未完成条目**，不重复已有内容；每项标注：来源 / 当前状态 / 前置（调研或
> 用户拍板）/ 建议落地。状态变更时同步改**源文档**与本表。
> 约定：含 UI 的条目按 AGENTS §4.11 必须先问用户或给方案，本表一并列出"待拍板"项。

---

## 0. 批次定义

| 批次 | 含义 | 通常需要 |
| --- | --- | --- |
| **P0 校正/收尾** | 文档矛盾修正、指针归位，无新功能 | 无 |
| **P1 需调研** | 先读原始输出/源码/xls 结构，产出结论再动工 | 用户样例 / 沙箱运行 |
| **P2 需复刻** | 可实现，沉淀为代码/模板 | 部分需 UI 拍板 |
| **P3 需转模板/文档** | 把知识沉淀为 `templates/` 与 `docs/游戏文件内容详解.md` | 无 |
| **P4 暂缓/外部/不做** | 不列入近期，仅登记 | 无 |

---

## P0 — 校正与收尾

| # | 项 | 现状 / 问题 | 动作 |
| --- | --- | --- | --- |
| 1 | 错误日志分析（矩阵 §3.4 + 状态表） | 矩阵 §3.4 行标「需复刻/当前项目=无」，但状态表 244 已「已完成」；状态表 249 重复「待开始」，且 `tools/error_log_analyzer.py` + `src/error_log.py`（含子系统归类）已存在 | 矩阵改「已完成（2026-08-19）」；删状态表重复行 249 |
| 2 | interface 注册（矩阵 §3.4） | 行标「需复刻」，但状态表 243 已「已完成」；`src/interface_reg.py` 已存在 | 矩阵改「已完成（2026-08-19）」 |
| 3 | AGENTS §6.17「遗留」缺 2026-08-19 批次 | 本地化编辑器 / QIUQI 词条 / 实体资源工作台 / 一、二批工具 / 角色 UI / RHoiScribe 吸收均未写入 AGENTS §6（§6 最后一条是 6.21） | AGENTS 补一节 6.22「已完成（2026-08-19）摘要」并挂本文档链接 |
| 4 | 各源文档指针 | 矩阵「下一步建议」未指向唯一未完成总表 | 矩阵 §6 增加指向本文档；AGENTS §6.17 增加指向本文档 |

---

## P1 — 需调研后再做

> 前置统一要求：调研产出一份「原工具输出/输入规格」结论（记入 `docs/`），再决定复刻范围。

| # | 项 | 来源 | 现状 | 先做的调研任务 | 建议落地 |
| --- | --- | --- | --- | --- | --- |
| 1 | **地图文件划分大洲** | `工具/地图/地图文件划分大洲(来自月茗星稀).exe`（33MB GUI） | 无实现 | 运行/观察其输出（可能按大洲给省/州分组写文件或 strategicregions），需用户提供样例输出或允许沙箱跑 exe | `tools/continent_splitter.py` + `map_region_ops.py` 增强 |
| 2 | **批量填鸭工具（AOR）** | `工具/通用/批量填鸭工具：AOR自研版.xls` | 无实现 | 用 pandas/openpyxl 解 xls 列结构（表头行/列含义），判断能否映射到 `template_scheduler.py`/词条库 → 表格批量生成代码 | `template_scheduler.py` 增强或独立导入器 |
| 3 | **电台生成 python 脚本** | `工具/通用/电台生成python脚本` | 无实现 | 读脚本，识别输入/输出（music 文件 + songs.txt + 电台本地化）；OJGG 转换部分依赖外部编码器 | 文件/本地化生成部分脚本化，OGG 转换保留外部 |
| 4 | **HoI4ModdingPythonScripts 逐一审查** | `E:\HoI4ModdingPythonScripts`（python2/python3 ~11 个小脚本） | 未逐个给出结论 | 已覆盖：`hoi4fileformatter`→pdx_formatter、`focusgfxshine`/`hoi4ideagfxentry`→icon_ops、`hoi4localisationadder`→本地化、`hoi4statemanpowermultiplier`→state_batch。待评估：`DHtoHoi4MinisterConverter`、`hoi4transfertechsegen`、`hoi4newspaperheaderadded`、`hoi4statemapgenerator`、`USAElectionGenerator` | 逐脚本标注「已覆盖 / 价值低 / 需复刻」清单 |
| 5 | **核心圈层工具** | `工具/高级/核心圈层工具.url` → GitHub `Leslie-Lin/Hoi4_inner_circle_modding_tool`（外部仓库） | 外部链接 | 克隆分析其 inner circle 决议/机制输出，决定吸收为模板或保留外部 | 保留外部（登记）+ 机制进文档 |
| 6 | **本地化生成（IRIS）特有功能验证** | `工具/通用/本地化生成（来自IRIS Rekordny）.exe` | 已覆盖（重叠） | 与现有「一键补写/缺失检测」对比，找出 IRIS 独有能力（Excel 模板导入/多语言骨架等）再决定增强 | 按验证结果增强 `localisation_editor_*` |
| 7 | **大众脸生成器** | `工具/通用/钢铁雄心4大众脸生成器(https).url` → GitHub（外部） | 未登记 | 外部；仅登记 URL（人脸生成依赖外部引擎/素材，不宜内置） | 保留外部 |

---

## P2 — 需复刻 / 实现

| # | 项 | 现状 | 落地 | 是否需拍板 |
| --- | --- | --- | --- | --- |
| 1 | **自生成 GUI 决议包（滑条适配版）** | QIUQI 自带完整成品 mod `decision_outlay_gui`（common/mtth、on_actions、scripted_effects/guis/localisation/triggers、technologies、interface 滑条 GUI 模板、中文本地化、教程） | 吸收为 `templates/系统模板/GUI编辑/自生成决议包`（基础 file + 各块模板）+ 可选 `tools/gui_decision_generator.py` | 需拍板（模板库 vs 脚本生成器） |
| 2 | **民族精神编辑器（专用 UI）** | 生成器已完成（`src/idea_gen.py` / `content_generators.py ideas`），**编辑仍走通用树** | 专用编辑器（参考 BOP/AI 专用 UI 模式） | 需拍板 |
| 3 | **意识形态生成器专用 UI** | `src/ideology_gen.py` 完成，入口在 `country_setup_dialog` | 独立专用 UI | 需拍板 |
| 4 | **事件专用 UI（含图片注册一键组合）** | `src/event_gen.py` + `src/icon_batch.py` 完成 | 事件专用编辑器 + 「生成框架→注册图片」一键流 | 需拍板 |
| 5 | **唯一标识符扫描工具** | 吸收自 RHoiScribe 补全 A-3；`validation.collect_entity_keys` / error_log 重复 ID 思路已有基础 | 跨 mod+game 扫描 角色/旗标/TAG/理念 token/动态修正/国策（区分 节点 ID vs 树 ID）/决议/事件命名空间 等重复 → 红/黄报告 | 否 | 已完成（2026-08-19）`tools/unique_id_scanner.py`（CLI，无 UI）+ `UniqueIdScannerTest` 4 用例 |
| 6 | **编制编辑器小项（AGENTS §6.17）** | 三个遗留 | ① 模板改名后校验 history/units 部署引用一致性提示；② 装备 IC 花费估算（当前只统计件数）；③ 海军/空军 version_name 设计解析（调研已完成未实现） | 否 |
| 7 | **兵牌图标接入单位标牌库** | OOB 现用 `GFX_unit_<type>_icon_medium`，失败回退黑底占位 | 接 `unit_counter_library`（onmap_*.dds 已导入） | 否（内部增强） |
| 8 | **RHoiScribe MCP 服务器复刻为项目插件** | 现仅「知识吸收 + 接口 + 工具增强」，MCP 本体仍是外部 Rust 程序 | 若要以插件形态内置，需评估移植成本 | 需拍板（可选后续） |

---

## P3 — 需转模板 / 文档



### 3b. RHoiScribe 补全回填主文档

> ✅ **已完成（2026-08-19）**：A/A-2/A-3/D/F(限时)/G(新闻)/I/J/K 要点+真实语法已回填
> `docs/游戏文件内容详解.md`（§16.3 on_actions / §18.4 数组 / §18.5 唯一标识 / §4.1 化妆标签 /
> §6.2 限时任务 / §12.3 战略区域补给区域 / §12.5 邻接补给 / §17.4 脚本化 GUI / §17.5 音乐音效）；
> `RHoiScribe知识映射与补全.md` 顶部已加"回填入库"说明。仍待：E(defines 独立章节)、H(shared_focus_filters 详细)。

### 3c. 特殊案例 / 教程提炼（矩阵 §2.3，高优先）

> ✅ **已完成（2026-08-19 高优先项）**：`逆向AI策略简明教程` → 主文档 **§15.9**（allowed/enable/enable_reverse 语义 + 不能自指 + imgui 调试）；
> `TNO行政区划系统运作原理` → 主文档 **§12.6** 设计模式摘要；
> `OOB文件通用模板`+`开局部队和生产线设置` → §12.3 完整版格式（含海军/空军/开局生产）。
> 其余低优先教程（P语言/defines/GUI/Shader）仍按需提交。

---

## P4 — 暂缓 / 外部 / 不做

| 项 | 说明 |
| --- | --- |
| 旗帜创建 / 转换 / 处理（3 项） | 图像算法复杂，暂缓；外部工具保留（`工具/国家/旗帜工具/`） |
| 议会 GUI 生成器 | 复杂，先调研 |
| Shader 编辑器 | 保留外部 |
| 彩虹图（帧图）拼接 | 图像算法，暂缓 |
| 电台系列 OGG 转换 | 依赖外部编码器；文件生成部分可选（见 P1-3） |
| 系统预览插件 | 系统级插件，不入库 |
| VModer / HOI4DEV | 参考借鉴（词条/语法提示），不做复刻 |
| 战略区域创建 | **已覆盖**（region_editor_dialog 支持框选划分 strategicregions/supplyareas/states），不再做 |
| idea 排列叠加生成器 | 用户已拍板不做（2026-08-19） |
| 阵营模板 / 高级 GUI / 超事件 / 预设GUI | 暂缓 |

---

## 需用户拍板清单（AGENTS §4.11）

1. **民族精神 / 意识形态 / 事件** 是否需要专用编辑器 UI（还是维持通用树 + 生成器即可）。
2. **大洲划分** 调研方式：提供样例输出，还是允许沙箱运行原 exe。
3. **自生成 GUI 决议包** 以「模板库」还是「脚本生成器」形态落地。
4. **RHoiScribe MCP 服务器** 是否需要内置为项目插件（还是维持现有「接口+知识」接入）。
5. 兵牌图标接入单位标牌库、编制 3 小项等内部增强的优先级。

---

## 建议执行顺序

1. **P0**：先消除矩阵/AGENTS 状态矛盾，让「已完成/未完成」一目了然。
2. **P3b+P3a**：RHoiScribe 补全回填主文档 + 模板落库（无 UI、无拍板，纯沉淀，价值高）。
3. **P1**：逐项调研（大洲/填鸭/电台/H4MPS），每项产出规格结论再决定复刻。
4. **P2**：等用户对拍板项答复后，按「内部增强（5/6/7）→ 专用 UI（2/3/4）→ 决议包（1）→ MCP 插件（8）」推进。

> 本文件为未完成条目的唯一总表；完成后把源文档状态改掉并同步本表。
