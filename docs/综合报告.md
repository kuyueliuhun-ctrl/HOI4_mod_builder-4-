# 综合报告（当前状态 · 2026-08-23）

> 本文件整合/替代以下历史报告：
> `docs/UI评估报告.md`、`docs/UI回归验证结果.md`、`docs/接口复现报告.md`、
> `docs/archive/UI评分清单.md`、`docs/archive/UI评分评估结果.md`、
> `docs/覆盖检查报告.md`。
> 机器生成类报告（如 `ui_gap_probe.py` 输出）可按需再生成，不入库。

---

## 1. 工程与验证状态

- Python：Windows `.venv` 3.14.5 / WSL 3.14.4 双版本。
- `tools/verify_contracts.py`：语法编译、ruff、约 395 用例、写入纪律、四层依赖、
  行数预算、UI 缺口轻量探针全部通过。
- 工程债 F0~F10 已完成；UI 第一批 A1~A8 已完成。
- 当前主战场：B 线 P10~P39（见 `docs/整合计划.md`）。

## 2. UI 评估结论（原 UI评估报告/回归验证）

- 历史 v2 的两类 🔴 严重问题（词条不可见/不可编、强迫编辑原始 PDX）已通过
  批次 A~E/§0.x 修复并回归验证。
- 低分 UI 回归结果：

| UI | 历史评分 | 现状态 |
| --- | --- | --- |
| 科技树 | 4 分 | ✅ 完整表单 + 画布联动，tech 缺口 0 |
| 飞机设计器 | 2 分 | ✅ 槽位/空配件/同款同步/未定义机体容错 |
| 坦克设计器 | 2 分 | ✅ 完整底盘/模块/变体读写 |
| BOP | 4 分 | ✅ 区间/势力/决议 CRUD + 本地化 |
| 角色编辑器 | 4 分 | ✅ roles/portraits 结构化，缺口 0 |

- 缺口探针：`event/tech/character/bop --max-files 0` = 0/0；
  focus/state/country_history 为登记长期豁免项。

## 3. 覆盖检查结论（原覆盖检查报告）

- 游戏 `common/` 78 个子目录 + mod 顶层目录：入口覆盖 **100%**。
- 内容类型总数：**93**；其中 12+ 种已有专用 UI，其余走通用树形编辑器
  （后续按 B2/B3 逐步专用化）。
- B2/B3 专用化进度（通用 SimpleBlockEditorDialog + 侧栏 CRUD 铺开）：
  动态修正 / 间谍行动 / 科学家特质 / 战术 / 触发修正 / 事件修正 /
  意识形态 / 占领法 / 抵抗活动 / 和会 / 特种能力 / 王牌 / 集合 / MTTH /
  主界面 / 奖章 / 缎带 / 部队勋章 / 突袭 / 限时活动 / 情报 / 生成 / 行动阶段 /
  地图模式 / 行动令牌 / 脚本化外交行动 / 计分器 / 修正量定义 / 科技标签 /
  抵抗合规 / 脚本 GUI / 国家领袖 / 州类别 / 资源 / 装备组 / 脚本常量 /
  脚本化本地化 / 科技共享 / 战略要地 / 观点修正 / 战争目标 / 难度设置 /
  游戏规则 / 自治状态 / 国家别名 / 剧本 / 修正类型 / 部队标签 / 特殊计划。
- B3 嵌套实体编辑器（NestedBlockEditorDialog，wrapper → 实体块）：
  on_actions / 持续国策 / 将领特质 / 理念槽位 / 理念 / 决议 / 建筑 / 地形，
  支持按 id 字段定位 + 侧栏 CRUD。
- B2 脚本库（RawBlockEditor，原始脚本体编辑 + 侧栏 CRUD）：
  效果结构体 scripted_effects / 条件结构体 scripted_triggers /
  枚举结构体 script_enums，工作台三分项共用一个编辑器。
- 扩展名：`.txt/.gui/.lua/.yml/.yaml/.gfx/.mod` 均支持。
- 无文件模式：已覆盖 12 项功能。

## 4. 接口复现 / MCP 结论（原接口复现报告 + MCP规格）

- MCP 工具总数 **159**，HTTP `/api/mcp/<tool_name>` 同源桥可调用全部工具。
- ApiCore 域扩展：states/designers/ai_content/bop/loc_tools/health/media/generators/project。
- 文件级复现：78 次读写逐文件核对通过；实体级 create 为追加式，
  文件级端点补齐包装结构；无批量端点已登记。

## 5. 有效报告/规格入口

| 文档 | 用途 |
| --- | --- |
| `docs/整合计划.md` | 唯一执行总表 |
| `docs/MCP与接口规格.md` | MCP/HTTP 接口规格 |
| `docs/功能与实现文档.md` | 功能与实现说明 |
| `docs/验证契约.md` | 工程契约 |
| `ui_gap_probe.py` | UI 缺口探针（可再生成报告） |