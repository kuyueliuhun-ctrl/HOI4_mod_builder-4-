# 整合计划（唯一执行总表）

> 生成：2026-08-23（2026-08-23 用户拍板：被整合的旧计划文档已删除）
> 用途：当前**唯一执行总表**。
> 被整合并删除：`问题.md`、`docs/修复计划.md`、`docs/未完成计划.md`、
> `对话.md`、`docs/UI修复与建构执行方法.md`。详细任务规格已并入本文件，
> 剩余参考：`docs/待制作功能.md`。

---

## 1. 已完成（不再重复执行）

| 块 | 内容 | 状态 |
| --- | --- | --- |
| 工程债 F0~F10 | 拆分提交 / 测试拆分 / ruff+预算 / UI 验证 / 设计器基类 / 探针门禁 / 环境 / 仓库卫生 / Python 3.14 / 限制汇总 | ✅ |
| UI 第一批 A1~A8 | 事件/科技/州字段/BOP/兵种/raw 降级/§0.x/收尾 | ✅ |
| 问题 P1~P11 | 均已闭环或登记长期豁免/触发式 | ✅ |

## 2. 当前主战场：B 线 P10~P39

### B0 · 公共组件（依赖前置，**当前开始**）

- 新建 `src/ui_widgets.py`：
  `BlockTreeList` / `BlockListCard` / `BLOCK_CN` 词典 / `LocEdit` /
  `StructuredBlockBrowser`（高级入口）/ `TriggerCard` / `WeightCard` /
  `WeightTable` / `RefPicker` / `OrderRowList` / `OtherFieldsTable` /
  `source_badge`。
- `ScriptBlockEditorDialog` 内部重构为 BlockTreeList 封装。
- 测试：`UiWidgetsTest`（每组件 roundtrip + 树编辑/右键动作/双语显示）。

### B1 · AI 工作台改版（P10~P16）

| 任务 | 说明 |
| --- | --- |
| P10 战略计划 | traits/内嵌策略解析写回；TriggerCard×3、OrderRowList、WeightTable×3、WeightCard |
| P11 战略倾向 | 特工条目字段；type 枚举下拉；source_badge |
| P12 师模板 | 两级侧栏；升级链 RefPicker + 成环校验 |
| P13 AI 装备 | category/roles/target_variant 卡 + 设计器联动 |
| P14 AI 海军 | goals/fleet/taskforce 全结构化，消灭 raw |
| P15 区域+战区 | regions 地图框选联动 |
| P16 AI 杂项新建 | attitudes/personalities/ai_peace/mio_ai_weights |

### B2 · 通用类型工作台（P17~P27）

P17 脚本库 / P18 on_actions / P19 动态修正 / P21 装备定义 / P22 MIO /
P23 国家历史 / P24 特殊计划 / P25 学说 / P26 派系 / P27 间谍行动。
统一四件套：数据层 → 对话框 → 路由 → ui_gap_probe spec + 测试。

### B3 · 全量覆盖（P28~P39，`simple_entity_tab` 驱动）

P28 决议 / P29 民族精神+理念标签 / P30 意识形态 / P31 领袖与特质 /
P32 持续国策+内嵌窗口 / P33 政治与和会 / P34 经济与地图 / P35 军事奖励与情报 /
P36 脚本与修正 / P37 剧本与主界面 / P38 配置与清单 / P39 高级文件。

## 3. 功能性待办

- **P1 需调研**：大洲划分 / 批量填鸭(AOR) / 电台生成 / H4MPS 审查 / 核心圈层 / IRIS / 大众脸。
- **P2 需复刻/实现**：自生成 GUI 决议包、民族精神/意识形态专用 UI、编制小项、兵牌图标接标牌库、RHoiScribe MCP 插件。
- **P2.5 已知限制**：数值估算、未定义 airframe 容错、version_name 联动、兵牌图标、装备 IC 花费（登记）。
- **P3 需转模板/文档**：RHoiScribe 补全、特殊案例/教程提炼。
- **P4 暂缓/外部/不做**：旗帜、议会 GUI、Shader、电台 OGG、系统预览等。
- **需用户拍板清单**：大洲调研方式、GUI 决议包形态、MCP 插件、民族精神/意识形态 UI 等。

## 4. 触发式/长期项

- `generic_tree_editor.py` / `advisor_assign_dialog.py` / `map_canvas.py`
  行数预算白名单保留，下次功能改动时顺手拆分（F5 第 5 项）。
- state/country_history/focus 的 UI 缺口按 `ui_gap_probe` spec `ci_exempt`
  长期收敛，见上文 §3。

## 5. 执行顺序

1. **B0 公共组件**（现在开始，B1/B2/B3 全部依赖）。
2. B1（P10~P16）与 B2（P17~P27）并行；B3 待 `simple_entity_tab` 就绪后铺开。
3. 未完成计划中无拍板依赖的 P2 内部增强可穿插（兵牌图标、编制 3 小项）。
4. 每任务交付：代码 + 测试 + 真实数据冒烟 + `ui_gap_probe --max-files 0`
   闭环 + 本表状态更新。

## 6. 状态

| 块 | 状态 |
| --- | --- |
| B0 公共组件 | ✅ 已完成（组件全量首版 + PDX 解析 + ScriptBlockEditorDialog 结构化树封装） |
| B1 AI 工作台 | 🔶 进行中（P10 国策顺序 OrderRowList 完成） |
| B2 通用类型 | ⬜ 未开始 |
| B3 全量覆盖 | 🔶 前置完成（simple_entity_tab 已建），P28~P39 待铺开 |
| 未完成计划 P1~P4 | ⬜ 未开始（按拍板/调研推进） |