# 钢铁雄心4（Hearts of Iron IV）游戏文件内容详解

> 本文档面向 HOI4 Mod 作者，结合游戏本体文件结构、`common/` 各子目录的实际用法、钢铁雄心4 wiki 词条与本项目 `常用代码/` 中多份社区模板，对各类游戏文件的作用、位置、写法进行系统剖析。
>
> 引擎版本：Clausewitz（Paradox 系通用），脚本语言通常称为 **PDX 脚本 / HOI4 脚本语言**。

---

## 目录

1. [前置知识：PDX 脚本语言](#一前置知识pdx-脚本语言)
2. [Mod 工程结构](#二mod-工程结构)
3. [游戏目录总览](#三游戏目录总览)
4. [国家与意识形态](#四国家与意识形态)
5. [国策、持续国策](#五国策与持续国策)
6. [事件与决议](#六事件与决议)
7. [民族精神与理念](#七民族精神与理念)
8. [角色与国家领袖](#八角色与国家领袖)
9. [科技与装备](#九科技与装备)
10. [兵种与师编制](#十兵种与师编制)
11. [建筑与资源](#十一建筑与资源)
12. [历史档：国家 / 州 / 初始部队](#十二历史档国家--州--初始部队)
13. [剧本（Bookmarks）](#十三剧本bookmarks)
14. [外交与政治机制](#十四外交与政治机制)
15. [AI 体系](#十五ai-体系)
16. [脚本化机制](#十六脚本化机制)
17. [本地化与界面资源](#十七本地化与界面资源)
18. [编写规范与常见错误](#十八编写规范与常见错误)
19. [速查附录](#十九速查附录)

---

## 一、前置知识：PDX 脚本语言

HOI4 的绝大部分数据文件使用 Paradox 自研的 **Clausewitz 脚本语言**，语法类似 JSON / XML 的混合体。

### 1.1 基本元素

| 元素 | 写法 | 说明 |
|------|------|------|
| 键值对 | `key = value` | 最基本单元，值可以是数字、布尔（yes/no）、标识符或字符串 |
| 代码块 | `key = { ... }` | 花括号包裹的子作用域 |
| 字符串 | `"带空格的文本"` | 必须用英文双引号；无空格的关键字可省略引号 |
| 注释 | `# 这是注释` | `#` 到行尾均为注释 |
| 布尔 | `yes` / `no` | 相当于 true / false |

```text
# 单个国策节点示例
focus = {
    id = GER_rearmament
    icon = GFX_goal_generic_construct_civilian
    cost = 10
    available = { has_political_power > 100 }
    completion_reward = { add_political_power = 120 }
}
```

### 1.2 作用域（Scope）

脚本是**作用域嵌套**的。上层代码决定了当前作用域（国家 / 州 / 角色 / 部队等），子块切换作用域。

- **国家作用域（country）**：`add_stability`、`has_war`、`tag = GER`
- **州作用域（state）**：`is_core_of`、`add_building_construction`
- **角色作用域（character）**：`has_trait`、`is_field_marshal`
- **部队作用域（unit）**：`division_has_majority_template`

关键作用域代名词：

| 关键字 | 含义 |
|--------|------|
| `ROOT` | 当前主体（触发此事件/使用此国策树的国家） |
| `FROM` | 触发方（如让另一国触发本事件的国家） |
| `PREV` | 上一层作用域 |
| `THIS` | 当前作用域 |

### 1.3 三大脚本元素

整个游戏脚本体系由三类构成，几乎每个文件都在写这三样东西：

| 元素 | 关键字 | 出现位置 |
|------|--------|----------|
| **触发器 trigger** | 判断真假的条件 | `available` / `visible` / `allowed` / `bypass` / `trigger` / `limit` |
| **效果 effect** | 改变游戏状态的指令 | `completion_reward` / `complete_effect` / `option` / `immediate` |
| **修正 modifier** | 持续性数值加成 | `modifier = { ... }` 块内 |

```text
# 触发器（返回真/假）
has_war = yes
has_stability < 0.5
NOT = { country_exists = SOV }
OR = { has_idea = A  has_idea = B }

# 效果（执行动作）
add_political_power = 100
set_global_flag = my_flag
annex_country = { target = SOV transfer_troops = no }

# 修正（持续加成，放在 modifier = { } 内）
stability_weekly = 0.05
research_speed_factor = 0.1
army_attack_factor = 0.2
```

### 1.4 逻辑控制

```text
# 条件分支
if = {
    limit = { has_war = yes }
    add_stability = 0.1
}
else_if = { limit = { has_war = no }  add_war_support = 0.1 }
else = { add_political_power = 50 }

# 概率效果（权重总和不必为 100）
random_list = {
    70 = { add_political_power = 200 }
    30 = { add_political_power = -100 }
}

# 循环（效果类，从 0 到 24，步进 1）
for_loop_effect = { start = 0  end = 24  add = 1  ... }
```

### 1.5 变量系统

```text
set_variable = { var = my_var  value = 10 }
set_temp_variable = { var = temp  value = 5 }   # 临时变量
add_to_variable = { var = my_var  value = 3 }
subtract_from_variable = { var = my_var  value = 1 }
multiply_variable = { var = my_var  value = 2 }
divide_variable = { var = my_var  value = 2 }
clamp_variable = { var = my_var  min = 0  max = 100 }
check_variable = { my_var > 50 }                 # 触发器比较变量
```

### 1.6 AI 权重（ai_will_do / ai_chance）

决定 AI 选择某个国策、决议或事件选项的倾向：

```text
ai_will_do = {
    factor = 10                        # 基础权重
    modifier = {
        factor = 50                    # 满足条件时的权重
        has_war = yes
    }
}
```

> **注意**：`modifier` 内若放国家作用域条件，一个 `modifier` 只能放一个国家的 tag；多个国家需拆成多个 `modifier` 块，否则无效。

---

## 二、Mod 工程结构

### 2.1 描述文件

HOI4 的 Mod 需要两个描述文件：

| 文件 | 位置 | 作用 |
|------|------|------|
| `xxx.mod` | 用户 `Documents/Paradox Interactive/Hearts of Iron IV/mod/` | 让启动器识别 Mod，含路径指向 |
| `descriptor.mod` | Mod 内容目录内 | 内容目录自身的描述，打包上传时用 |

`.mod` 文件示例：

```text
name = "My Mod"
path = "mod/my_mod"
tags = { "Alternative History" }
picture = "thumbnail.png"
supported_version = "1.14.*"
```

常见字段：`name`（名称）、`path`（相对 mod 目录的路径）、`tags`（标签）、`picture`（缩略图）、`supported_version`（支持版本）、`remote_file_id`（Steam 工坊 ID）、`replace_path`（整目录替换原版，常用于完全重写国策/地图）。

### 2.2 内容目录结构

Mod 内容目录镜像游戏目录，只放**要覆盖/新增**的文件：

```
my_mod/
├── descriptor.mod
├── common/           # 数据定义（覆盖原版 common/ 下同名文件）
├── events/           # 事件
├── history/          # 开局历史
├── localisation/     # 本地化
├── interface/        # .gfx 精灵定义
├── gfx/              # 图像资源
└── ...
```

> **覆盖规则**：Mod 中与游戏同路径的文件会**覆盖/合并**原版；不同文件名则作为新增内容加载。`replace_path` 可强制整个目录只读 Mod 版本。

---

## 三、游戏目录总览

游戏根目录（`Hearts of Iron IV/`）主要顶层目录：

| 目录 | 内容 | 说明 |
|------|------|------|
| `common/` | 数据定义（核心） | 78 个子目录，涵盖国策/科技/装备/决议等 |
| `events/` | 事件文件 | 国家事件、新闻事件 |
| `history/` | 开局历史 | `countries/` `states/` `units/` `general/` |
| `localisation/` | 本地化文本 | `.yml` 文件，语言分目录 |
| `map/` | 地图数据 | `strategicregions/` `supplyareas/` `terrain/` |
| `gfx/` | 图像资源 | 头像、图标、兵牌等 |
| `interface/` | 界面资源 | `.gfx` 精灵定义、`.gui` 界面脚本 |
| `music/` | 音乐 | |
| `sound/` | 音效 | |
| `dlc/` | DLC 内容 | |
| `defines.lua` | 全局常量 | Lua 格式的游戏底层数值 |

---

## 四、国家与意识形态

### 4.1 国家定义 `common/country_tags/`

**作用**：把国家 tag 映射到具体的国家定义文件。文件名常为 `00_countries.txt` 等。

```text
# common/country_tags/00_countries.txt
GER = "countries/Germany.txt"
SOV = "countries/Soviet.txt"
USA = "countries/United States.txt"
```

**规则**：`TAG = "countries/文件名.txt"`，路径相对于 `common/`。新增国家时，文件名为裸 tag（如 `14K.txt`）。

### 4.2 国家颜色与属性 `common/countries/`

**作用**：定义国家颜色（地图着色）、图形文化、颜色 ui 等。

```text
# common/countries/Germany.txt
color = { 73 89 108 }
graphical_culture = european_gfx
graphical_culture_2d = european_2d
color_ui = { 73 89 108 }
```

| 字段 | 含义 |
|------|------|
| `color` | 地图上的国家颜色（RGB 0-255） |
| `color_ui` | UI 界面颜色 |
| `graphical_culture` | 3D 兵模文化 |
| `graphical_culture_2d` | 2D 兵牌文化 |

### 4.3 意识形态 `common/ideologies/`

**作用**：定义四种基础意识形态（民主/法西斯/共产/中立）及其子意识形态、规则设定。

```text
ideologies = {
    democratic = {
        color = { 65 70 240 }
        types = {
            conservatism = { color = { 60 60 160 } }
            liberalism = { color = { 60 100 180 } }
        }
        rules = {
            can_create_factions = yes
            can_send_volunteers = yes
            ...
        }
    }
    fascism = { ... }
    communism = { ... }
    neutrality = { ... }
}
```

**关键**：意识形态种类英文固定为 `democratic / fascism / communism / neutrality`；子意识形态（如 `nazism`、`leninism`、`liberalism`）定义在 `types` 内。

---

## 五、国策与持续国策

### 5.1 国策树 `common/national_focus/`

**作用**：定义国家的国策树（National Focus Tree），是 Mod 制作最核心的文件之一。文件位置 `common/national_focus/xxx.txt`。

**整体结构**：一个文件通常包含 `focus_tree = { ... }` 顶层块，内有 `id`、`country`（分配权重）、`continuous_focus_position` 和若干 `focus = { }` 节点。

```text
focus_tree = {
    id = ger_focus_tree
    country = {
        factor = 0                    # 默认权重
        modifier = {
            add = 10                  # GER 使用此树的权重
            tag = GER
        }
    }
    default = no
    continuous_focus_position = { x = 1200 y = 100 }

    focus = {
        id = GER_rearmament
        icon = GFX_goal_generic_construct_military
        x = 5
        y = 0
        cost = 10                      # 每 1 点 = 7 天，10 = 70 天
        available = { ... }            # 可点条件
        bypass = { ... }               # 跳过条件
        completion_reward = { ... }    # 完成效果
    }
}
```

**国策节点关键字段**：

| 字段 | 含义 |
|------|------|
| `id` | 国策唯一 ID（用于引用/本地化） |
| `icon` | 图标 sprite 名（在 `.gfx` 中定义） |
| `x` / `y` | 绝对坐标 |
| `relative_position_id` | 相对定位：以另一国策为原点 |
| `prerequisite = { focus = A }` | 前置国策（多个 prerequisite 块=需全部完成，实线；单个块内多个 focus=完成任一即可，虚线） |
| `mutually_exclusive = { focus = B }` | 互斥国策 |
| `cost` | 所需天数（1 = 7 天） |
| `available` | 可点击条件 |
| `bypass` | 自动跳过条件 |
| `available_if_capitulated` | 投降后是否可点 |
| `cancel_if_invalid` / `continue_if_invalid` | 条件失效时取消/继续 |
| `will_lead_to_war_with` | 提示将向某国宣战 |
| `allow_branch` | 满足条件才显示此分支（配合 `mark_focus_tree_layout_dirty = yes`） |
| `search_filters` | 国策搜索筛选标签 |
| `completion_reward` | 完成时效果 |
| `ai_will_do` | AI 选择倾向 |

**搜索筛选标签**（`search_filters`）常用值：

```text
FOCUS_FILTER_POLITICAL     政治
FOCUS_FILTER_RESEARCH      科研
FOCUS_FILTER_INDUSTRY      工业
FOCUS_FILTER_STABILITY     稳定度
FOCUS_FILTER_WAR_SUPPORT   战争支持度
FOCUS_FILTER_MANPOWER      人力
FOCUS_FILTER_ANNEXATION    吞并
```

**共享国策**：`shared_focus = { ... }` 定义不需要国策树直接生效的国策；`shared_focus = 国策id` 调用共享国策。

### 5.2 持续国策 `common/continuous_focus/`

**作用**：定义底部持续执行的国策（如"建筑工业"）。结构与普通国策类似，通过 `continuous_focus_position` 定位。

---

## 六、事件与决议

### 6.1 事件 `events/`

**作用**：定义游戏运行时弹出的事件。位置在顶层 `events/` 目录（注意不是 common 内）。

**结构**：顶层 `add_namespace = xxx` 定义命名空间，事件 ID 为 `xxx.N`。

```text
add_namespace = my_mod

country_event = {                      # 国家事件
    id = my_mod.1
    title = my_mod.1.t
    desc = my_mod.1.d
    picture = GFX_report_event_generic
    is_triggered_only = yes            # 仅供调出（不会自动触发）
    fire_only_once = yes               # 仅触发一次
    hidden = yes                       # 隐藏弹出（配合 immediate）
    trigger = { ... }                  # 自动触发条件
    immediate = { ... }                # 事件弹出同时立即执行的效果
    option = {                         # 事件选项
        name = my_mod.1.a
        ai_chance = { factor = 100  modifier = { factor = 0  has_war = yes } }
        add_political_power = 100
    }
}
```

**关键字段**：

| 字段 | 含义 |
|------|------|
| `id` | 事件 ID（`命名空间.编号`） |
| `title` / `desc` | 标题/描述（本地化键） |
| `picture` | 事件图片 |
| `is_triggered_only` | 只被其他效果调出，不自动触发 |
| `fire_only_once` | 全局只触发一次 |
| `hidden` | 隐藏（不显示弹窗，常配合 immediate） |
| `trigger` | 自动触发条件 |
| `mean_time_to_happen` | 平均触发时间（MTTH，随机触发用） |
| `immediate` | 弹出瞬间执行 |
| `option` | 选项（可多个），`ai_chance` 控制 AI 选择 |

**其他事件类型**：`news_event`（新闻事件，供 `news_event = { id = ... }` 调出）、`super_event`（超事件）。

**调出事件**：

```text
country_event = { id = my_mod.1 days = 3 random = 12 }   # 3 天后 ±12 小时弹出
news_event = { id = my_mod.2 }
```

### 6.2 决议 `common/decisions/`

**作用**：定义玩家可主动点击的决议（Decision）。

**分类文件**：`common/decisions/categories/xxx_decision_category.txt`

```text
XX_decision_category = {
    icon = 图标名
    picture = 图片名
    allowed/available/visible = { ... }
    priority = 优先度              # 越高越靠上
}
```

**单个决议**：`common/decisions/xxx.txt`

```text
XX_decision_category = {             # 分类块
    my_decision = {                  # 单个决议
        icon = GFX_decision_xxx
        cost = 50                    # 政治点消耗
        days_remove = 30             # 决议持续天数
        days_re_enable = 60          # 完成后多少天可再点
        fire_only_once = yes         # 只能点一次
        allowed/available/visible = { ... }
        ai_will_do = { factor = 1 }
        modifier = { ... }           # 决议进行中的持续修正
        complete_effect = { ... }    # 点击时效果
        remove_effect = { ... }      # 移除时效果
    }

    my_mission = {                   # 任务模板（mission）
        activation = { ... }         # 触发任务的条件
        available = { ... }          # 完成任务的条件
        is_good = yes                # 进度条颜色
        days_mission_timeout = 30    # 任务时限
        timeout_effect = { ... }     # 超时效果
        complete_effect = { ... }    # 完成效果
    }
}
```

**visible / allowed / available 区别**：

| 字段 | 检测时机 | 用途 |
|------|----------|------|
| `visible` | 随时检测 | 是否显示该决议 |
| `allowed` | 仅开局检测 | 是否可用（显示灰） |
| `available` | 随时检测 | 是否可点击执行 |

---

## 七、民族精神与理念

### 7.1 民族精神 / 法案 / 内阁 `common/ideas/`

**作用**：定义民族精神（National Spirit）、法案（Law）、内阁（Political Advisor）等。文件位置 `common/ideas/`。

**结构**：顶层 `ideas = { ... }`，可含分组。

```text
ideas = {
    my_idea = {
        allowed = { tag = GER }       # 使用条件
        modifier = {
            stability_weekly = 0.01
            research_speed_factor = 0.05
        }
        ai_will_do = { factor = 10 }
        removal_cost = -1             # 移除消耗（-1 = 不可移除）
    }
}
```

**法案（Law）** 需加 `law = yes` 与 `use_list_view = yes`：

```text
ideas = {
    conscription_law = {              # 兵役法案分组
        law = yes
        use_list_view = yes
        volunteer_only = {            # 志愿兵役
            removal_cost = -1
            modifier = { ... }
        }
        limited_conscription = { ... } # 有限征兵
    }
}
```

> **注意**：法案需在 `common/idea_tags`（或 `00_idea`）中注册 `slot = 法案名字`，否则不生效。

**隐藏民族精神**：把 `ideas` 替换为 `hidden_ideas`，下面放民族精神即可。

---

## 八、角色与国家领袖

### 8.1 角色 `common/characters/`

**作用**：新版人物系统，统一管理领导人、顾问、将领（元帅/将军/海军将领）。文件位置 `common/characters/xxx.txt`。

```text
characters = {
    DOH_leader = {
        name = DOH_leader_name          # 本地化词条
        portraits = {
            army = {
                large = "gfx/leaders/DOH/DOH.png"
                small = "gfx/interface/ideas/DOH.png"
            }
            civilian = {
                large = "gfx/leaders/DOH/DOH.png"
                small = "gfx/interface/ideas/DOH.png"
            }
        }
        country_leader = {              # 国家领导人角色
            ideology = nazism
            traits = { ... }
            expire = "1965.1.1.1"
        }
        advisor = {                     # 内阁角色
            slot = political_advisor    # 或 army_chief/high_command 等
            idea_token = DOH_advisor    # 用于 has_idea 检测
            traits = { ... }
            ai_will_do = { factor = 1 }
        }
        field_marshal = {               # 元帅（corps_commander / navy_leader 同理）
            legacy_id = 10001
            traits = { ... }
            skill = 5
            attack_skill = 4
            defense_skill = 3
            planning_skill = 4
            logistics_skill = 5
        }
    }
}
```

**角色槽位（advisor slot）**：`political_advisor`（政治顾问）、`theorist`（理论家）、`army_chief`（陆军部长）、`navy_chief`（海军部长）、`air_chief`（空军部长）、`high_command`（总司令）等。

### 8.2 国家领袖特质 `common/country_leader/`（旧版特质）

旧版特质文件（`common/country_leader/xx_traits.txt`）定义内阁/领袖特质，直接写 modifier 与 equipment_bonus：

```text
silent_workhorse = {
    random = no
    political_power_factor = 0.15
    research_bonus = { industry = 0.1 }
    ai_will_do = { factor = 10 }
}
```

---

## 九、科技与装备

### 9.1 科技 `common/technologies/`

**作用**：定义科技树（Technology）。文件位置 `common/technologies/xxx.txt`。

```text
tech = {                              # 顶层块
    folder = {                        # 科技文件夹（分组）
        name = infantry_folder
        position = { x = 0 y = 0 }
    }
    infantry_weapons = {              # 单个科技
        research_cost = 100
        start_year = 1936
        categories = { infantry_weapons }
        ai_will_do = { factor = 1 }
        path = {
            research_cost_coeff = 1
            leads_to_tech = infantry_weapons1   # 通往的科技
        }
        enable_equipments = { ... }           # 解锁的装备
        on_research_complete = { ... }        # 研究完成效果
    }
}
```

**关键字段**：`research_cost`（研究花费）、`start_year`（起始年份）、`categories`（科技类别，用于 research_bonus 加成）、`path`（科技树连线）、`folder`（分组定位）、`enable_equipments` / `enable_equipment_modules`（解锁装备/模块）、`on_research_complete`（完成效果）。

### 9.2 装备 `common/units/equipment/`

**作用**：定义装备原型（步兵武器、坦克、飞机、舰船等）及其属性。文件位置 `common/units/equipment/xxx.txt`。

```text
equipments = {
    infantry_equipment = {
        type = { infantry }
        reliability = 0.9
        defense = 20
        soft_attack = 3
        ...
    }
}
```

> 装备设计器（坦克/飞机/舰艇）依赖 `chassis/airframe/ship_hull` 及模块定义，设计变体写回 `history/countries/` 中的 `create_equipment_variant`（见 12.1）。

**装备变体**（在历史文件中创建）：

```text
create_equipment_variant = {
    name = "Light Tank Mk. IV"
    type = light_tank_chassis_1
    parent_version = 0
    modules = {
        main_armament_slot = tank_heavy_machine_gun
        turret_type_slot = tank_light_one_man_tank_turret
        suspension_type_slot = tank_christie_suspension
        armor_type_slot = tank_riveted_armor
        engine_type_slot = tank_gasoline_engine
    }
    upgrades = {
        tank_nsb_engine_upgrade = 2
        tank_nsb_armor_upgrade = 1
    }
    icon = "GFX_ENG_basic_light_tank_medium"
    obsolete = yes
}
```

---

## 十、兵种与师编制

### 10.1 兵种 `common/units/`

**作用**：定义营级单位（步兵、装甲、炮兵、支援连等）。文件位置 `common/units/xxx.txt`。

```text
sub_units = {
    my_unit = {
        sprite = infantry
        map_icon_category = infantry
        active = yes / no               # 是否开局可用（no 需科技激活）
        cavalry = no
        special_forces = no
        marines = no
        mountaineers = no
        can_be_parachuted = no
        group = infantry                # infantry/mobile/armor/support
        categories = { category_army category_all_infantry }
        combat_width = 2
        max_strength = 25
        max_organisation = 70
        default_morale = 0.3
        manpower = 300
        training_time = 120
        suppression = 5.0
        weight = 0.5
        supply_consumption = 0.20
        need = { infantry_equipment = 100 }
        # 地形加成
        mountain = { attack = 0.05 defence = 0.05 movement = 0.05 }
        forest = { movement = -0.2 }
        urban = { attack = 0.25 }
    }
}
```

### 10.2 师编制模板 `division_template`

师编制模板通常在历史文件或 OOB 中定义，格式如下：

```text
division_template = {
    name = "皇家步兵师"
    is_locked = yes
    priority = 2
    regiments = {
        infantry = { x = 0 y = 0 }
    }
    support = {
        mot_recon = { x = 0 y = 0 }
    }
}
```

---

## 十一、建筑与资源

### 11.1 建筑 `common/buildings/`

**作用**：定义建筑类型（军工、民工、基建、要塞、港口等）。文件位置 `common/buildings/xxx.txt`。

```text
buildings = {
    arms_factory = {
        show_on_map = 6
        base_cost = 6000
        base_cost_conversion = 3000
        military_production = 1
        icon_frame = 2
        max_level = 20
        shares_slots = yes
        value = 8
        infrastructure_construction_effect = yes
    }
}
```

**关键字段**：

| 字段 | 含义 |
|------|------|
| `base_cost` | 基础建造花费 |
| `per_level_extra_cost` | 每级增加花费 |
| `max_level` | 建造上限 |
| `shares_slots` | 是否与其他建筑共享地区槽位 |
| `provincial` | 是否只能在省级（小地块）建造 |
| `icon_frame` | GFX_buildings_strip 精灵中的帧号 |
| `value` | 空袭基础生命值 / 和平会议价值 |
| `infrastructure_construction_effect` | 是否受基建等级建造速度修正 |
| `military_production` / `general_production` / `naval_production` | 视为军/民/船坞工厂 |
| `infrastructure` / `air_base` / `is_port` / `land_fort` / `naval_fort` / `radar` / `nuclear_reactor` | 功能标记 |
| `allied_build` | 盟军共享修正 |
| `disabled_in_dmz` | 非军事区禁用 |
| `damage_factor` | 空袭/陆战受伤系数 |

### 11.2 资源 `common/resources/`

**作用**：定义战略资源（钢/油/钨/铬/铝/橡胶）的基础属性。

```text
resources = {
    steel = { icon = 0 }
    oil = { icon = 1 }
    tungsten = { icon = 2 }
    chromium = { icon = 3 }
    aluminium = { icon = 4 }
    rubber = { icon = 5 }
}
```

---

## 十二、历史档：国家 / 州 / 初始部队

### 12.1 国家历史 `history/countries/`

**作用**：定义国家开局状态。**本质是开局时生效的 effect 合集**，可写任意效果（宣战、加政治点等）。文件位置 `history/countries/`。

**文件名严格规则**：`国家TAG - 国家名.txt`，如原版德国为 `GER - Germany.txt`（TAG 来自 country_tags，国家名来自 countries 文件名）。

```text
capital = 64                        # 首都省份 ID
oob = "GER_1936"                    # 陆军初始部队文件名
if = {
    limit = { has_dlc = "Man the Guns" }
    set_naval_oob = "GER_1936_navy" # 有 DLC 时的海军
    else = { set_naval_oob = "GER_1936_navy_legacy" }
}
set_research_slots = 4
set_stability = 0.6
set_war_support = 0.5
set_technology = {
    infantry_weapons = 1
    support_weapons = 1
}
set_convoys = 300
set_politics = {
    ruling_party = fascism
    last_election = "1933.3.5"
    election_frequency = 48
    elections_allowed = no
}
set_popularities = {
    democratic = 5
    fascism = 90
    communism = 3
    neutrality = 2
}
add_ideas = { free_trade }          # 民族精神/内阁/法案
set_autonomy = {
    target = SLO
    autonomous_state = autonomy_puppet
}
```

### 12.2 州 / 地块 `history/states/`

**作用**：定义每个州（State）的开局归属、核心、建筑、人口、资源等。文件位置 `history/states/`。

```text
state = {
    id = 64
    name = "STATE_64"
    history = {
        owner = GER
        buildings = {
            infrastructure = 3
            industrial_complex = 4
            arms_factory = 2
        }
        resources = {
            steel = 12
            oil = 3
        }
    }
    provinces = { 1234 1235 1236 }   # 所属省份 ID
}
```

### 12.3 初始部队 OOB `history/units/`

**作用**：定义开局部署的军队（Order of Battle）。文件位置 `history/units/`。

```text
# 由 history/countries 中的 oob = "文件名" 载入
units = {
    # 师编制定义
    division_template = {
        name = "步兵师"
        regiments = { infantry = { x = 0 y = 0 } }
        support = { artillery = { x = 0 y = 0 } }
    }
    # 部队部署
    division = {
        name = "第一步兵师"
        division_template = "步兵师"
        start_experience_factor = 0.5
        start_equipment_factor = 0.5
    }
}
```

### 12.4 顾问分配 `history/general/`

**作用**：开局已启用的顾问/内阁分配。文件位置 `history/general/`。

---

## 十三、剧本（Bookmarks）

**作用**：定义游戏开始日期与各剧本（1936/1939 等）。文件位置 `common/bookmarks/`。

```text
bookmarks = {
    bookmark = {
        name = "example_bookmark_1"
        desc = "example_bookmark_1_desc"
        date = 1936.1.1.12           # 年.月.日.小时（伦敦时间）
        picture = GFX_select_date_1936
        default = yes                # 是否默认剧本
        default_country = GER        # 默认选择国家
        "GER" = {                    # 国家条目（必须英文双引号）
            history = GER_SCENARIO_DESC
            ideology = fascism
            available = { ... }      # 显示条件（常做 DLC 检查）
            ideas = { ... }          # 展示的民族精神（最多 3 个）
            focuses = { ... }        # 展示的国策（最多 3 个）
            minor = yes              # 次要国家（下方小旗显示）
        }
        "---" = {                    # 其他国家通用描述
            history = OTHER_SCENARIO_DESC
        }
        effect = {
            randomize_weather = 1228 # 必须写：随机天气
        }
    }
}
```

---

## 十四、外交与政治机制

### 14.1 关系修正 `common/opinion_modifiers/`

**作用**：定义外交关系修正（改善关系、威胁等）。文件位置 `common/opinion_modifiers/xxx_opinion_modifiers.txt`。

```text
opinion_modifiers = {
    my_modifier = {
        value = 20                   # 修正数值（正加负减）
        max_trust = 100
        min_trust = -100
        trade = no                   # 是否贸易关系修正
        days = 10                    # 配合 decay 使用
        decay = 1                    # 每 days 天减少的关系
    }
}
```

**用法**（在国策/事件/决议效果中）：

```text
add_opinion_modifier = { target = TAG modifier = my_modifier }
remove_opinion_modifier = { target = TAG modifier = my_modifier }
reverse_add_opinion_modifier = { target = TAG modifier = my_modifier }
```

### 14.2 战争目标 `common/wargoals/`

**作用**：定义战争目标类型（征服、傀儡、吞并等）。

### 14.3 占领法 `common/occupation_laws/`

**作用**：定义占领政策（温和/严厉等），影响抵抗与顺从度。

### 14.4 自治状态 `common/autonomous_states/`

**作用**：定义附庸等级（殖民地/自治领/傀儡/半吞傀儡）及其规则。

```text
autonomy_free        自由
autonomy_colony      殖民地
autonomy_dominion    自治领
autonomy_puppet      傀儡
autonomy_integrated_puppet  半吞傀儡
```

### 14.5 力量平衡 `common/bop/`

**作用**：定义力量平衡（Balance of Power）机制。含 `initial_value`、`left_side`、`right_side`、`decision_category`、`range`、`side` 等，并关联决议动作。

### 14.6 派系 `common/factions/`

**作用**：定义阵营（同盟国、轴心国、共产国际等）及加入规则。

### 14.7 和平会议 `common/peace_conference/`

**作用**：定义和平会议的 AI 行为与规则。

---

## 十五、AI 体系

AI 相关文件统一放在 `common/` 下的多个子目录，控制 AI 国家的战略、生产、科研、部队等行为。

### 15.1 AI 战略倾向 `common/ai_strategy/`

**作用**：定义 AI 的外交/军事/生产战略策略组。核心是 `ai_strategy = { type = ... id = ... value = ... }`。

```text
my_strategy = {
    enable = { ... }            # 开启条件（可无限重复）
    abort = { ... }             # 终止条件
    abort_when_not_enabled = yes
    ai_strategy = {
        type = unit_ratio       # 战略种类
        id = infantry           # 执行对象
        value = 100             # 倾向值
    }
}
```

**常用战略种类（type）**：

| type | 含义 |
|------|------|
| `alliance` | 与某国结盟（id 需引号） |
| `antagonize` / `befriend` / `contain` / `protect` / `support` | 外交态度 |
| `declare_war` | 有战争目标即宣战 |
| `conquer` | 鼓励征服某国 |
| `prepare_for_war` | 对某国准备战争 |
| `invade` | 海上入侵 |
| `send_volunteers_desire` / `send_lend_lease_desire` | 派遣意愿 |
| `unit_ratio` | 兵种训练比率 |
| `role_ratio` | 模板角色训练倾向 |
| `template_prio` | 兵种模板训练倾向 |
| `equipment_production_factor` | 装备生产倾向 |
| `equipment_stockpile_surplus_ratio` | 装备储备比例 |
| `building_target` | 建筑倾向 |
| `force_build_armies` | 强制造兵 |
| `spare_unit_factor` | 备用部队 |
| `front_control` / `front_unit_request` | 前线控制/部署 |
| `garrison` / `garrison_reinforcement_priority` | 驻军 |
| `research_tech` | 指定研究科技 |
| `occupation_policy` | 占领策略 |

### 15.2 AI 战略计划 `common/ai_strategy_plans/`

**作用**：定义 AI 的国策选择顺序（计划），含 `name`/`desc`、国策顺序列表、高级块（allowed/enable/abort/ai_will_do）。

### 15.3 AI 师模板 `common/ai_templates/`

**作用**：定义 AI 使用的师编制模板与角色。核心字段 `available_for`（适用国家）、`roles`（角色，供 `role_ratio` 引用）、`target_width`（战斗宽度）、`target_template`（目标编制）、`allowed_types`（允许兵种）。

### 15.4 AI 装备 `common/ai_equipment/`

**作用**：定义 AI 使用的装备设计（飞机/坦克/舰艇变体），含 `category`、`history`、`allowed_modules`、`priority`、`target_variant`。

### 15.5 AI 科研权重 `common/ai_focuses/`

**作用**：定义 AI 对各科技的研究权重（`research` 键值表）。

### 15.6 AI 海军 `common/ai_navy/`

**作用**：定义 AI 海军目标/舰队/特遣队。

### 15.7 AI 区域 `common/ai_areas/`

**作用**：定义 AI 区域（供 `area_priority` 等战略引用），含 `strategic_regions` 列表。

### 15.8 AI 派系战区 `common/ai_faction_theaters/`

**作用**：定义 AI 派系战区，含 `name`/`regions`/`preferred_countries`。

---

## 十六、脚本化机制

### 16.1 脚本化效果与触发 `common/scripted_effects/`、`common/scripted_triggers/`

**作用**：将常用的效果/触发器封装为可复用"函数"，在其他文件中调用。

```text
# common/scripted_effects/my_effects.txt
my_effect = { add_stability = 0.1 }
my_effect_with_arg = { add_political_power = $AMOUNT$ }   # 参数

# 调用
my_effect = yes
```

### 16.2 脚本化本地化 `common/scripted_localisation/`

**作用**：动态生成本地化文本，通过 `$KEY$` 引用。

### 16.3 触发动作 `common/on_actions/`

**作用**：定义全局事件钩子（如 `on_startup`、`on_declare_war`、`on_annex`），在特定时机自动执行效果。

```text
on_actions = {
    on_startup = {
        effect = { ... }
    }
    on_declare_war = {
        effect = { ... }
    }
}
```

### 16.4 动态修正 `common/dynamic_modifiers/`

**作用**：定义动态修正（可随时间/条件变化的修正）。

### 16.5 修正定义与类型 `common/modifier_definitions/`、`common/modifiers/`

**作用**：定义自定义修正量的显示与类型。

### 16.6 游戏规则 / 难度 / 限时活动

| 目录 | 作用 |
|------|------|
| `common/game_rules/` | 游戏规则（开局可选） |
| `common/difficulty_settings/` | 难度设置 |
| `common/timed_activities/` | 限时活动 |

### 16.7 其他机制目录

| 目录 | 作用 |
|------|------|
| `common/military_industrial_organization/` | MIO（军事工业组织） |
| `common/special_projects/` | 特殊计划 |
| `common/abilities/` | 特种作战能力 |
| `common/operations/` | 情报行动 |
| `common/raids/` | 突袭 |
| `common/intelligence_agencies/` | 情报机构 |
| `common/intelligence_agency_upgrades/` | 情报机构升级 |
| `common/medals/` | 勋章 |
| `common/doctrines/` | 军事学说 |
| `common/names/` | 命名列表 |
| `common/defines/` | 游戏底层定义（`.lua`） |

---

## 十七、本地化与界面资源

### 17.1 本地化 `localisation/`

**作用**：所有显示文本的中英翻译。按语言分目录（`english/`、`simp_chinese/` 等），`.yml` 文件。

```yaml
l_english:
  my_focus:0 "My Focus"
  my_focus_desc:0 "This is a focus."
  my_event.1.t:0 "Event Title"
  my_event.1.d:0 "Event description."
  my_event.1.a:0 "Option A"
```

**格式规则**：
- 键后加 `:0` 版本号，再跟字符串。
- **HOI4 本地化文件需 UTF-8 BOM 编码**（`utf-8-sig`），否则中文可能乱码。
- 特殊字符需转义（如 `$`、`£`、`§` 是图标/格式 token）。

**常用 token**：

| token | 含义 |
|-------|------|
| `$KEY$` | 引用另一本地化键 |
| `£KEY` | 图标 token |
| `§R` / `§G` / `§Y` | 颜色代码（红/绿/黄） |
| `\n` | 换行 |

### 17.2 图形定义 `interface/*.gfx`

**作用**：定义精灵（sprite），把图像文件注册为游戏可引用的图标名。

```text
spriteTypes = {
    spriteType = {
        name = "GFX_goal_generic_construct_military"
        texturefile = "gfx/interface/goals/GFX_goal_generic_construct_military.dds"
        noOfFrames = 1
    }
}
```

**命名约定**：
- 国策图标：`GFX_<国策id>` 或任意名
- 科技图标：`GFX_<科技id>_medium`（引擎按 sprite 名约定解析）
- 单位图标：`GFX_unit_<单位名>_icon_medium` / `_icon_small`

### 17.3 界面脚本 `interface/*.gui`

**作用**：定义游戏界面元素（按钮、窗口等）的布局。

---

## 十八、编写规范与常见错误

### 18.1 编码与格式契约

| 项目 | 规范 |
|------|------|
| 脚本文件编码 | UTF-8 无 BOM，LF 换行 |
| 本地化 `.yml` | UTF-8 **带 BOM**（`utf-8-sig`） |
| 注释 | `#` 到行尾 |
| 字符串 | 含空格/特殊字符必须加英文双引号 |

### 18.2 常见错误

1. **括号不配对**：缺 `}` 或多 `}`，是 Mod 最常见的崩溃/加载失败原因。
2. **引号错误**：中文引号、未闭合引号。
3. **ID 重复**：国策/科技 ID 跨文件重复会冲突。
4. **本地化缺失**：实体（国策/事件/决议/理念等）无对应词条，游戏内显示为 key。
5. **前置国策悬空**：`prerequisite` 引用了不存在的国策。
6. **sprite 未注册**：图标名在 `.gfx` 中无定义，或贴图路径不存在。
7. **编码问题**：本地化文件缺 BOM 导致中文乱码。

### 18.3 引用完整性检查清单

发布前应检查：

- `.mod` 描述文件存在，`path` 指向的目录存在，有 `name` 字段
- 全部脚本文件为合法 UTF-8、括号配对
- `interface/*.gfx` 定义的 sprite 贴图存在（mod 内或游戏本体回退）
- 国策 prerequisite 无悬空引用
- 科技图标 sprite 已注册
- 本地化词条完整（mod + 游戏均无时报告）
- 国策/科技 ID 无跨文件重复

---

## 十九、速查附录

### 19.1 常用触发器（国家作用域）

| 触发器 | 含义 |
|--------|------|
| `has_political_power > 100` | 政治点 |
| `has_manpower > 1000000` | 人力 |
| `country_exists = SOV` | 国家存在 |
| `is_in_faction_with = SOV` | 同一阵营 |
| `is_subject_of = SOV` | 是某国附庸 |
| `is_subject = yes` | 是附庸 |
| `has_stability < 0.4` | 稳定度 |
| `has_war_support = 2.0` | 战争支持度 |
| `is_ai = yes` | 是否 AI |
| `original_tag = SOV` | 原始 tag |
| `has_country_leader = { ruling_only = yes name = "..." }` | 国家领袖 |
| `has_global_flag` / `has_country_flag` | 全局/国家标记 |
| `date > 1937.7.7` | 日期 |
| `tag = SOV` | 国家 tag |
| `has_government = communism` | 执政意识形态 |
| `communism > 0.2` | 意识形态支持率 |
| `is_historical_focus_on = yes` | 历史国策 |
| `surrender_progress > 0.5` | 投降倾向 |
| `has_war_with = SOV` | 与某国交战 |
| `has_civil_war = yes` | 内战 |
| `has_war = yes` | 处于战争 |
| `has_capitulated = yes` | 已投降 |
| `has_opinion = { target = SOV value > 59 }` | 对某国关系 |
| `capital_scope = { state = 123 }` | 首都位置 |
| `controls_state = 123` | 控制某州 |
| `owns_state = 123` | 拥有某州 |
| `has_full_control_of_state = 123` | 完全控制某州 |
| `threat > 0.1` | 全球紧张度 |
| `has_completed_focus = SOV_focus1` | 已完成国策 |
| `focus_progress = { focus = A progress > 0.5 }` | 国策进度 |
| `num_of_factories > 10` | 工厂总数 |
| `industrial_complex > 10` | 民用工厂 |
| `arms_factory > 10` | 军用工厂 |
| `dockyard > 5` | 海军船坞 |
| `has_idea = SOV_idea1` | 拥有民族精神 |
| `has_tech = infantry_weapons_1` | 拥有科技 |
| `has_army_manpower = { size > 1000000 }` | 陆军现役兵力 |
| `has_deployed_air_force_size = { size > 200 }` | 部署飞机数 |

### 19.2 常用触发器（州作用域）

| 触发器 | 含义 |
|--------|------|
| `is_owned_and_controlled_by = SOV` | 拥有且控制 |
| `is_controlled_by = SOV` | 被控制 |
| `is_owned_by = SOV` | 被拥有 |
| `is_claimed_by = SOV` | 被宣称 |
| `is_core_of = SOV` | 是核心 |
| `is_capital = yes` | 是首都 |
| `is_demilitarized_zone = yes` | 是非军事区 |
| `has_state_category = rural` | 州类别 |
| `compliance > 50` | 顺从度 |
| `resistance > 50` | 抵抗 |
| `free_building_slots = { building = arms_factory size > 10 }` | 剩余建筑槽 |

### 19.3 常用效果（effect）

| 效果 | 含义 |
|------|------|
| `set_global_flag` / `clr_global_flag` | 设/清全局标记 |
| `set_country_flag` / `clr_country_flag` | 设/清国家标记 |
| `set_technology = { tech = 1 }` | 添加科技 |
| `add_political_power = 100` | 政治点 |
| `add_stability = 0.1` | 稳定度 |
| `add_war_support = 0.1` | 战争支持度 |
| `add_research_slot = 1` | 研究槽 |
| `army_experience` / `navy_experience` / `air_experience` | 三军经验 |
| `add_ideas = { idea }` / `remove_ideas = { idea }` | 加/移除民族精神 |
| `add_timed_idea = { idea = X days = 30 }` | 限时民族精神 |
| `add_popularity = { ideology = fascism popularity = 10 }` | 增加支持率 |
| `annex_country = { target = SOV transfer_troops = no }` | 吞并 |
| `declare_war_on = { target = SOV type = take_state }` | 宣战 |
| `create_wargoal = { target = SOV type = puppet_wargoal_focus }` | 制造战争借口 |
| `white_peace = SOV` | 无条件和平 |
| `transfer_state = 123` | 转移州所有权 |
| `add_state_claim = 123` | 获得宣称 |
| `add_state_core = 123` / `add_core_of = TAG` | 添加核心 |
| `set_capital = 64` | 设置首都 |
| `create_faction = my_faction` | 创建阵营 |
| `add_named_threat = { threat = 1 name = X }` | 添加紧张度 |
| `set_party_name = { ideology = fascism long_name = X }` | 修改政党名 |
| `load_oob = "文件名"` | 载入军队文件 |
| `set_autonomy = { target = TAG autonomy_state = autonomy_puppet }` | 设置傀儡 |
| `add_autonomy_ratio = { value = 0.1 localization = X }` | 增加自治度 |
| `add_equipment_to_stockpile = { type = infantry_equipment amount = 1000 }` | 添加装备 |
| `add_command_power = 10` | 指挥点数 |
| `country_event = { id = x days = 3 }` | 调出事件 |
| `news_event = { id = x }` | 调出新闻事件 |
| `create_corps_commander` / `create_field_marshal` / `create_navy_leader` | 创建将领 |
| `add_offsite_building = { type = arms_factory level = 5 }` | 地图外工厂 |

### 19.4 州/省份建筑与资源效果

```text
# 州建筑
123 = {
    add_extra_state_shared_building_slots = 2
    add_building_construction = { type = arms_factory level = 5 instant_build = yes }
    add_resource = { type = steel amount = 10 }
}

# 省份建筑
1234 = {
    add_building_construction = { type = naval_base level = 1 province = 1234 }
}
```

### 19.5 常用修正（modifier）速查

| 修正 | 含义 |
|------|------|
| `political_power_gain` / `political_power_factor` | 政治点（点数/百分比） |
| `stability_weekly` / `stability_factor` | 稳定度 |
| `war_support_weekly` / `war_support_factor` | 战争支持度 |
| `research_speed_factor` | 研究速度 |
| `consumer_goods_factor` | 生活消费品 |
| `industrial_capacity_factory` / `industrial_capacity_dockyard` | 工厂/船坞产出 |
| `production_speed_buildings_factor` | 建造速度 |
| `production_factory_max_efficiency_factor` | 生产效率上限 |
| `production_factory_efficiency_gain_factor` | 生产效率增长 |
| `line_change_production_efficiency_factor` | 生产效率保持 |
| `army_attack_factor` / `army_defence_factor` | 陆军攻击/防御 |
| `army_org_factor` / `army_morale_factor` | 组织度/恢复 |
| `army_speed_factor` | 陆军速度 |
| `army_infantry_attack_factor` / `army_armor_attack_factor` | 步兵/坦克攻击 |
| `breakthrough_factor` | 突破 |
| `planning_speed` / `max_planning` | 计划速度/上限 |
| `navy_org` / `naval_speed_factor` | 海军组织度/速度 |
| `navy_capital_ship_attack_factor` / `navy_screen_attack_factor` | 主力舰/屏卫舰攻击 |
| `air_attack_factor` / `air_range_factor` / `air_agility_factor` | 空军攻击/航程/机动 |
| `air_superiority_efficiency` / `air_intercept_efficiency` | 制空/拦截效率 |
| `conscription` / `conscription_factor` / `non_core_manpower` | 适役人口 |
| `monthly_population` | 月度人力 |
| `training_time_army_factor` | 训练时间 |
| `attrition` / `supply_consumption_factor` | 损耗/补给 |
| `experience_gain_army_factor` / `_navy_` / `_air_` | 三军经验增长 |
| `resistance_target` / `compliance_growth` | 抵抗/顺从度 |
| `justify_war_goal_time` | 正当化战争目标时间 |
| `surrender_limit` | 投降界限 |
| `autonomy_gain` | 自治度增长 |
| `drift_defence_factor` | 意识形态抵制 |

---

## 附：本项目编辑器覆盖说明

本项目（HOI4 模组编辑器）的 `workbench.py` 通过 `CONTENT_TYPES` 清单覆盖了游戏 `common/` 全部 78 个子目录 + 顶层目录（`events`/`history`/`interface`/`localisation`/`map`/`gfx`），共 90+ 内容类型，并为国策、科技、初始部队、力量平衡、AI 各类型、编制/舰艇/飞机/坦克设计器等提供了专用编辑器；其余类型走通用 PDX 树形编辑器。编写 mod 时可对照本文档理解各文件作用，再通过编辑器落盘（统一原子写、UTF-8 无 BOM、本地化 `.yml` 带 BOM 等契约）。
