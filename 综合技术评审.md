# HOI4 Mod 编辑器（hearts_of_iron_builder）综合技术评审

> **评审基准**：当前工作区快照（含在途改动 `B3 批二②`——Agent 偏好/审计日志等 5 个文件未提交；`git status` 非干净）。
> **评审方式**：四路并行子任务独立核查——① 架构与文档、② 代码质量深挖、③ 测试与 CI 门禁、④ API/MCP 接口与安全；每条结论带 `文件:行号` 证据。
> **执行口径**：沙箱内实跑 `tools/verify_contracts.py` 退出码 0（全绿）；因沙箱无法安装 PyQt6 wheel，UI 测试套件以代码审读 + 文档记录（"Ran 510 tests … OK"）为准，未实跑。

---

## 0. 执行摘要

**一句话结论**：这是一个工程量很大、架构意识与写入纪律明显高于普通个人项目的 HOI4 编辑器，`verify_contracts.py` 全绿、门禁真实可跑、原子写/撤销/编码契约/双版本验证不是纸上谈兵；**但当前最紧迫的不是再加功能，而是四件事**：

1. **接口层存在任意文件读写/可就地改写游戏本体的高危链路**（报告④，综合评级「高」）。
2. **mod/vanilla 覆盖顺序颠倒**，用户对兵种/船体/坦克/飞机的 mod 覆盖全部静默失效（报告②，实测复现）。
3. **核心 PDX 解析/撤销/格式化存在静默数据损坏**，且**招牌功能（国策树解析等核心模块）零测试**（报告②③）。
4. **文档与代码系统性失真**：模块数/测试数/MCP 工具数/docs 数/完成度口径/工作区状态，六处数字或状态均漂移（报告①③④都撞到同一事实）。

**整体评级**：方向正确、地基扎实、**"门禁通过"是真的，但覆盖面窄且被设计成"少报"**。按维度分档：

| 维度 | 评级 | 一句话依据 |
| --- | --- | --- |
| 架构与工程纪律 | **B**（设计 A⁻，执行 B，门禁覆盖 C） | 四层/原子写/撤销是真设计，但 `check_layer_deps` 只查 34 个模块 |
| 代码质量与正确性 | **C** | 复杂度/重复属实，但存在静默数据损坏 + 覆盖顺序颠倒 |
| 测试与 CI | **C** | 基建执行认真、内容虚胖：浅冒烟占大头、1/3 模块零覆盖、两道静态门禁接近摆设 |
| API/MCP 接口与安全 | **D**（网络层 A，接口层/原版保护高危） | 127.0.0.1+128-bit token 防住了远程，却挡不住"持 token = 本机任意文件读写" |

---

## 1. 项目画像与实测规模

**做什么**：面向《钢铁雄心 4》MOD 作者的**桌面**编辑器（非网页/非打包器），直接读写 mod 目录下 `.txt / .gfx / .yml / .mod / .csv`，覆盖 100 种内容类型，内置 PDX 解析、多源翻译、图标管理、健康检查、模板、AI 辅助创作、HTTP API 与 MCP 服务。技术栈 Python 3.14 + PyQt6（Qt 6.11）+ Pillow/numpy，Windows 与 WSL/Linux 双环境。

| 指标 | 文档声称 | 实测（当前仓库） | 状态 |
| --- | --- | --- | --- |
| `src/` 源码 | 235 个 `.py` / 63,773 行 | **252 个 `.py` / 约 68,668 行** | ❌ 落后 |
| 测试 | 86 文件 / 510 用例 | **92 文件 / 553–556 个 `test_*` 方法** | ❌ 落后 |
| `docs/` 文档 | 12 个 `.md` | **14 个 `.md`** | ❌ 落后 |
| MCP 工具数 | README=159；PROJECT_DOC/规格=168 | 代码 `build_tools()` 返回 **173**（+3 导航=176） | ❌ 落后 |
| 内容类型 | 100 种 | 100 种（`CONTENT_TYPES` 实测确认） | ✅ |
| 模板库 / 词条库 / 单位标牌 | 1,105 / 约 3,100 / 448 | —（未复核） | ✅ 未复核 |

> 小口径说明：`src` 模块数两个子任务分别数到 252 / 253（是否计入入口/`__init__` 的差异）；测试方法数分别为 553（`verify_contracts.py` 契约用例口径）与 556（`grep test_*` 口径）。差异不影响任何结论。

---

## 2. 四维评审

### 2.1 架构与工程纪律（报告①）

**设计层面：合理且可执行**
- 四层分离定义清楚（算法 ← 绘图 ← UI ← 信号槽），下层禁止反向 import 上层；判定顺序"算法 > 绘图 > UI > 信号槽"。
- 写入纪律是全项目最扎实部分：mod 文件必须走 `write_utils.atomic_write_text`（临时文件 + `os.replace`）；涉及游戏本体先 `ensure_file_in_mod` 复制到 mod 再写，绝不直写原版；写前自动登记撤销快照。
- 12 条可执行契约（编码默认 UTF-8 无 BOM/yml 例外、原子写语义、撤销、双版本兼容、测试配套等），并配套 `check_write_discipline.py` / `check_layer_deps.py` / `check_file_budget.py`，统一进 `tools/verify_contracts.py` + CI。
- 指定 WSL venv 实跑 `verify_contracts.py`：**退出码 0**，语法编译/ruff/契约单测/写入纪律扫描/四层依赖/行数预算/UI 缺口探针全部 OK。

**执行层面：通过门禁 ≠ 全库贯彻（要打折扣）**
- `check_layer_deps.py` 只检查硬编码的 **34 个**算法/绘图模块（占全部模块约 13%），大量 UI/控制器不参与，也不检查"信号槽层是否够薄""UI 层是否偷偷做算法或写文件"。
- 写入纪律扫描是 AST 白名单式：识别 `open(...,"w/a")`、`write_text`、`write_bytes`，跳过 `tests/`/`prototypes`，二进制写只提示；豁免靠人工维护的 `write_discipline_allowlist.json`（详见 2.3）。
- 大文件债务仍在：`generic_tree_editor.py`（1260 行）、`advisor_assign_dialog.py`（1254 行）、`map_canvas.py`（1251 行）被行数预算白名单保留。

### 2.2 代码质量与正确性（报告②，对 `report.html` 的独立复核）

**原报告结论哪些属实（独立 AST 复核一致）**
- 长度/复杂度：`advisor_assign_dialog.py` 1254 行 56 函数；`ship_design.load_ship_hulls` L147-269 123 行 McCabe 54 嵌套 7；`tank_design.load_tank_chassis` L131-245 115 行 McCabe 59；`plane_design.load_plane_airframes` L140-259 120 行 McCabe 58；`map_editor_dialog.py` 1189 行 32 import；`main_window.py` 1047 行 67 import、`__init__` L20-222 共 203 行；`focus_render.render_tech_tree` L50-199 150 行 8 参；`country_setup_dialog._file_matches` L159-227 69 行 McCabe 29。
- 最差函数：`api_server._route` L724-864 141 行/复杂度 76；`tree_node.from_focus_load` L137-327 191 行/复杂度 52；`tree_model.data` L145-230 86 行/复杂度 39；`oob_stats.division_stats` L207-310 104 行/复杂度 41。
- 重复：ship/tank/plane 三个 `load_*` 归一化 diff 相似度 **0.80–0.91**；`ship_design._upgrades_text` L584-590 与 `_modules_text` L593-599 仅块名不同；`focus_render.add_tech_edge` L14-30 与 `add_cross_tech_edge` L33-47 同模板。

**原报告哪些是误报（重要纠正）**
- "未处理的易出错调用"绝大多数是**假阳性**：被点名的行几乎全是 Qt 信号 `.connect(...)`、带默认值的 `dict.get(...)`/`setdefault`、受保护 `list.remove`，不是外部 I/O。
- "命名规范"指标**基本是噪声**：把 `__init__`（dunder）、`showEvent`（Qt 事件覆写必须 camelCase）、`_setup_ui`（正确的私有 snake_case）都标成问题。该指标不能用于排名。
- "最复杂函数"里有相当一部分是**死代码**：`CharacterAdvisorDialog` L545-830、`load_character_assignments` L482-540、`collect_advisor_files` L458-479 全项目无调用，复杂度排行榜被放大。

**报告②独立核实、原报告没提的新问题（重点）**

| 级别 | 问题 | 证据 |
| --- | --- | --- |
| 🔴 系统级 | **mod/vanilla 覆盖顺序颠倒**：多个 loader 写成 `for base in (mod_path, hoi4_path)` 且后写覆盖前写 → **游戏数据覆盖 mod 数据**，与注释"mod 覆盖游戏"相反。实测 `load_sub_units(mod, game)["infantry"]["abbreviation"]` 返回 `"GAME"` | `oob_loader.py:281,331`；`ship_design.py:163,239`；`tank_design.py:138,212`；`plane_design.py:152,226` |
| 🔴 核心层 | **PDX 解析静默数据损坏**：`#` 在字符串内被截断（`parse_pdx_script('name = "color #FFFFFF"')` → `{'name':'color'}`）；tokenizer 正则可静默丢 `@ : + / ( ) ,`（`@x = 5`→`x = 5`）；魔法键 `list` 冲突可 `AttributeError` 崩溃；未闭合引号/多余 `}`/跨行值被静默吞 | `pdx_parser.py:24,29,48-49,67-70,92-98` |
| 🔴 核心层 | **撤销丢 BOM/CRLF**：快照 `utf-8-sig`+`errors="ignore"` 读、默认 `utf-8` 写回；BOM 消失、CRLF 归一为 LF；且 undo 先 `pop()` 后写，失败即永久丢条目 | `undo_mgr.py:27,50,46-53` |
| 🔴 核心层 | **`read_text_contract` 检测不到坏编码**：默认 `errors="ignore"`，`UnicodeDecodeError` 分支是死代码；格式化器不忽略注释内大括号、对 `.yml` 无条件前置空行 | `write_utils.py:104-114`；`pdx_format.py:18-21,63-66,77` |
| 🔴 OOB | **保存路径写坏文件**：`save_names_group` 替换最后一组丢外层 `}`（实测 `division_names_group` 替换后 4 `{` 对 3 `}`、`}units = {` 粘行）；`OobFile.save` 会把无 `name` 字段/转义引号的模板当"已删除"删掉再重写并加 `name=""`；**装备缓存清理是 no-op**（清的是 `globals().get('_EQUIP_STATS_CACHE')`，而缓存定义在未导入的 `oob_stats.py`） | `oob_loader.py:1006-1010,626-638,872-876`；`oob_stats.py:34` |
| 🟠 UI | `on_manage_terms` 每次点击弹两个窗口（同一 QAction 连两次同槽）；选 HOI4 路径 `setRootPath` 无 `setRootIndex` 致文件树视图错乱；AI 块合并丢标量值（`ai_will_do = 10` → `ai_will_do = {}`）；advisor 切换角色后旧 slot 残留并被保存；`focus_render` 多列瀑布布局不生效（`col_x` 从未推进，全画在 x=40） | `main_window.py:126,136,246,796,837-841,875-880`；`advisor_assign_dialog.py:607,930,1068,1171`；`focus_render.py:92-98,135,146` |
| 🟠 API/线程 | HTTP 线程直接调 Qt GUI（跨线程）；`/api/mcp/<tool_name>` 反射任意 core 方法；500 响应泄露 `str(e)`；`_safe_join` 不防符号链接 | `api_server.py:111-116,840-856,862-864,420-429` |
| 🟠 树模型 | `TreeNode.child_index` 对 stale parent 抛 `ValueError`（Qt 绘制时可崩视图）；`tree_model.data` 每次显示都调 translator/loc_manager，大树热路径慢；`_parse_ai_will_do` 序列化结构错误 | `tree_node.py:66-70`；`tree_model.py:145-230` |

### 2.3 测试体系与 CI 门禁（报告③）

**测试真实度：执行认真、内容虚胖**
- 不是假测试：每条用例造临时 mod 文件、真实 import `src/`、真实解析/写回/开 offscreen 对话框；`test_contracts.py` 的 `AtomicWriteTest.test_bom_rejected_and_file_untouched`、`test_infra.py` 的 `HealthCheckTest`、`test_map.py` 的 `StateEditOpsTest.test_set_state_owner_writes_file` 是高质量行为测试。
- 但水分在**断言的深度与分布**：
  - 分布畸形：`test_map.py` 101 方法 + `test_contracts.py` 85 方法 = 186/556 ≈ **33%**；**44 个文件只有 1 个测试方法**、19 个只有 2 个。
  - 大面积停在冒烟层：**73 处** `sidebar.list.count()==1` 式浅断言——即使整条保存/写回链路是坏的也全绿。
  - 错误路径近零：556 条里 `assertRaises` 只有 **6 处**（`test_contracts.py` 3、`test_mcp_categories.py` 2、`test_agent_tools.py` 1）。
  - 高度耦合私有成员（`_stat_labels`/`_slot_buttons`/`_on_*`），甚至断言缓存身份（`test_contracts.py:250`）。
  - `test_contracts.py`（1879 行）装 38 个类，是"杂项堆积"而非契约测试。
- **约 1/3 模块从未被测**：`src/` 241 个顶层模块里 **74 个从未被任何测试 import**，包括招牌功能 `focus_parser / focus_algo / focus_processor / focus_render / tree_model / validation / game_data / entity_scanner / content_types / oob_format` 等。

**静态门禁体检：一道可用但有洞、两道近摆设、最强一道在 CI 中关闭**

| 门禁 | 结论 | 关键证据 |
| --- | --- | --- |
| `check_write_discipline.py` | **可用但有洞**：AST 白名单式；别名 `opener = builtins.open` **检测不到**（实测漏 1/5）；`write_discipline_allowlist.json` 有 **11 个整模块豁免**（含 1046 行的 `main_window.py`、`ai_assist.py` 等），豁免理由靠自证、**无到期机制** | 实测；`write_discipline_allowlist.json` |
| `check_layer_deps.py` | **基本形同虚设**：只查硬编码 **34/253 ≈ 13.4%** 模块；且 `check_layer_deps.py:115` **显式放行算法层 import PyQt6**——而这恰是"算法层不得碰 Qt"契约的核心意图；只查单向、UI/CTRL 层怎么乱都不管 | `tools/check_layer_deps.py` |
| `check_file_budget.py` | **近乎纯摆设**：阈值 1200 行只扫 `src/*.py` 顶层（不查子包、`tools/`、949 行的 `ui_gap_probe.py`）；超限白名单 3 个（1260/1254/1251）里 **2 条是死条目**（`focus_view` 1189、`focus_view_ctrl` 1106 已不再超限）；白名单**永不过期** | `tools/check_file_budget.py:37` |
| `ui_gap_probe --ci` | **最强的一道门禁在 CI 里是关闭的**：只在 `settings.json` 存在时执行，CI 无 settings.json → SKIP 并记 `[OK]`；`tools/check_ui_coverage.py`（271 行）**完全没被 `verify_contracts.py` 调用** | `verify_contracts.py:134-145` |

**CI 可信度：执行路径真实、但被设计成少报**
- ✅ 真实跑全量 `unittest discover -s tests`，任一失败 → workflow 红；`setup.sh` 也用同一入口。
- ❌ UI 缺口门禁 CI 关闭；依赖不锁版本（`requirements-wsl.txt` 裸装 + Python 3.14 前沿）；`_count_test_methods()` **只打印不设门槛**（删 50 条用例 CI 照样绿）；无覆盖率度量；Windows 路径无 CI 守护（只有 ubuntu）；`ruff.toml` 只开 `E9/F63/F7/F82`（语法级，抓不到逻辑 bug，却给了"静态检查通过"的虚假安全感）。

### 2.4 API/MCP 接口与安全（报告④）

**网络层：方向正确、较安全（远程/未授权=低风险）**
- 绑定回环 `("127.0.0.1", port)`；token `secrets.token_hex(16)` 128-bit 随机；每请求先鉴权 401。
- 卫生问题（低）：非恒时比较 `==`（`:713`）；允许裸 token；token 明文打印 stdout（`:964`）且 `--token` 进 argv；CORS 全开 `*` + 允许 `Authorization`；无请求体上限/无并发限制；`if not self.token: return True` 死分支。

**工具暴露面：A+B 分类只是"隐藏"，不是安全边界（高）**
- `tools/call` 直接在全部 **173** 个工具中查找（`mcp_server.py:241`）；导航 `invoke_tool` 按名调任意工具含隐藏（`mcp_server.py:109-115`）；HTTP `/api/mcp/invoke_tool` 同样遍历全部 `build_tools()`（`api_server.py:828-838`）；文档自己也承认"隐藏工具都可经它调用，能力不丢"。
- 运行时无 `eval/exec/os.system/subprocess/pickle/yaml.load`（✅ 全目录 grep 未命中）；动态 `getattr` 仅来自内部固定 `kind`。
- 官方 `mcp` 库路径在 mcp 2.0.0 下是死代码（FastMCP 缺失，回退内置实现）；stdio 无鉴权（本地可接受，TCP 暴露则零认证）。

**接口边界：统一 ApiCore 概念清晰，但无强制边界（中）**
- HTTP 通用桥按**任意方法名** `getattr` 分派、无白名单（`:845-851`）——任何新加的内部方法自动变成 HTTP 端点；`inputSchema` 只是元数据，**无任何一方用它校验入参**；500 回传 `str(e)` 泄露内部细节；模块级单例（撤销栈/缓存）被多实例多线程共用，并发非线程安全。

**数据越权 / 写原版：高危（设计级缺陷，非卫生问题）**

做得好的部分：`write_file/get_file` 走 `_safe_join` 约束在 mod 根内（`:388-429`）；多数域写先 `ensure_file_in_mod`（`state_build_ops.py:29-56`）；`set_state_owner` 拒绝写"仅存在于游戏本体"的州（`state_edit_ops.py:125-160`）。

**高危：未做 mod 根目录约束的任意路径读写（均可经 HTTP/MCP 直接调用，仅需持 token）**

| 漏洞 | 证据 | 影响 |
| --- | --- | --- |
| OOB 工具 `path` 参数未校验，且 `isfile` 命中外部文件时不复制直接就地读写 | `api_core_ext/designers.py:492-516,574-657` | 可读任意文件；**可就地改写游戏本体文件**（绕过 ensure_file_in_mod） |
| `create_entity` / `create_focus_project` 的 `country` 无字符白名单，拼文件名 | `api_server.py:322-334,490-512` | `../..` 越出 mod 写任意 `.txt` |
| `write_localisation` 的 `tag` 未校验 | `api_server.py:531-540`；`project_wizard.py:51-57` | 任意路径写 yml |
| 生成器 `filename` 未校验 | `api_core_ext/generators.py:15-45,47-58` | 任意路径写 |
| `upload_entity_icon` 的 `icon_base`/`entity_id` 未校验 | `api_core_ext/media.py:40-58` | 任意路径写 PNG+gfx |
| `create_new_country_files` 的 `tag` 未校验 | `country_setup_dialog.py:356-379(:373)` | 任意路径写空文件 |
| `convert_dds` 允许绝对路径 | `api_core_ext/media.py:84-110` | 任意文件读+转写 |
| `analyze_error_log` 允许 `absolute_path` | `api_server.py:454-470` | 任意文件读 |

- 直接违反文档自述的"写回纪律/原版保护"（`docs/MCP与接口规格.md:13-14`）。
- **Token 是全权**：读/写/删任意 mod 文件、改游戏文件、任意位置建 mod（`create_mod`，`project.py:75-103`），无只读模式、无角色/范围隔离。**"持 token = 本机任意文件读写"**，远超"mod 编辑器"预期边界。

**风险评级（报告④）**

| 维度 | 评级 | 依据 |
| --- | --- | --- |
| 远程/未授权访问 | 低 | 127.0.0.1 + 128-bit 随机 token，无直接绕过 |
| 持 token 调用方（信任主体） | **高** | 多条任意文件读写/路径穿越 |
| 工具暴露面 | 高 | 173 工具全可经 invoke_tool + 通用桥调用 |
| 接口边界 | 中 | 概念统一但无白名单/入参校验 |
| 原版保护 | **高** | OOB 写路径可就地改写游戏文件；其余穿越路径可命中游戏目录 |
| **综合** | **高** | 网络层防护到位，接口层没守住"只写 mod、绝不写原版/任意文件"这条安全不变量 |

---

## 3. 交叉印证的关键发现（多路独立撞到同一信号）

1. **文档-代码数字系统性漂移**：模块数（235→252）、测试数（86/510→92/553–556）、MCP 数（159/168→173/176）、docs 数（12→14）、完成度（"全部落地"vs"大部分落地"）、工作区状态（"干净"vs 5 个文件修改）——四路子任务**独立**得出同一结论：文档快照落后于真实仓库，且"唯一执行总表"没有自动校验。
2. **mod/vanilla 边界两面失守**（同一不变量两端）：读时游戏覆盖 mod（报告②，loader 覆盖顺序颠倒）；写时可就地改写游戏本体（报告④，OOB 写路径绕过 ensure_file_in_mod）。"只写 mod、绝不写原版"这条项目最引以为豪的纪律，在数据读写两端都有破口。
3. **门禁"绿灯无代价"**：报告①承认 layer_deps 覆盖窄；报告③量化到 13.4% + PyQt6 放行 + 白名单永不过期 + UI 探针 CI 关闭。结论一致：三道静态门禁里 `layer_deps` 与 `file_budget` 几乎不可能红，`ui_gap_probe` 在 CI 里必然 SKIP。
4. **接口层无白名单/无入参校验**：报告②（`/api/mcp/<tool_name>` 反射任意方法、`_safe_join` 不防 symlink）与报告④（invoke_tool 全量可达、通用桥任意分派、schema 不校验）**互为佐证**——这是安全与质量问题的共同根因。
5. **核心逻辑零回归保护**：招牌功能（国策树解析）的 `focus_parser` 等 74 个模块无任何测试；而解析器恰恰存在可复现的静默截断。**最需要测试的地方恰好裸奔。**
6. **"B3 已全部落地"口径过宽**：明细里 P33/P34/P35/P36/P38 都是"✅ 大部分落地"，汇总却写"全闭环"；§2.5 自认缺少条件/剧本模拟、可用性预览、地图 QA、事件树、GUI 预览、DDS/TGA、DLC 加载等深水能力。**"全部落地"= 文件类型"可编辑/可写回"的广度，不包含预览—模拟—校验。**

---

## 4. 问题清单（按严重度 P0→P2，带证据）

### P0 — 立即处理（数据损坏 / 原版篡改 / 崩溃）

| # | 问题 | 证据 |
| --- | --- | --- |
| P0-1 | 接口层任意文件读写、可就地改写游戏本体（9 处入口，见 2.4 表） | `designers.py:492-516`；`api_server.py:322-334,490-512,531-540`；`generators.py:15-45`；`media.py:40-58,84-110`；`country_setup_dialog.py:373`；`project.py:75-103` |
| P0-2 | mod/vanilla 覆盖顺序颠倒：游戏数据覆盖 mod 数据，用户 mod 覆盖全部失效 | `oob_loader.py:281,331`；`ship_design.py:163,239`；`tank_design.py:138,212`；`plane_design.py:152,226` |
| P0-3 | PDX 解析器静默数据损坏（`#` 截断、字符丢弃、`list` 冲突崩溃、坏编码检测失效） | `pdx_parser.py:24,29,48-49,67-70,92-98`；`write_utils.py:104-114` |
| P0-4 | 撤销丢 BOM/CRLF 且失败即丢条目；格式化器误判注释内大括号、`.yml` 前置空行 | `undo_mgr.py:27,46-53`；`pdx_format.py:18-21,63-66,77` |
| P0-5 | OOB 保存写坏文件（丢外层 `}`、删模板、装备缓存清理 no-op） | `oob_loader.py:1006-1010,626-638,872-876`；`oob_stats.py:34` |

> **修复跟踪**：P0-1 ~ P0-5 的核心修复已落地并补回归测试，见 `docs/已知问题与修复.md`。
> 其中 P0-5 的“装备缓存清理 no-op”尚未处理，P0-3 的 `analyze_error_log` 绝对路径读、
> `import_unit_counters.output_dir`、`project.create_mod` 等条目仍在待办清单。

### P1 — 高优先（回归保护 / 门禁补齿 / 文档止血）

| # | 问题 | 证据 |
| --- | --- | --- |
| P1-1 | 74 个核心模块零测试 + CI 无覆盖率门槛 | 2.3 覆盖统计 |
| P1-2 | 浅冒烟升级为"改值→保存→读回断言"，并补错误路径（现仅 6 处 assertRaises） | `test_abilities.py:44` 等 73 处浅断言 |
| P1-3 | `check_layer_deps.py` 全量扫描 + 移除 PyQt6 放行；`file_budget` 白名单到期/趋势机制；`ui_gap_probe --ci` 接进 CI；`_count_test_methods` 设门槛并同步 86/510→92/553–556 | `tools/check_layer_deps.py:115`；`verify_contracts.py:134-145` |
| P1-4 | 文档数字同步（模块/测试/MCP 159/168/173、docs）并做成 CI 校验脚本 | 2.4 / 报告①④ |
| P1-5 | 完成度口径分级（"可树形编辑/兜底" vs "专用 UI" vs "完整读写+冒烟" vs "预览/模拟/校验"），纠正"B3 全部落地"表述 | 报告① 4.2 |
| P1-6 | 接口白名单注册（invoke_tool/通用桥）、`inputSchema` 真校验、token 改 `hmac.compare_digest`、收紧 CORS、加体积/并发限制 | `api_server.py:845-851,705,882-893,713`；`mcp_tools.py:736-753` |
| P1-7 | HTTP 线程直调 Qt 改队列投递；500 不再回传 `str(e)`；`_safe_join` 加 symlink 检查 | `api_server.py:111-116,862-864,420-429` |

### P2 — 中/低优先

| # | 问题 | 证据 |
| --- | --- | --- |
| P2-1 | 三个大文件拆分（generic_tree_editor 1260 / advisor_assign_dialog 1254 / map_canvas 1251） | `tools/check_file_budget.py` |
| P2-2 | 死代码清理（advisor 三个无调用函数、focus_render `cursor_x` 死变量） | 报告② 1.2/1.3 |
| P2-3 | 依赖锁版本 + Windows 路径 CI 守护（`setup.bat` 无验证） | 报告③ 3 |
| P2-4 | 文档笔误（`2025-08-25`→`2026`，两处）；官方 mcp 库路径死代码/文档过时 | `PROJECT_DOC.md:57,654`；`mcp_server.py:396,427-430` |

---

## 5. 建议行动路线

**第一档 · 立即止血（本迭代）**
1. 收口所有写路径到 `_safe_join` 类校验或字符白名单（`country/tag/filename/icon_base/path`）；OOB 路径命中外置文件时改为复制进 mod 再写——恢复"只写 mod、绝不写原版"不变量。
2. 修 4 个 loader 的 mod/vanilla 覆盖顺序（改为 mod 最后写）。
3. 为 PDX 解析/撤销/格式化/OOB 保存补上回归测试后修 bug（先写红再修绿）。

**第二档 · 短期（1–2 迭代）**
4. 测试优先级三件事：给 74 个未覆盖模块补行为测试 + CI 接 `coverage --fail-under`；把浅冒烟升级为写回断言 + 补 5–8 组 `assertRaises`；把三个静态门禁修到"能红"（layer_deps 全量去 Qt 放行、file_budget 到期机制、ui_gap 进 CI、计数设门槛）。
5. 文档数字脚本化校验（模块/测试/MCP/docs 计数进 `verify_contracts.py`），一次性把 159/168/173、86/510→92/553–556、12→14 全部同步。
6. 完成度按 4 级口径重新表述；把 P33~P36/P38 的"剩余项"逐条列清。

**第三档 · 中期（需用户拍板）**
7. §2.5 的 9 类候选缺口（条件/剧本模拟、可用性预览、地图 QA、事件树、GUI 预览、DDS/TGA、DLC 加载）取舍；P1~P4 计划（调研/剩余/已知限制/转文档/暂缓）；P23 国家历史豁免。
8. 放慢"新增类型"节奏，优先文档一致性与上述 P0/P1 债，再考虑预览—模拟—校验这条深水线。

---

## 6. 需用户拍板（Open Questions）

| # | 事项 |
| --- | --- |
| Q1 | P0 安全止血是否按"白名单 + 收口到 _safe_join"方案立即执行（会触及 API 契约）？ |
| Q2 | 文档数字是否统一：MCP 工具数 159/168 → 173（+3 导航=176）？模块/测试/docs 数是否按实测同步？ |
| Q3 | "B3 已全部落地"是否改为分级口径表述？ |
| Q4 | P23 国家历史豁免、P1~P4、§2.5 的 9 类候选缺口是否进入执行？ |
| Q5 | 在途 `B3 批二②`（5 个未提交文件）与本次评审产物是否提交 git？ |
