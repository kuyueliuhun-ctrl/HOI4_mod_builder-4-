# 问题与风险（整合总表）

> 日期：2026-08-23
> 来源：整合 `问题1.md` / `问题2.md` / `问题3.md`（同日三次独立评估），去重合并、
> 过时项已按最新事实修正。**本文件是问题与风险的唯一事实源**；
> 对应修复方法见 `docs/修复计划.md`（批次 F0~F10）；
> 功能性待办唯一总表 = `docs/未完成计划.md`。
> 严重度：🔴 高（正在产生实际维护成本）｜🟠 中（可预见风险）｜🟡 低（长期税）。
> 背景基线：双版本 `verify_contracts.py` 全绿（语法编译 / 契约测试 / 写入纪律 /
> 四层依赖），src/ 156 个 py 文件 58,916 行，测试约 395 用例，MCP 159 工具无重名。

---

## 🔴 高严重度

### P1 工作区大量未提交变更 ✅（2026-08-23 F0 已完成）

- **已处理**：74 项按 C1~C8 拆分为 8 个独立 commit（MCP 域扩展 / 设计器 /
  角色 / 科技事件 / 批次5~8 / 工作台接线 / 测试门禁 / 文档同步），
  `prototypes/`、`已分析.md`、`ui_gap_after.md` 已加入 `.gitignore` 默认不入库；
  双版本 `verify_contracts.py` 退出码 0。
- **原现象**：`git status` 74 项；45 个已跟踪文件 +5669/-1130 行；分支 `main` 比
  `origin/main` **ahead 4 个提交**；另有未跟踪新模块（MCP 142 工具 + UI 批次 1~9
  + §0.x 补充）。
- **影响**：已消除误操作不可恢复风险；后续可按 commit 粒度回滚。

### P2 三设计器复制粘贴式重复 ✅（2026-08-23 F4 已完成）

- **已处理**：新建 `src/designer_base.py`（`VariantDesignerBase`，690 行），
  吸收 `_save/_reset/模板/同款跨国家/槽位校验/高级字段/模块选择` 等共享方法；
  三 dialog 降为 `ship 486 + plane 473 + tank 441 = 1400 行 < 1600`。
- **数据层 Provider 注入**：`KIND/TITLE/SLOT_COLS/HOSTS_LOADER/STATS_FN/APPLY_*`
  等类属性由三个子类注入，`_save/_sync/_new_from_template` 等只保留单份实现。
- **回归**：`ShipDesignDialogSmokeTest / PlaneDesignDialogSmokeTest /
  TankDesignDialogSmokeTest / test_designers` 全部原样通过，双版本 verify 绿。

### P3 存量大文件（8 个超 1100 行，无预算防新增）

- **现象**（src/ 行数 top，实测）：
  | 文件 | 行数 |
  | --- | --- |
  | ~~`main_window.py`~~（F5 已拆） | ~~1547~~ → 1046 + `main_window_docks.py` 553 |
  | ~~`ai_loader.py`~~（F5 已拆） | ~~1449~~ → 459 + `ai_loader_crud.py` 1091 |
  | ~~`oob_loader.py`~~（F5 已拆） | ~~1290~~ → 1027 + `oob_stats.py` 313 |
  | ~~`bop_editor_dialog.py`~~（F5 已拆） | ~~1287~~ → 626 + `bop_editor_pages.py` 764 |
  | `generic_tree_editor.py` | 1260 |
  | `advisor_assign_dialog.py` | 1254 |
  | `map_canvas.py` | 1244 |
  | `focus_view.py`（拆分后）+ `focus_view_ctrl.py` | 1189 + 1092 |
- 四层门禁（`check_layer_deps.py`）只拦**反向依赖**，拦不住 UI/信号槽层内部
  继续膨胀；**F2 已加入行数预算门禁**（白名单只减不增）。
- **F5 已完成前 4 项拆分**；`generic_tree_editor.py` / `advisor_assign_dialog.py` /
  `map_canvas.py` 按计划为**触发式**，仍留在白名单。
- **风险**：回归影响面大、定位慢；`map_canvas.py` 这类核心画布出问题时，
  地编/区域/OOB 三个入口同时受影响。

### P4 UI 专用化缺口与验证欠账 ✅（2026-08-23 F3 已完成）

- **低分 UI 回归**：科技树/飞机/坦克/BOP/角色 已完成离线回归验证，
  结果见 **`docs/UI回归验证结果.md`**（全量契约绿 + 真实数据计数 + 缺口核对），
  均判定「已解决」。
- **缺口证据链**：`ui_gap_probe --types focus,tech,event,character,bop,state,
  country_history --max-files 0` 中 tech/event/character/bop 为 **0/0**；
  focus/state/country_history 剩余缺口已在 spec `note` 登记为长期收敛项
  （挂 `docs/未完成计划.md` 通用类型 F 批），非无名豁免。
- **角色豁免收敛**：character 规格已含 `characters.*.**`，未知块经
  ScriptBlockEditorDialog 结构化编辑，`--max-files 0` 缺口=0，无 note 豁免。
- **覆盖面**：94 种工作台类型中 12+ 种有完整专属 UI；F 批（通用类型逐个
  结构化/专用化）仍为长期任务（见 `docs/待制作功能.md`）。

## 🟠 中严重度

### P5 测试巨石单文件 + 文档数字漂移 ✅（2026-08-23 F1 已完成）

- **已处理**：`tools/split_tests.py` 按类名前缀把 104 类 / 341 用例拆分到
  `test_map.py`（20 类）/ `test_designers.py`（9）/ `test_ai.py`（11）/
  `test_bop.py`（5）/ `test_localization.py`（9）/ `test_workbench.py`（3）/
  `test_infra.py`（9），主文件降到 **1879 行 / 38 类 / 85 用例**；新增
  `tests/test_common.py` 公共设施（get_app / make_temp_mod / _send_move）。
- **数字指针化**：AGENTS §2 已改为「以 unittest 实时输出为准」；`verify_contracts.py`
  每次验证打印「契约用例总数」。拆分前后用例总数一致（341→341，全量 395）。
- 后续 GUI 测试脆弱性（QTest/offscreen 顺序）仍属长期税，未纳入本批。

### P6 UI 覆盖探针未进门禁 ✅（2026-08-23 F6 已完成）

- `ui_gap_probe.py` 新增 `--ci` 门禁：非豁免类型缺口 > 0 时退出码 1；
  focus/state/country_history 已加 `ci_exempt` 登记长期豁免。
- `verify_contracts.py` 新增第 6 步：本地有 `settings.json` 时轻量跑
  `event,tech,character,bop --max-files 5 --ci`；CI 无 settings 自动 SKIP。
- 双版本 verify 绿；`AGENTS.md §2` 与 CI workflow 已同步说明。

### P7 无通用静态检查门禁 ✅（2026-08-23 F2 已落地）

- 已新增 `ruff.toml`（E9/F63/F7/F82 起步）+ `verify_contracts.py` 的 ruff 静态检查
  步骤 + `tools/check_file_budget.py` 行数预算门禁；双版本 `verify_contracts.py` 绿。
- 顺带修复 ruff 发现的 36 个 F821 未定义名（`focus_view_ctrl.py` 缺 Qt/GUI/icon_ops/
  translator 导入、`tech_icon_ops.py` 缺 PIL.Image）。
- 后续可逐步收紧到 F401/F811/E7xx。

### P8 环境/依赖不够"开箱即用" ✅（2026-08-23 F7 已完成）

- 双版本验证：Windows `.venv`（Python 3.14.5）+ WSL 3.14.4。
- 已新增 `requirements.txt`、`docs/环境搭建.md`、`setup.bat`、`setup.sh`；
  Windows/Linux 均可一键建环境 + 安装依赖 + 跑验证。

## 🟡 低严重度（长期税）

### P9 Python 3.8 已 EOL ✅（2026-08-23 已升级）

- 已完成：`.venv` 从 3.8.10 升级为 **Python 3.14.5**（Windows），旧环境备份为
  `.venv_py38_backup`；WSL 3.14.4 一并验证通过；3.8 语法限制解除
  （新代码可用 walrus/match/list[str]/str.removeprefix 等）。
- 对应 `docs/修复计划.md` F9 已完成，回滚方式为改回 `.venv_py38_backup`。

### P10 仓库/工作区卫生 ✅（2026-08-23 F8 已完成）

- `已分析.md`、`ui_gap_after.md`、`prototypes/` 已入 `.gitignore`（默认不入库）；
  `__pycache__` 残留已清理（排除 .venv）。
- `docs/archive/` 收纳 `UI评分清单.md` / `UI评分评估结果.md`（历史评估保留原文）；
  `UI修复与建构执行方法.md` 顶部加「状态以本文档批次表为唯一事实源」指针。
- `问题.md`（问题唯一总表）与 `docs/未完成计划.md`（待办唯一总表）互相链接。
- 写入纪律豁免复核（2026-08-23）：全部仍必要，结论写入 allowlist `_comment`。
- tracked 大文件长期项：`AGENTS.md` 76KB、翻译 JSON 500KB 级可接受；
  `test_contracts.py` 已由 F1 拆分降为 1879 行。

### P11 已知功能限制散落 ✅（2026-08-23 F10 已完成）

- 全部散落限制已登记进 `docs/未完成计划.md` **P2.5 已知功能限制汇总**
  （数值估算 / 未定义 airframe 容错 / version_name 联动 / 兵牌图标 / 装备 IC 花费），
  重复项指向 P2-6/P2-7，来源标注 `问题.md` P11。

---

## 问题 → 修复批次总览

| # | 问题 | 严重度 | 修复批次（`docs/修复计划.md`） |
| --- | --- | --- | --- |
| P1 | 工作区未提交 | 🔴 | **F0 ✅ 已完成**（C1~C8） |
| P5 | 测试巨石 + 数字漂移 | 🟠 | **F1 ✅ 已完成** |
| P7 | 无静态门禁 | 🟠 | **F2 ✅ 已完成**（ruff + 行数预算） |
| P4 | UI 缺口/验证欠账 | 🔴 | **F3 ✅ 已完成**（回归验证 + 豁免收敛） |
| P2 | 三设计器重复 | 🔴 | **F4 ✅ 已完成** |
| P3 | 存量大文件 | 🔴 | **F5**（F2 门禁已落地，待拆分） |
| P6 | 探针未进门禁 | 🟠 | **F6 ✅ 已完成** |
| P8 | 环境门槛 | 🟠 | **F7 ✅ 已完成** |
| P10 | 仓库卫生 | 🟡 | **F8 ✅ 已完成** |
| P9 | Python 3.8 EOL | 🟡 | **F9 ✅ 已完成**（升级 3.14.5） |
| P11 | 限制散落 | 🟡 | **F10 ✅ 已完成** |
