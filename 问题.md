# 问题与风险（整合总表）

> 日期：2026-08-23
> 来源：整合 `问题1.md` / `问题2.md` / `问题3.md`（同日三次独立评估），去重合并、
> 过时项已按最新事实修正。**本文件是问题与风险的唯一事实源**；
> 对应修复方法见 `docs/修复计划.md`（批次 F0~F10）。
> 严重度：🔴 高（正在产生实际维护成本）｜🟠 中（可预见风险）｜🟡 低（长期税）。
> 背景基线：双版本 `verify_contracts.py` 全绿（语法编译 / 契约测试 / 写入纪律 /
> 四层依赖），src/ 156 个 py 文件 58,916 行，测试约 395 用例，MCP 159 工具无重名。

---

## 🔴 高严重度

### P1 工作区大量未提交变更 ✅（2026-08-23 F0 已完成）

- **已处理**：74 项按 C1~C8 拆分为 8 个独立 commit（MCP 域扩展 / 设计器 /
  角色 / 科技事件 / 批次5~8 / 工作台接线 / 测试门禁 / 文档同步），
  `prototypes/`、`已分析.md`、`ui_gap_after.md` 已加入 `.gitignore` 默认不入库；
  双版本 `verify_contracts.py` 退出码 0。
- **原现象**：`git status` 74 项；45 个已跟踪文件 +5669/-1130 行；分支 `main` 比
  `origin/main` **ahead 4 个提交**；另有未跟踪新模块（MCP 142 工具 + UI 批次 1~9
  + §0.x 补充）。
- **影响**：已消除误操作不可恢复风险；后续可按 commit 粒度回滚。

### P2 三设计器复制粘贴式重复（实测 83%~88% 相似）

- **现象**（difflib.SequenceMatcher 实测）：
  | 对比 | 相似度 |
  | --- | --- |
  | `ship_design_dialog.py` ≈ `plane_design_dialog.py` | 88% |
  | `plane_design_dialog.py` ≈ `tank_design_dialog.py` | 83% |
- `plane` 由 `ship` 复制改造、`tank` 又由 `plane` 复制（AGENTS §6.8 自述）；
  `designer_common.py` 只吸收了 `ModulePickerDialog` / `UpgradePointsCard` 少量公共件。
- **后果已实际发生**：`_block_map` 单行内联块漏解析、`_tag_of`/`_save_path`
  纯 `AAA.txt` 文件名不识别——两个 bug 都是**三处同修**（AGENTS §6.14）。
- **风险**：每修一个设计器 bug 实为修三个；三份副本行为漂移后，
  "同款跨国家同步""模板新建"等跨设计器功能会不一致。

### P3 存量大文件（8 个超 1100 行，无预算防新增）

- **现象**（src/ 行数 top，实测）：
  | 文件 | 行数 |
  | --- | --- |
  | `main_window.py` | 1547 |
  | `ai_loader.py` | 1449 |
  | `oob_loader.py` | 1290 |
  | `bop_editor_dialog.py` | 1287 |
  | `generic_tree_editor.py` | 1260 |
  | `advisor_assign_dialog.py` | 1254 |
  | `map_canvas.py` | 1244 |
  | `focus_view.py`（拆分后）+ `focus_view_ctrl.py` | 1189 + 1092 |
- 四层门禁（`check_layer_deps.py`）只拦**反向依赖**，拦不住 UI/信号槽层内部
  继续膨胀；**F2 已加入行数预算门禁**（白名单只减不增），存量名单仍待 F5 拆分。
- **风险**：回归影响面大、定位慢；`map_canvas.py` 这类核心画布出问题时，
  地编/区域/OOB 三个入口同时受影响。

### P4 UI 专用化缺口与验证欠账

- **覆盖面**：94 种工作台类型中仅 12+ 种有完整专属 UI（`docs/待制作功能.md`，
  批次 4~8 落地后 event/tech 已加入），其余大部分类型仍依赖通用树形编辑器；
  F 批（通用类型逐个结构化/专用化）是长期任务。
- **收尾状态修正**：早期评估记录批次 4~8 为 🔶（option 卡/MTTH、州字段
  resources/VP 表单化、BOP 区间 modifier 编辑、兵种属性表单、ScriptBlock
  结构化默认视图未完成）——**2026-08-23 批次 4~8 完整版已落地**（AGENTS §6.23，
  `tests/test_batch4~8` 存在），剩余工作是**回归验证**确认真实解决用户痛点，
  而非补实现。
- **UI 评估 v2 两类 🔴 严重问题**（词条不可见/不可编、强迫用户编辑原始 PDX）
  是否已清零需用 `ui_gap_probe.py` 复核，尚无全类型缺口=0 的证据链。
- **角色豁免口径偏宽**：portraits/roles 子树 87,938 条嵌套缺口按 note 豁免，
  长期挂起等于给 §4.12「展示 100%」开永久例外。
- **用户低分 UI 待回归**：科技树 4 分、飞机设计器 2 分、坦克设计器 2 分、
  BOP 4 分、角色编辑器 4 分——需验证最近批次是否真实改善。

## 🟠 中严重度

### P5 测试巨石单文件 + 文档数字漂移

- `tests/test_contracts.py` **7,839 行 / 352KB / 104 类 / 341 用例**，本批又改了它
  （+758 行）；7 个 `test_batch*` 文件（54 用例）已开始拆分但主文件仍在膨胀。
- 失败定位慢、多代理并行维护冲突高发、无法按域并行跑；GUI 测试本身依赖
  QTest 变通手段（手工构造 QMouseEvent、offscreen 多窗口顺序敏感），脆弱性
  已有多次踩坑记录（AGENTS §6.3-6 / §6.4-5）。
- **文档与事实不同步**：AGENTS §2 仍写「206 个用例 / 41 个测试类」，实测已
  341+54；批次表 / UI评估报告 / 未完成计划 / 对话.md 多处记录同一进度，
  靠人工回写。

### P6 UI 覆盖探针未进门禁

- `ui_gap_probe.py` + `UI_COVERAGE_SPECS` 机制良好，但需改专用 UI 后**手工**
  更新规格并运行；忘记同步时缺口静默扩大，直到下次人工运行才发现。

### P7 无通用静态检查门禁 ✅（2026-08-23 F2 已落地）

- 已新增 `ruff.toml`（E9/F63/F7/F82 起步）+ `verify_contracts.py` 的 ruff 静态检查
  步骤 + `tools/check_file_budget.py` 行数预算门禁；双版本 `verify_contracts.py` 绿。
- 顺带修复 ruff 发现的 36 个 F821 未定义名（`focus_view_ctrl.py` 缺 Qt/GUI/icon_ops/
  translator 导入、`tech_icon_ops.py` 缺 PIL.Image）。
- 后续可逐步收紧到 F401/F811/E7xx。

### P8 环境/依赖不够"开箱即用"

- 双版本验证已升级为 Windows `.venv`（Python 3.14.5）+ WSL 3.14.4；
  Windows 已新增 `requirements.txt`（2026-08-23，与 `requirements-wsl.txt` 同源）；
  仍缺环境搭建文档与一键 setup 脚本。

## 🟡 低严重度（长期税）

### P9 Python 3.8 已 EOL ✅（2026-08-23 已升级）

- 已完成：`.venv` 从 3.8.10 升级为 **Python 3.14.5**（Windows），旧环境备份为
  `.venv_py38_backup`；WSL 3.14.4 一并验证通过；3.8 语法限制解除
  （新代码可用 walrus/match/list[str]/str.removeprefix 等）。
- 对应 `docs/修复计划.md` F9 已完成，回滚方式为改回 `.venv_py38_backup`。

### P10 仓库/工作区卫生

- 根目录 `已分析.md` **60MB**、`ui_gap_after.md` 600KB 生成产物在工作区
  （易误提交）；`src/`、`tools/`、根目录残留多版本 `__pycache__`
  （cp38/313/314 混存）。
- `prototypes/`（44 文件：8+ 个 proto_*.py 原型与调研 md）未落库，去留未定。
- docs/ 下 5 份 UI 评估报告（UI评估报告 / UI评分清单 / UI评分评估结果 /
  UI树形缺口检测报告 / UI修复与建构执行方法）内容交叠，状态以哪份为准缺
  明确指针。
- tracked 大文件长期项：`test_contracts.py` 352KB、`AGENTS.md` 76KB、翻译
  JSON 500KB 级（后两者属数据/文档文件，可接受）。
- 写入纪律豁免 21 处 + 3 处二进制写，应随重构定期复核是否仍必要。

### P11 已知功能限制散落（未汇总进总表）

- 数值面板为基础值估算（未含科技/将领修正）；部分设计引用未定义 airframe
  时容错降级；OOB version_name 与设计改名后的引用联动未校验；兵牌图标
  未接单位标牌库（回退黑底占位）；编制装备 IC 花费只统计件数。
  均已在 UI/文档标注，属诚实取舍，但散落多处、未进 `docs/未完成计划.md`。

---

## 问题 → 修复批次总览

| # | 问题 | 严重度 | 修复批次（`docs/修复计划.md`） |
| --- | --- | --- | --- |
| P1 | 工作区未提交 | 🔴 | **F0 ✅ 已完成**（C1~C8） |
| P5 | 测试巨石 + 数字漂移 | 🟠 | **F1** |
| P7 | 无静态门禁 | 🟠 | **F2 ✅ 已完成**（ruff + 行数预算） |
| P4 | UI 缺口/验证欠账 | 🔴 | **F3**（回归验证 + 豁免收敛） |
| P2 | 三设计器重复 | 🔴 | **F4**（专项） |
| P3 | 存量大文件 | 🔴 | **F5**（F2 门禁已落地，待拆分） |
| P6 | 探针未进门禁 | 🟠 | **F6** |
| P8 | 环境门槛 | 🟠 | **F7**（requirements.txt 已建） |
| P10 | 仓库卫生 | 🟡 | **F8** |
| P9 | Python 3.8 EOL | 🟡 | **F9 ✅ 已完成**（升级 3.14.5） |
| P11 | 限制散落 | 🟡 | **F10**（极小） |
